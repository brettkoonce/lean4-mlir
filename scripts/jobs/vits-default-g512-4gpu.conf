# ViT-Small (DeiT-S) / ImageNet-1k, VERIFIED path (Lean → StableHLO → XLA/PJRT), 4 GPUs.
#
#     scripts/supervise.sh vits-default-g512-4gpu
#
# 4 replicas × bs 128 = global 512, 2,502 steps/epoch — DeiT's batch and the driver's default, so
# this config overrides nothing about the recipe.
#
# ⭐ MEASURED 2026-08-27, 40 steps: 531 ms/step fp32, 323 bf16 ⇒ 113 h (4.7 d) and 71 h (3.0 d)
#   for 300 epochs incl. 37.5 s/epoch eval + ckpt.
#
# ⚠⚠ NO LEAN_MLIR_MEM_FRACTION, AND THE FIRST VERSION OF THIS FILE SET IT as headroom (peak
#   10.27 GiB, 88 % of the default arena). It is not free headroom: at 0.97 ConvNeXt's bf16 arms
#   die CUDA_ERROR_OUT_OF_MEMORY where they complete at the default, because a 97 % pool starves
#   what lives outside it. Probed both ways this net reads 528 → 319 set and 531 → 323 unset, so
#   the option bought nothing. ⭐ Raise it only for a graph that does not otherwise fit — ViT-B is
#   the one net here that qualifies. ▶ runs/2026-08-28-convnext-sb-jobs/

# ▶ §2c N1: this must equal the second dash-field of the filename. supervise.sh refuses otherwise.
# ⚠ Net token `vits`, not `vit-s` — the engine splits the filename on dashes, so the exe's
#   hyphenated size (N4) cannot be the job token. Same reason as `vitb`.
RECIPE=default
DEVS="0,2,3,4"          # the AER-clean four; idx 1 and 5 are excluded (reference_ares_pcie_aer)
EPOCHS=300              # vitSImagenetConfig.epochs — the DeiT schedule length

CMD=(.lake/build/bin/vit-s-imagenet-verified data)

ENV_EXTRA=(
  PJRT_PLUGIN=".venv/lib/python3.12/site-packages/jax_plugins/xla_cuda12/xla_cuda_plugin.so"
  PJRT_REPLICAS=4
  LEAN_MLIR_REPLICAS=4
  LEAN_MLIR_VARIANT=adamdp128x4wxclipdrop
  LEAN_MLIR_BATCH=128                # PER REPLICA ⇒ global 512
  PJRT_FFI_RESIDENT=1
  SHIM_WORKERS=8
)

TEMP_MAX=78
TEMP_RESUME=62
REST_EPOCHS=""          # temperature governs

CKPT_EPOCH_FILE=".lake/build/vitsin_adamdp128x4wxclipdrop_ckpt_xla.bin.epoch"
epoch_now() {
  [ -f "$CKPT_EPOCH_FILE" ] || { echo 0; return; }
  local n; n="$(tr -cd '0-9' < "$CKPT_EPOCH_FILE")"
  echo "${n:-0}"
}

PRECHECK=precheck_vits
precheck_vits() {
  [ -x .venv/bin/python3 ] || { echo "missing .venv/bin/python3 (the pinned env)"; return 1; }
  [ -f verified_mlir/vitsin_adamdp128x4wxclipdrop_train_step.mlir ] || {
    echo "missing the 128×4 render — lake build LeanMlir.Proofs.Codegen.ViTRenderB"; return 1; }
  # ⚠ The driver opens `<slug>_fwd.mlir` BY NAME for its eval pass, and `vitsin_drop_fwd` does not
  # satisfy it — that one declares 24 %dp<n> mask inputs an eval forward has no values for. This
  # was found by running the trainer, not by a build-time check; no gate covers an artifact NAME.
  [ -f verified_mlir/vitsin_fwd.mlir ] || {
    echo "missing verified_mlir/vitsin_fwd.mlir — the eval forward the driver opens by name"
    echo "(vitsin_drop_fwd is NOT it: 24 extra mask inputs)"; return 1; }
  [ -f jax/.lake/build/generated_vit_tiny_imagenet_shim.py ] || {
    echo "missing ViT's OWN tfds shim — scripts/gen_shims.sh"; return 1; }
  printf '%s\n' "${ENV_EXTRA[@]}" | grep -qx 'PJRT_FFI_RESIDENT=1' || {
    echo "PJRT_FFI_RESIDENT=1 is missing from ENV_EXTRA — opt-in, defaults OFF, silent."; return 1; }
  printf '%s\n' "${ENV_EXTRA[@]}" | grep -qx 'SHIM_WORKERS=8' || {
    echo "SHIM_WORKERS=8 is missing from ENV_EXTRA."; return 1; }
  printf '%s\n' "${ENV_EXTRA[@]}" | grep -qx 'LEAN_MLIR_BATCH=128' || {
    echo "LEAN_MLIR_BATCH must be 128 to match the render named by LEAN_MLIR_VARIANT."; return 1; }
  return 0
}
