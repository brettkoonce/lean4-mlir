# Docstring citations that do not resolve to any declaration.
#
# ⚠ These are NOT one thing, and an earlier version of this file said they were.
# `--update-baseline` writes a bare list, and a bare list cannot tell a stale
# citation from a name a docstring PROPOSES as future work. Triaged by hand into
# the three kinds below. Each line is `name  # why`.
#
# This list may SHRINK, never grow. A new entry appearing without a category is a
# rename that just orphaned a docstring with CI green, which is the hole this gate
# exists to close — categorise it or fix it, do not just re-record it.

# ── PROPOSED: named as work not yet done. The prose says so; nothing is broken.
#    A reader is not misled, so these are not debt. They leave when the work lands.
globalAvgPool_has_vjp        # SE.lean:140 "you'd need to add …" — sketch of the SE fold
broadcast_has_vjp            # SE.lean:140, same sentence, the adjoint of GAP
cnn_has_vjp_at3              # CNN.lean:2337 explicitly "(future)"
r34_float_close              # Resnet34FloatBridge.lean:22 "the remaining work for a whole-net …"
convWGrad_faithful           # StableHLO.lean:7885, a theorem that would validate the wgrad render

# ── OUT OF SCOPE: exists, but in a lib this gate does not import.
#    Widening the import set to reach it costs more than the check is worth here.
chk_relu_has_vjp_correct     # real, tests/comparator/ChallengeArch.lean — comparator lib

# ── DEBT: cited as if it exists, and it does not. These are the real finds, left
#    here rather than guessed at: picking a plausible replacement would put a NEW
#    false citation in the docs, which is worse than a flagged one. Each needs an
#    author who knows which theorem was meant.
cnnVerified_has_vjp_at       # MainMnistCnnVerified.lean:16 — cnn_has_vjp_at? cnnConcrete_has_vjp_at?
flatten_has_vjp              # MainVjpOracleConv.lean:3 — no flatten VJP exists under any name
maxPool_has_vjp              # MainVjpOracleConvPool.lean — maxPool2_has_vjp3 is the likely referent
convStrided_faithful         # TestStrided1x1.lean
kernel_faithfulness_probe    # TreeReduceBridge.lean:33 — the .py script, cited bare elsewhere
