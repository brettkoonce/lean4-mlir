% This file makes a web version of the blueprint
% It should include all the \usepackage needed for this version.
% The template includes standard AMS packages.
% It is otherwise a very minimal preamble (you should probably at least
% add cleveref and tikz-cd).

\documentclass{report}

\usepackage{amssymb, amsthm, amsmath}
\usepackage{hyperref}
\usepackage{graphicx}
\graphicspath{{figures/}}
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta, calc, fit}
\usepackage{pgfplots}
\pgfplotsset{compat=1.16}
\usepackage[showmore, dep_graph]{blueprint}


\input{macros/common}
\input{macros/web}

% plasTeX compiles each TikZ/vector figure in a standalone latex run whose
% preamble does NOT pick up the blueprint package's web-only \home/\github/
% \dochome (they're undefined there, which crashes the figure's image compile).
% Guard them as no-ops: \providecommand leaves the real HTML-render defs intact
% and only kicks in for the image-compile context. See the figures in §3.1/§4.1.
\providecommand{\home}[1]{}
\providecommand{\github}[1]{}
\providecommand{\dochome}[1]{}

\home{https://brettkoonce.github.io/lean4-mlir}
\github{https://github.com/brettkoonce/lean4-mlir}
\dochome{https://brettkoonce.github.io/lean4-mlir/docs}

\title{Verified Deep Learning with Lean 4 \\[0.6ex]
  {\large Formal Backpropagation from MLP to Attention, via MLIR}}
\author{Brett Koonce}

\begin{document}
\maketitle
\input{content}
\end{document}