# Audit v2 — shared facts (read first)

You are one of seven auditors, each on one slice of a Lean 4 repository. You run TWO lenses over your
slice and write ONE report file with two parts. Report findings; **do not edit anything in the repo.**

## Repository facts

- Repo root: `/home/skoonce/lean/klawd_max_power/lean4-jax-mlir`. Branch `proof-cleanup`, clean tree
  for `LeanMlir/`. Toolchain `leanprover/lean4:v4.34.0`, Mathlib tag `v4.34.0`.
- Mathlib is vendored at `.lake/packages/mathlib/Mathlib` (a full git clone) — **grep it directly**,
  it is ground truth. Batteries at `.lake/packages/batteries`. Core at
  `~/.elan/toolchains/leanprover--lean4---v4.34.0/src/lean/`.
- The oleans are freshly built for HEAD (`lake build Certs LeanMlir` just passed). You may typecheck
  scratch files with `cd <repo> && lake env lean <scratch>/x.lean`, importing only the modules you
  need. **Never run `lake build`, never `lake clean`, never edit files under the repo.** Six other
  auditors share this 32-core box: run at most ONE `lake env lean` at a time, and prefix it with
  `timeout 600`. Scratch files go in your own directory under
  `/tmp/claude-1000/-home-skoonce-lean-klawd-max-power-lean4-jax-mlir/930c08a6-fb00-48bf-888b-92d928930a22/scratchpad/<your-slice>/`.
- ⚠ The local `grep` may honour .gitignore; for `.lake/packages/mathlib` use `grep -r` with explicit
  paths (that works) and if a search looks suspiciously empty, confirm with `find … -exec grep`.
- ⛔ Kernel/elab hazards learned here: `simp only [<rfl lemmas>]` on big defs can make the kernel unfold
  everything; never put `den` in a simp set; literal-width `rfl` steps can hit kernel deep recursion.
  If a proposed replacement would do that, say so.
- **Generated files** — skip any file whose header says `Generated by`, `AUTO-GENERATED`,
  `DO NOT EDIT`, or `@generated` (e.g. `Certificates/*Scorecard*`, `SmoothingDecChunk*`,
  `SmoothingNetWitness`, `LipschitzCertInstance`'s data half, `Training/TrainedCnn{Seal,Witness}`,
  `TrainedLinearDescent`, `IbpConvScorecard*`). Check the header yourself. If a generator emits the
  problem, report the generator script (under `scripts/`), not the output.
  `Foundation/IntervalBound.lean`, `ListDot.lean`, `Codegen/ViTRenderB.lean`, `MlirCodegen.lean`
  mention generation in prose but are hand-written — audit them.
- `tests/comparator/Challenge*.lean` use `sorry` on purpose.

## History you must not repeat

A first Mathlib-reuse audit ran 2026-09-18 (at `aed84250`) and ~24.7k lines were removed; then a
census of audit-only declarations, then a proof-quality cleanup (`planning/proof_cleanup.md`), then
the CertLayer re-spelling (`planning/certlayer_nets.md`). 106 commits, ~22k lines churned since.

- `planning/mathlib_reuse_audit.md` — the v1 report. Its Status table lists every landed batch; its
  "Deferred on purpose" paragraph and per-item notes list things **checked and rejected**
  (e.g. `Mat.mulVec`/`dense`/`reindexCLM`/flatten are `rfl`-equal to Mathlib's but swapping gains no
  lines; `dpIterate_lockstep`; `sigmoidScalar := Real.sigmoid`). Read the section for your slice.
  **Do not re-report a landed or rejected item** unless the code changed and the rejection reason no
  longer holds — then say what changed. Items v1 marked *likely* or *lead* that are STILL PRESENT
  are fair game: resolve them (verify or kill).
- `planning/proof_cleanup.md` §1 (done) and §4 (checked and set aside), and
  `planning/proof_cleanup_audits/*.md` (the proof-quality audit — brittleness, heartbeats) — don't
  redo that audit either; it's a different lens.
- `planning/certlayer_nets.md` — in-flight thread; findings that would collide with it get flagged
  as "overlaps certlayer_nets".
- Consequential lesson from v1: every hand-rolled lemma it found was ALREADY in Mathlib when written.
  "Mathlib didn't have it yet" is not an excuse. Also v1 found the repo's own shared kits (`CertLayer`,
  `pdiv_clm`, `flat_differentiable_comp`, `stageHasVJPAt`, `BnPairTiedB`, …) under-used — check whether
  NEW code since v1 bypasses them.

## Pinned names are expensive

Many declarations are pinned in `tests/AuditAxioms.lean`, cited by `blueprint/src/content.tex` (the
book), `formalization.yaml`, `.github/workflows/certs*.yml`, `scripts/gates/gen_comparator_tier.py`, and
docstrings. When you propose a rename, a move, or a deletion, **count** those citations
(`grep -c`) and state the cost. Moving a lemma into a root file (`Foundation/Tensor.lean` has ~423
downstream modules) costs rebuild time; say how many modules import the target.

## Output

Write your report to `<your scratch dir>/REPORT.md`, then reply with a ≤ 15-line summary: counts per
lens, the top 3 findings of each, and the report path. The report has:

```
# <slice> — audit v2

## Part A — Mathlib / core / in-repo reuse
<findings, format below, ranked most valuable first>
<summary paragraph: declarations examined, replaceable, approx lines>

## Part B — Clarity and abstraction boundaries
<findings, format below, ranked>
<summary paragraph>

## Checked, not findings
<one line each: what you looked at and why it isn't a finding — this saves the next auditor time>
```

A short list of verified findings beats a long list of maybes. A clean slice is a useful result —
say so plainly.
