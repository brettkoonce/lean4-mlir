# Ledger — slice J (LeanMlir.lean + LeanMlir/*.lean, not Proofs/)

`file:line | kind | old claim | what the statement says | new text | how checked`

| site | kind | old claim | statement | new text | checked |
|---|---|---|---|---|---|
| LeanMlir.lean:80 | overclaim §1(b) | "Two things are proved for every net … Descent — that step provably decreases the loss" | `linear_sgd_descends` (SgdDescentLinear.lean:206): W only, one example's CE, abstract step `W − lr•gh` with η-inexact gh, hyps `hsmall`, `h1`, `h2`; concl. drop ≥ lr·Σgrad²/2. Other descent theorems: `mlp_hidden/input_sgd_descends`, `cnn_conv{1,2}[_bias]_sgd_descends`, `cifar8_lastConv_sgd_descends` (margin hyps). None for R34/R50/MNv2/MNv4/B0/ConvNeXt/ViT | "The linear classifier shows the two kinds of result …"; Descent item states one example, weight matrix, step-size + two dominance hyps; new paragraph enumerates the MLP/CNN/CIFAR-8 descent theorems and names the seven nets with none | `grep -rn "^theorem [a-z_0-9]*descends" LeanMlir/Proofs/`; read docstrings+signatures; namespaces `grep -n "^namespace" SgdDescent*.lean` (all `Proofs`); 650 lines = `wc -l LinearTrainStep LinearFold SgdDescentLinear` = 647 |
| LeanMlir.lean:112 | overclaim §1(c) | "Whole-network VJPs — resnet34…, mobilenetv2HasVJPAt, efficientnetHasVJP, convnextHasVJP, vitFullHasVJP" | the last four are: stem+skip IR+no-skip IR+GAP+dense (MNv2), stem+two MBConv (B0), stem+two blocks (ConvNeXt), weight-shared `vitBody` (ViT) | lists the seven full-depth witnesses: `Proofs.resnet34ForwardBFullHasVJPAt`, `Proofs.resnet50ForwardBFullHasVJPAt`, `Proofs.mobilenetv2ForwardBFullHasVJPAt`, `Proofs.StableHLO.mobilenetv4ForwardBFullHasVJPAt` (batched, smooth-point), `Proofs.efficientnetForwardBFullHasVJP` (batched), `Proofs.convNextForwardTChHasVJP`, `Proofs.vitForwardKVHasVJP` (one example; ViT depth k, per-block params); last three only `0 < ε` | `grep -rnE "(def\|theorem) <name>\b" LeanMlir/`; namespace by `awk` over `^namespace/^end` up to the def line (MNv4 in `Proofs.StableHLO`, B0 at :371 outside StableHLO section 269–361); read each signature: HasVJPAt + `*SmoothAt*` for R34/R50/MNv2/MNv4 (R34/R50 smooth structs include `pool` field), HasVJP + ε hyps for B0 (`hεw`), ConvNeXt (23 ε hyps), ViT (`hε`); ConvNeXt/ViT `x : Vec (3*224*224)` / per-example |
| LeanMlir.lean:114 | overclaim §1(a) | "for every net … each emitted parameter-update node denotes the certified descent step" | capstones `r34/r50/mnv2/mnv4_net_tiedB`, `efficientnet_net_tiedG`, `cnx_net_tiedGB`, `vit_net_tiedGB`: every parameter GRADIENT node denotes certified batched Σ_n gradient at the constructed cotangent; one replica (each docstring); B0: "optimizer update and bf16 `*GradBBf16` nodes are outside it", drop-free; ConvNeXt/ViT: drop-free. Optimizer per-op: `sgdParamF_faithful`, `mom_pair_faithful`, `adamW_triple_faithful`, `lamb_triple_faithful` (all `Proofs.StableHLO`). Sync twins `*_net_syncTiedB/G` exist for R34, R50, MNv2, MNv4, B0 only | gradient nodes, optimizer per-op theorems named, "stated at one replica, in f32, on the chain without drop-path"; bf16 → `Bf16GradNodes`; sync twins for the five BN nets at global batch R·N; `*drop*` chains not named. Kept verbatim: "at the batch BatchNorm and the ImageNet head its artifacts run" — deferred §5 part: the B0 10-class head (`efficientnet_net_tiedG` `t : Vec (N*(1*10))`) — phrase left for the nCls row | `grep -rn "theorem <capstone>\b"`; read docstrings/signatures; `grep -rhoE "theorem [a-zA-Z_]*(mom\|sgd…)faithful"` in Proofs/Codegen; `grep -n "^theorem [a-z0-9_]*net[a-zA-Z_]*" Nets/*/*Sync*.lean` (5 nets); Bf16GradNodes module doc read |
| LeanMlir.lean:108 | stale §1(j) | "over 73 of the headline theorems" | configs list 13 + 39 + 35 | "87" | `python3` len of `theorem_names` in tests/comparator/config{,-arch,-tier}.json = 13/39/35, 87 unique; AuditAxioms not cited on the page (`grep -c "^#print axioms" tests/AuditAxioms.lean` = 1610 for the record) |
| LeanMlir.lean:122 (R34 bullet) | stress/wording | "its step IS the single-device step at the global batch" | `r34_net_syncTiedB`: mean over replicas of gradient node = single-device node at R·N | "whose all-reduced gradient nodes are the single-device nodes at the global batch" | ResNet34SyncStepTieB module doc read |
| LeanMlir.lean:135 | process (tiers) + wording | "The four conv nets … (T2 graph) … (T1 VJP) … (T3 train-step tie) … (T6 …)" | the four = R34, R50, MNv2, MNv4 | names the four nets; tier labels replaced by what each file holds | `ls` each `*BackB0/FullB/FullBVJP/FullBSeal/StepTieB/WholeBackCertifiedTieB/SyncB/SyncStepTieB` per net (R34's backward file is `ResNet34BackCertifiedTieB`) |
| LeanMlir.lean:145 | wording | "linear, MLP and CNN capstones" | CIFAR-8 last-conv descent also exists | adds "CIFAR-8 last-convolution" | `grep` above |
| LeanMlir.lean:147 | category word | "`FloatClose` for the toy nets; past them the gap stays trusted" | FloatBridge: dense/relu bounds; per-layer bridges Bn/ConvMixed/Depthwise; FloatClose in FloatClose.lean composes | "rounding bounds for dense and ReLU layers, with per-layer bridges for BatchNorm, convolution and depthwise convolution and `FloatClose` to compose them; where no bound is stated, the gap stays trusted" | `ls LeanMlir/Proofs/Float/`; FloatBridge module doc |
| LeanMlir.lean:62 | process | import `--` comment "since 2026-07-07: it rotted …" | — | not changed: `--` comment, not a docstring | — |
| VerifiedAttack.lean:172 `attackPgdMlp` | overclaim §2(f) | "attacks through IREE with the proven `mlpInputGrad` VJP kernel" | body writes `genMlpPgdStep` text, `compileVmfb` (iree-compile) + `LowererSession.create`; VerifiedPgdGen module: kernels hand-typed, no tie | "runs PGD through IREE with `genMlpPgdStep`, a hand-typed StableHLO kernel that follows the formula of `Proofs.mlpInputGrad` (no theorem ties the kernel's text)"; "Phase-3", planning ref removed. Report's "runtime default is PJRT" does not apply here: this path calls iree-compile directly | read body (lines 205–240); `grep -n "def compileVmfb" -A12 VerifiedTrain.lean`; `mlpInputGrad` in Proofs/Foundation/BackwardMaps.lean, namespace `Proofs` |
| VerifiedAttack.lean:278 `attackPgdSpectralMlp` | overclaim §2(f) | "The verified CE gradient stays in the proven kernel" | training step = proof-rendered train step; PGD = `genMlpPgdStep` (line 314) | "The training gradient comes from the proof-rendered train step; the projection is host-side …, and the PGD kernel is the hand-typed `genMlpPgdStep`" | read body |
| VerifiedAttack.lean:396 `attackPgdConvNet` | overclaim §2(f) | "`genKernel` — the full proven backward" | `genKernel` = `genCnnPgdStep`/`genCifarPgdStep`, hand-typed | "a hand-typed StableHLO kernel that computes the input gradient `dx` (… following the backward ops of the net's train step); no theorem ties the kernel's text" | VerifiedPgdGen.lean module doc + defs |
| VerifiedAttack.lean:676 `attackPgd` | overclaim §2(f) | "`genLinearPgdStep` StableHLO kernel (the proven `dx = (softmax−onehot)·Wᵀ` VJP)" | hand-typed kernel | "a hand-typed StableHLO kernel that follows the formula … (no theorem ties the kernel's text)" | as above |
| VerifiedPgdGen.lean:9, :86 | overclaim §2(f) | "the proven linear input-VJP" / "the proven `mlpInputGrad` VJP" | kernel text written by hand | "the formula of the linear input-VJP, written by hand" / "the formula of `Proofs.mlpInputGrad` written by hand" | module docstring of same file |
| VjpOracleNets.lean:3 module | overclaim §2(f) | "trains each net one step through the verified path (Lean → MLIR → IREE)" | tests/vjp_oracle/phase3/Main*.lean call `VjpOracle.<net>.train` = `NetSpec.train` (Train.lean:1302) → `MlirCodegen` | "through the Lean reference codegen (`NetSpec.train`, which lowers the `NetSpec` to MLIR at run time through `MlirCodegen`) … Both backwards are hand-written: the oracle checks them against each other, not against the verified_mlir/ artifacts or a theorem" | `cat tests/vjp_oracle/phase3/MainVjpOracleConv.lean`; `grep -n "^def train" Train.lean` |
| VjpOracleNets.lean:25–182 (11 net docstrings) | overclaim + stale | "tests `reluHasVJP`", "tests `flattenHasVJP`", "tests `maxPoolHasVJP`", "the one axiom family" | oracle runs MlirCodegen backwards; `flattenHasVJP`, `maxPoolHasVJP` do not exist; no axioms | "<op> backward (math side: `Proofs.X`)" with X ∈ denseHasVJP, reluHasVJP, vjpComp, conv2dHasVJP3, convBnHasVJP, maxPool2HasVJP3, biPathHasVJP, transformerBlockHasVJPMat, elemwiseProductHasVJP; "axiom family" → "op kinds" | `grep -rlE "(def\|theorem) <n>\b" LeanMlir/Proofs` for each; namespaces MLP/Tensor/CNN/Attention all `Proofs` (top level) |
| ViTRender.lean:3 module | overclaim §2(f) + stale | "ch10 ViT — verified-faithful … each line what the proven-faithful emitter produces … validated by … TestSDPA" | hand-written String emitter; no theorem ties its text; `import LeanMlir.ViTRender` only in LeanMlir.lean + 9 tests; apps mention it only in docstrings; ViT is chapter 9 | "hand-written StableHLO fragments … not `pretty` of a proven graph, and no theorem ties its text; verified_mlir/ ViT steps come from LeanMlir/Proofs/Codegen/ViTRender.lean … only tests import this file (lists 9) … each fragment spells an op the proof side also has (`Proofs.StableHLO.SHlo.softmaxRowF`, …)". Report's "legacy" label and TestViTAdamTie claim not used (TestViTAdamTie does not import it) | `grep -rn "import LeanMlir.ViTRender"`; `grep -rnE "ViTRender\.[a-zA-Z]+"` in apps (docstring-only); `\chapter` list in content.tex; `grep -nE "\| (softmaxRowF\|softmaxRowBack\|geluF) " StableHLO.lean` |
| ViTRender.lean:559 `emitGradAllReduce`, :589 `emitAdamVDP`, :697 | process | "⭐ Since 4d piece 2 (2026-09-07) …", "planning/archive/xla_pjrt_ladder.md §11/§10.4", "(Phase 2)" | — | present-tense: proof-side renders call `Proofs.StableHLO.prettyAllReduceMean`; refs dropped | `grep -n "def prettyAllReduceMean\|def allReduceMeanText" Proofs/Codegen/StableHLOPretty.lean` |
| VerifiedSmoothing.lean:3 module, :95 `clopperPearsonLower` | overclaim | "a **sound** Clopper–Pearson lower bound (a genuine 1−α lower bound, not an approximation …)" / "The SOUND 1−α lower bound" | Float code, no proof; 60-step bisection returns `0.5*(lo+hi)` | "the exact binomial bound, not a normal approximation … solved by 60-step bisection. None of this is proved, and the bisection returns the midpoint of its final bracket rather than rounding toward the conservative end" | read `clopperPearsonLower`, `betacf` (eps 3e-12, 200 iters), `lgammaF` |
| VerifiedSmoothing.lean:111 `smoothCertify` | wrong fact + process | "a unanimous vote still only certifies `p_A ≤ α^(1/n)`"; planning ref + §3; "honest" | CP lower bound at k=n is α^(1/n): certifies p_A ≥ α^(1/n) | "a unanimous vote certifies only `p_A ≥ α^(1/n)`"; planning ref removed | Beta(n,1) α-quantile = α^(1/n) |
| MlirCodegen.lean:9 module | overclaim | "Covers every `Layer` constructor" | 28 of 50 `Layer` constructors appear as `.<ctor>` in MlirCodegen; forward walk's fallback emits `// UNSUPPORTED LAYER` | "Emits 28 of `Layer`'s 50 constructors …; the other 22 — (all 22 listed) — have no emitter here; the forward walk writes an `UNSUPPORTED LAYER` comment for them". Report listed 5; the true set is 22 | awk the `inductive Layer` block for `\| ctor`, then `grep -c "\.$c\b" MlirCodegen.lean` per ctor; `grep -n "UNSUPPORTED LAYER"` |
| MlirCodegen.lean:8181 `fpnLossProbeModule` | overclaim + stale | "the conv heads feeding this are verified convBn, validated separately on ROCm" | MlirCodegen is the unverified reference path; box is CUDA | "the conv layers that feed it in the train step are not part of this probe" | module docstring "Unverified" |
| GradcheckHelpers.lean:12 | stale | "Used by the ch10 ViT de-risk tests (TestSDPA/TestMHSA/TestViTBlock)" | importers: tests TestSDPA, TestMHSA, TestViTBlock, TestViTTiny, TestSgdRenderTie; demos MainAraslSigns, MainPlantLeaf, probes/MainSegLossProbe use `parseFloat?` only | lists them | `grep -rln "GradcheckHelpers\|ViTGradcheck" tests apps demos LeanMlir` |
| VerifiedAttack.lean | process | 9 lines: "Phase-3" ×6, planning/archive refs ×4, "the research lever"/"gap-shrinking lever" labels | — | removed | ds.py hit list |
| VerifiedPgdGen.lean | process | 5 lines: "Phase-3" ×4, planning ref, ⚠ | — | removed; warning kept as a plain sentence | ds.py |
| VerifiedSmoothing.lean | process | 2 lines (planning ref, §3) | — | removed | ds.py |
| ViTRender.lean | process | 6 lines (⭐, 4d piece 2 + date, 2 planning §, "Phase 2", ch10/ch7–9/ch8) | — | removed | ds.py |
| MlirCodegen.lean | process | 8 lines (planning/archive ×6, "bite 4"/"bites 4+6", "brick #1"/"brick #3", "T1a refutation", "EffNetV2 eval-name bug", "e12") | — | removed; public docstrings (19 defs) read | ds.py + awk over public defs |
| LeanMlir/VerifiedTrain.lean:9 | stale | "emitted offline by `tests/Test*` from the proof stack" | no tests/ file writes verified_mlir/; the 12 writers are Proofs/Codegen/*.lean | "emitted offline by the renderers in LeanMlir/Proofs/Codegen/ and regenerated by scripts/regen_verified_mlir.sh"; "used to copy / is now" cut | `grep -rln 'writeFile "verified_mlir' tests/ LeanMlir/Proofs/Codegen/` (12, all Proofs/Codegen); `ls scripts/regen_verified_mlir.sh` |
| LeanMlir/VerifiedTrain.lean:40 `VerifiedNet.mlirDir` | process | "⚠⚠ … They used to be written into verified_mlir/ and 74 of them had been checked in … 30 of them" | field default "verified_mlir"; sweep specs set ".lake/build" | present-tense: committed corpus pinned by regen check; `mlpG`/`cnnG`/`cifar8BnG` write build products to .lake/build; history + "30" count cut | `grep -n 'mlirDir  := ".lake/build"' VerifiedNetsCore.lean` (3 sites) |
| LeanMlir/VerifiedTrain.lean:58 `specs` | stale | "`initKind`: 0 = He(fan-in)" | `mkParam` `_` branch: rank-4 `2/(oc·kH·kW)` (He fan-out), rank-2 `2/(in+out)` (Glorot); fan-in only under `heFanIn` | "0 = random weight (conv: He fan-out; dense: Glorot; see `mkParam`)" | read `mkParam` body |
| LeanMlir/VerifiedTrain.lean:71 `lossSlot` | process | "`VerifiedNet.train` used to append the slot unconditionally …" | Bool field; `lossSlot := true` only on mlpVerified/mlpG/cnnVerified/cnnG | "true for the `mlp` and `cnn` renders … false for nets whose train step returns parameters only"; G4 sentence kept | `grep -rn "lossSlot := true" LeanMlir/` (4 sites, all mlp/cnn specs) |
| LeanMlir/VerifiedTrain.lean:88 `dropKeeps` | stale | "every net today except EfficientNet's `*sd` variants" | set on R50, B0, ConvNeXt, ViT specs; marker is `drop` (`VerifiedVariant.sdOn := v.contains "drop"`) | "Empty = no drop sites. Consumed only by `*drop*` variants (`VerifiedVariant.sdOn`)"; ⚠ markers → plain | `grep -n "dropKeeps := " VerifiedNetsCore.lean` (13 sites); `grep -n "def sdOn"` |
| LeanMlir/VerifiedTrain.lean:103 `dropoutKeep` | process | "▶ CLASSIFIER DROPOUT (`recipe_gaps.md` gap C) … ⚠⚠ THE WIDTH IS HERE …" | Option (Float × Nat) | markers, planning ref and caps removed; content kept | — |
| LeanMlir/VerifiedTrain.lean:120 `shimScript` | process | "⚠⚠ THIS FIELD EXISTS BECAUSE ITS DEFAULT USED TO BE R34's …" | String field, no default fallback (spawnShim throws on empty) | states each shim carries its own reference's augmentation and that empty refuses; history cut | read `spawnShim` guard |
| LeanMlir/VerifiedTrain.lean:146 `valEveryEpochs` | process | "Measured 2026-09-21 on ConvNeXt-T … ~2 h over 300 epochs" | — | measurement sentence cut | — |
| LeanMlir/VerifiedTrain.lean:157 `vitInit` | process | "the phase-2 reference run used (blueprint §9.6)" | — | "`deit-init` recipe of the JAX reference" | — |
| LeanMlir/VerifiedTrain.lean:163 `cnxInit` | process | "(2026-09-18) … ⛔ Why it exists. The 2026-09-17 … pair run was killed at epoch 67 …" | Bool; mkParam `cnxInit` → σ² 0.0004 on weights | present-tense invariant ("must match the JAX reference's setting … or a pair run compares two inits"); run history moved to a `--` comment above the field | read mkParam; `jax/MainConvNeXtImagenet.lean` link kept |
| LeanMlir/VerifiedTrain.lean:190 `bnMomentum` | process | "a phase-2 ↔ phase-4 gap" | — | "a reference ↔ verified gap" | — |
| LeanMlir/VerifiedTrain.lean:202 `wilson95` | process | "⭐ Added 2026-08-30 because the eval line printed …" | Wilson interval function | purpose stated (±1.11 pt at n=3925, p≈0.85 kept); dated anecdote cut | — |
| LeanMlir/VerifiedTrain.lean:235 `bnEmaWeight` | process | "⭐ ONE definition …; ⚠ historic 0.01 … across the knob's introduction … pre-knob behaviour" | code unchanged | plain sentences, history wording cut | — |
| LeanMlir/VerifiedTrain.lean:251 variant-predicates section doc | process | "⚠⚠ THEY LIVE HERE BECAUSE THE TEST USED TO PIN COPIES … `next_session_…` §5" | — | "defined once, here, so the test, `trainAdamSched` and `scoreCheckpoint` read the same predicate" | — |
| LeanMlir/VerifiedTrain.lean:273–300 `rmsOn`/`sdOn`/`cdOn`/`accOn`/`lambOn`/`bceOn` | process | "⚠ SUBSTRING … (`planning/archive/ema.md`'s defect)" etc. | predicates | planning refs and markers removed, reasons kept | — |
| LeanMlir/VerifiedTrain.lean:~310 `nRegions`/`nScalars`/`emaRegion` | process | "⭐⭐ THIS USED TO BE `if emaOn || accOn then 4 else 3` … §4a"; "not the literal 3 any more" | `3 + acc + ema` | states the 5-region layout lets acc+EMA coexist and why G precedes E | read defs |
| LeanMlir/VerifiedTrain.lean:~370 `mkSession` | process | "(`planning/archive/xla_pjrt_ladder.md`) … All 58 call sites used to supply one … §1a ties" | — | history + planning ref cut; "§1a ties" → "train-step ties" | — |
| LeanMlir/VerifiedTrain.lean:~400 `mkSessionDp` | process | "where this eval lived until 2026-09-18 (3 of 4 GPUs idle …)" | — | cut | — |
| LeanMlir/VerifiedTrain.lean:~420 `mkParam` | process + stale | "⚠ Kind 3 is new on 2026-09-13 … ⚠ Both weight cases CHANGED 2026-08-04 … `emitLayerScaleInit`" | body: kinds 1/2/3, cnxInit/vitInit/heFanIn/fan-out/Glorot | present-tense spec of each rule vs jax/Jax/Codegen.lean (`emitConvBnInit`, `emitDenseInit`); `emitLayerScaleInit` (does not exist) → "the reference's ConvNeXt layer-scale init, 1e-6" | `grep -n "emitLayerScaleInit\|emitConvBnInit\|emitDenseInit" jax/Jax/Codegen.lean` (only the latter two exist) |
| LeanMlir/VerifiedTrain.lean:~505 shim section, `readInto`, `readExact`, `shimMixDefault`, `ShimProc` | process | "(handoff §2k)"; runs/ link + 79–136 MB/step; "This used to grow by `ByteArray.append`"; "§0.4 finding 3"; "`spawnShim` used to return the handle alone … `planning/…` §1" | — | mechanism kept in present tense; run link moved to a `--` comment above `readInto`; planning refs cut | — |
| LeanMlir/VerifiedTrain.lean:~770 `readShimBatchPartial` | process + stale | "⚠⚠ Why this exists (2026-08-14) …"; "It reads LABELS FIRST … infers rows from the label read" | body reads a 4-byte row count first, then exactly `rows` labels and images | describes the v3 framing (row count read, not inferred); dated history cut | read body (`readUpTo h 4`, then `readExact` ×2) |
| LeanMlir/VerifiedTrain.lean:~815 `spawnShimSharded` | process | "measured 2026-08-01 … §2a's double-writer disease" | — | measurements/§ cut; transform-not-duplicated and round-robin caveats kept | — |
| LeanMlir/VerifiedTrain.lean:~860–925 `EvalRows`, `spawnValStream`, `reapValStream`, `evalScore` | process | "(planning/streaming_val.md §3.2)", "today's slicing", "C4 bug class", "the `<defunct>` lesson", "⭐ ONE copy", "⚠⚠ THE RAGGED TAIL" | — | planning refs/markers cut; content kept | `ls scripts/gates/streamed_val_gate.sh scripts/gates/sharded_eval_gate.sh` |
| LeanMlir/VerifiedTrain.lean:~1040 `loadData` | process | "⚠ It is INERT … **7.4 GB**" | — | plain sentence | — |
| LeanMlir/VerifiedTrain.lean:~1130 `ckptPathFor` | process | "(`planning/archive/xla_pjrt_ladder.md` §3) … sweeps `chapter_makeover.md` §3c mandates … epoch 40" | — | planning refs cut, hazard kept | — |
| LeanMlir/VerifiedTrain.lean:~1150 `printBlurb` | process | "Every net's `blurb` used to hard-code its transport … Three of the seven print sites …" | — | "The blurb carries a placeholder … every print site goes through here" | — |
| LeanMlir/VerifiedTrain.lean:~1160 `VerifiedNet.train` | stale | "He/spec init" | calls `mkParam` | "spec init (`mkParam`)" | read body |
| LeanMlir/VerifiedTrain.lean:1366 `trainAdamPacked` | stale | "packed render `@<slug>_adam_train_step` (`ViTRender.vitTrainStepModuleAdamPacked`, optimizer = `Proofs.adamWParam`)" | loads `<mlirDir>/<slug>_adam_train_step.mlir` via `compileVmfb` (iree-compile) + `LowererSession.create` | "committed … `<slug>_adam_train_step.mlir` (rendered by LeanMlir/Proofs/Codegen/; optimizer ops tied to `Proofs.adamWStep` by `Proofs.StableHLO.adamW_triple_faithful`). Compiles through `compileVmfb`, so it runs on IREE only." Report's "`<slug>_<variant>`" was wrong for this driver (fixed `_adam_`) | `grep -n '_train_step.mlir' VerifiedTrain.lean`; `ls verified_mlir/ | grep _adam_train_step`; `grep -rn "def adamWStep"` (Proofs namespace, AdamStep.lean:44) |
| LeanMlir/VerifiedTrain.lean:~1370 `fwdRenderedShape` | process | "⭐ BOTH numbers come from ONE parse … double-writer failure"; "▶ Why the width" | — | plain | — |
| LeanMlir/VerifiedTrain.lean:1464 `trainAdamSched` | stale + process | "(Phase 2) … Drives `ViTRender.vitTrainStepModuleAdamSched` … `recipe_gaps.md` Tier C … every existing call site is unchanged" | loads `<mlirDir>/<slug>_<variant>_train_step.mlir` via `mkSession` | "Drives the committed `<slug>_<variant>_train_step.mlir`, rendered by LeanMlir/Proofs/Codegen/; optimizer ops tied per op (for example adamW/lamb/mom triple/pair theorems)"; Phase/Tier cut | `grep -rhoE "theorem [a-zA-Z_]*faithful"` in Proofs/Codegen (sgd/mom/adamW/lamb/rmsProp/clip exist; "for example" because RMSProp/clip ties are separate) |
| LeanMlir/VerifiedTrain.lean:2762 `scoreCheckpoint` | stale + process | "drain the val split \| `loadData` (all 50,000 as of `ccca380`)"; "available today on ConvNeXt and ViT"; "(`planning/…` §2)"; "since 2026-09-12" | body: `loadData … evalOnly` for held splits, `spawnValStream` for `.imagenet`, `evalScore` for the pass, BN nets via `.bn` companion | row → "`spawnValStream` (ImageNet) / `loadData` (held splits)"; batching/forward rows → `evalScore`; "available today" sentence dropped; ⭐/▶/dates cut | `sed` of body: `spawnValStream`, `loadData … (evalOnly := true)`, `evalScore` calls |
| LeanMlir/VerifiedTrain.lean:~2900 `trainLinear` | stale | "distinct from the packed-params, He-init `train` … shares `compileVmfb`" | body uses `mkSession`; `train` uses `mkParam` | "`mkParam`-init `train` … shares `mkSession` / `loadData`" | read body |
| LeanMlir/VerifiedTrain.lean:3081, 3151 E4M3 trainers | stale | "Run (GPU): `IREE_BACKEND=rocm .lake/build/bin/…`"; "§3b render-tie model"; "Scope (honest) … (the next, codegen-level step)" | both use `mkSession` (XLA-capable) | "Run: `lake exe mnist-{linear,mlp}-e4m3-verified data`"; "render-tie model of Proofs/Float/E4M3Fold.lean"; "Scope:" plain | `grep -n "mnist-linear-e4m3-verified\|mnist-mlp-e4m3-verified" lakefile.lean` |
| LeanMlir/VerifiedTrain.lean (whole file) | process | ⭐/⛔/⚠/▶ markers, dates, run IDs, commit hash, planning § refs, "used to" histories in docstrings | — | 91 removed docstring lines carried such markers (diff: 261 lines out, 198 in); 0 remain in docstrings (ds.py clean); 2 run references moved to `--` comments | `git diff -U0 … \| grep "^-" \| grep -cE "⭐\|⛔\|⚠\|▶\|20..-..\|planning/\|§\|[Pp]hase\|used to\|recipe_gaps\|ccca380\|runs/20"` = 91 |
| Types.lean:1 (module) | missing | "ML specification types: Layer, NetSpec, TrainConfig, DatasetKind." | file holds Activation, Normalization, Padding, PadStyle, Layer, NetSpec, LossKind, SegLoss, OptimizerKind, TrainConfig, DatasetKind, iree-compile arg builders | module doc naming the types, their consumers (Spec, MlirCodegen, JAX emitter, Train) and that this is not the verified path | `grep -nE "^(inductive\|structure\|def)" LeanMlir/Types.lean` |
| Types.lean:3 `Activation`, `Padding` | missing | (none) | 6 / 2 constructors | one-line docstrings; Padding: JAX emitter reads it | `grep -n "\.valid" jax/Jax/Codegen.lean` (2243, 2257) |
| Types.lean:25 `PadStyle` | process | "⚠ This exists for the same reason… (planning/archive/mnv4_verified.md §3c/§4b) … fixed 2026-08-04" | enum used by `NetSpec.convPadStyle`; JAX emitter passes no pad arg under `.symmetric` | plain paragraph: set per net, structurally invisible, JAX `.symmetric` passes nothing | jax/Jax/Codegen.lean:2236–2257 read |
| Types.lean:53 `Layer` (51 constructors) | missing | constructors documented only by `--` comments; 15 had none | `Layer.paramSlots` (Spec.lean:69–153) / `nParamsUntrained` (Spec:157) / MlirCodegen & JAX cases | `/-- -/` docstring on every constructor + type docstring (trained vs shape-only) | per ctor `grep -c "\.<ctor>\b"` in MlirCodegen/Spec/jax Codegen; paramSlots bodies read; 22 ctors with no MlirCodegen case, of which `separableConv`,`fireModule` lowered by JAX only |
| Types.lean `Layer.bilinearUpsample` | stale | "Currently shape-only — no codegen yet, so any NetSpec referencing it cannot compile to MLIR" | MlirCodegen lowers it (`emitBilinearUpsample`, cases at 2258/2663/2968/5639, VJP at 1828) | "Lowered by `MlirCodegen` (forward and VJP)" | `grep -n bilinearUpsample LeanMlir/MlirCodegen.lean` |
| Types.lean `Layer.unetUp` | stale | "transposed-conv upsample + concat + 2× (conv3x3+BN+ReLU)" | MlirCodegen: `emitBilinearUpsample … 2`, concat, convBn (ic+oc)→oc, oc→oc; paramSlots has no transposed-conv weights | "×2 bilinear upsample, concatenation with the skip, two 3×3 conv-BN-ReLU (`ic + oc → oc → oc`)" | MlirCodegen.lean `| .unetUp ic oc =>` (≈2425) read; Spec.lean:145 |
| Types.lean `Layer.convBn` | wording (new) | (none) | MlirCodegen `emitConvBn` relu := true, ignores `pad`; JAX reads `convBnAct` and `pad` | "BatchNorm and an activation: `NetSpec.convBnAct` in the JAX emitter, ReLU in `MlirCodegen`" | `grep -c convBnAct` jax=4, MlirCodegen=0; MlirCodegen:2263 `_` pad |
| Types.lean `Layer.layerNorm`, `convNextStem` | wording (new) | (comments) | MlirCodegen: `\| .layerNorm _ \| .convNextStem .. => pure () -- no lowering here` (2594, 5406, 7980) | "Lowered by the JAX emitter; `MlirCodegen` has no lowering for it" | grep above |
| Types.lean `Layer` comments | process | "▶ Added 2026-08-30 … (§7.1)", "See planning/archive/flash_attention.md", "(planning/archive/tinygpt_demo_v2.md Part II…)", "(planning/archive/yolo_fpn.md)", "Currently shape-only… See planning/archive/unet_demo.md", "DDPM v2 Workstream A" | — | dropped; content kept | — |
| Types.lean `NetSpec` | missing | (none) | structure: name, layers, imageH/W, detStride, buildTag, convBnAct, convPadStyle | docstring naming role and consumers | `grep -n "def NetSpec.train\|^def train" LeanMlir/Train.lean` (NetSpec namespace) |
| Types.lean `NetSpec.buildTag` | process | "which is why `brats-predict` grew…", "reproduces the old paths exactly, so no existing trainer moves" | `buildPrefix` = sanitizedName ++ tag (Train.lean:30) | history dropped; "Empty leaves `buildPrefix` as `name` alone" | Train.lean:30–32 |
| Types.lean `NetSpec.convBnAct` | process | "⚠ This exists because the JAX emitter hardcoded… (planning … §3f/§3h)… 458 construction sites" | JAX-only field (MlirCodegen 0 uses) | present tense: JAX emitter, MNv2 ReLU6 / B0 swish, small-activation invisibility warning kept as plain sentence | grep counts above |
| Types.lean `NetSpec.convPadStyle` | process | "the one site the 2026-08-04 helper-default fix could not reach", "⚠ Only bites" | — | present tense, stem only | — |
| Types.lean `LossKind` + ctors | process | 5 planning refs (yolo_final R1, unet_demo Phase 0, brats_demo ×2, rsb_a2) | — | refs dropped; `bce`: "JAX-only: `compileVmfbs` rejects it on the MLIR path" | Train.lean:214–217 throws |
| Types.lean `TrainConfig` | missing | (none) | structure | docstring naming groups and readers | — |
| Types.lean:782 `TrainConfig.dropPath` | stale | "currently wired for ConvNeXt blocks" | jax/Jax/Codegen.lean drops at convNextStage (2284), bottleneckBlock (2347), mbConv (2378), uib (2421), transformerEncoder (2484) | "drops at ConvNeXt, bottleneck, MBConv, UIB and transformer-encoder blocks" | `grep -n "cfg.dropPath > 0"` + enclosing `\| .ctor` via awk |
| Types.lean:894 `TrainConfig.bf16` | stale | "Measured ~2.7-3.6× … on gfx1100 … See reference_bf16_gfx1100_conv_vs_gemm" | JAX-only (jax Codegen:3523); reference is not a repo file | measurement + dangling ref dropped; "on the JAX path… convs follow `bf16Conv`" | `grep -rn "cfg.bf16" jax/Jax/Codegen.lean LeanMlir/MlirCodegen.lean` |
| Types.lean `TrainConfig.bf16Conv` | stress | "bf16 conv is ~1.6× FASTER" | — | plain: "a backend where bf16 convolution is slower (MIOpen)… CUDA/cuDNN can opt in" | — |
| Types.lean:909 `TrainConfig.runningBN` | stale | "Currently wired for the convBn + invertedResidual path… extend … See … Gap A" | jax Codegen branches on runningBN at convBn (2262), residual (2325), bottleneck (2340), mbConv (2372), invertedResidual (2392+), uib (2424), fusedMbConv (2439) | "Wired for convBn, residual, bottleneck, inverted-residual, MBConv, UIB and fused-MBConv blocks" | `grep -n runningBN jax/Jax/Codegen.lean` + awk enclosing ctor |
| Types.lean `TrainConfig.bnMomentum` | process | "audited 2026-08-30", "(2026-09-25)", "0.99 today", bold, ⚠⚠, ⚠ | — | dates/markers dropped; table kept | — |
| Types.lean `TrainConfig` other fields | process | "brick #1/#2", "yolo_drone WS-D", "yolo_fpn bite 7 / T1b / T1c", "Tier-2 lever", "C6, planning/imagenet_parity.md", "gap A/B/C/D", "Phase 4 of", "brats_demo Workstream A", "vit_imagenet item 0" ×3, ⚠/⛔ in cnxInit, bnEps | — | refs/markers dropped; warnings kept as plain sentences; evalEveryNEpochs history ("historical hardcoded cadence", "7-hour run") trimmed to the present reason | — |
| Types.lean `DatasetKind` | missing | (none) | 8 ctors | docstring; brats planning ref dropped | — |
| Types.lean `compileCheckB` | process | "Lifted here from thirteen gates… no caller overrides it any more" | — | "`stderrTake` bounds how much stderr is echoed" | — |
| Types.lean | process | — | — | 41 flagged lines in docstrings → 0 (3 residual hits are false positives: "legacy" as a semantic word, a float count) | ds.py before/after |
| Train.lean:8 module | process | "Each Main*Train.lean used to inline ~250 lines…" | `NetSpec.train` → compileVmfbs → runTraining | present-tense description of the loop | Train.lean read |
| Train.lean:63 `graphArtifact` | process | "(planning/archive/detector_pjrt_port.md) ⚠ This said … until 2026-08-25 …" | returns `.mlir` on xla, `.vmfb` otherwise via `LowererSession.backendName` | "active at run time (`LowererSession.backendName`). No executable is linked against a backend…" | Train.lean:83–86; lakefile.lean:376–411 (`lowererLink`) |
| Train.lean:110 `compileVmfbs` | stale | "compile each to a `.vmfb` … Returns the path to the train-step vmfb" | `runIreeCached` returns `(false,true)` on xla; returns `graphArtifact pfx "train_step"` | "On IREE, also compile… on XLA there is no compile step. Returns the train-step artifact `graphArtifact` resolves (`.mlir` on XLA, `.vmfb` on IREE)" | Train.lean:95, last line of compileVmfbs |
| Train.lean:480 `runTraining` | stale | "Adam + cosine-LR + running-BN-stats training loop" | `optName := if cfg.useAdam then "Adam" else "SGD+momentum"` (≈676) | "Adam or SGD+momentum (`TrainConfig.useAdam`), cosine LR, …" | `grep -n useAdam LeanMlir/Train.lean` |
| Train.lean `train`, `valAugmentBatch`, `segRegions`, `detectionIO` | process | "every Main*Train.lean now calls", "so existing trainers don't have to change", "The comment on … warned; the TTA path did it anyway", 2 planning refs | — | trimmed to present tense | — |
| Train.lean | process | — | — | 7 flagged lines → 0 | ds.py |
| IreeRuntime.lean:2 module | stale | "Also holds the verified nets' parameter-layout tables (`*Layout`), which the trainers and `VerifiedSpec` read" | tables are in ParamLayouts, imported here | "Imports `ParamLayouts` … so importers of this module see them too" | `head -1 LeanMlir/IreeRuntime.lean` |
| IreeRuntime.lean:8 | stale | "Opaque handle to an IREE runtime session" | session is lowerer-agnostic | "a lowerer session (PJRT/XLA or IREE): a loaded module on a device" | module doc |
| IreeRuntime.lean `create`, `createDp` | stale | "Use `VerifiedNet.mkSession`" / "`VerifiedNet.mkSessionDp`" | top-level `mkSession` (VerifiedTrain.lean:381), `mkSessionDp` (:401), outside `VerifiedNet` namespace (ends before) | "`mkSession` (`VerifiedTrain`)" / "`mkSessionDp`" | awk namespace scan of VerifiedTrain.lean up to 409 |
| IreeRuntime.lean `mlpTrainStepVDP` | stale | "all-reduces every gradient … (`ViTRender.emitAdamVDP`)" | committed DP renders use the AST node | "`Proofs.StableHLO.SHlo.allReduceMeanF` in the committed data-parallel renders" | `grep -n "\| allReduceMeanF" Proofs/Codegen/StableHLO.lean` (1234; namespaces 64–65) |
| IreeRuntime.lean | process | "§2d.3" ×4, "§2d.3a: 4 GPUs currently buy 1.46×", "§4's rule", "handoff", xla_pjrt_ladder / yolo_demo_v2 refs, ⚠ ×3, "REFUSES" | — | dropped/plain; yolov1 loss now points to `LossKind.yolov1Masked` | 10 flagged lines → 0 |
| F32Array.lean:152 `dropoutMask` | process | "⛔⛔ WHAT CHANGED, AND WHAT THE OLD NOTE GOT WRONG … measured 2026-08-30 table … ⭐ lesson", "(`recipe_gaps.md` gap C)", "(`:1037`)", "`dropoutKeep` is set on the two EfficientNet specs and nowhere else" | `dropoutKeep` is set at VerifiedNetsCore 815, 916, 979, 1662 (1662 = `mnv4ImagenetVerified`) — the "nowhere else" claim was stale | present-tense invariant (per-element draws, disjoint seed, 1/keep, host draw, loop is `dropoutFill` because n = 327,680 at 256×1280); measurement table moved to a `--` comment at the top of the body | `grep -n dropoutKeep LeanMlir/*.lean`; `grep -rn 999983 jax/` (generated files) |
| F32Array.lean:338 `loadDetBin` | process | "the Phase 3b format … (This docstring said 6076 …)" | 7200 = `30*7*7*4 + 7*7*4 + 4 + 56*20` (Train.lean:437) | record layout + "matching the detection dataset's `labelBytesPerRecord`" | Train.lean:437 |
| F32Array.lean `readLabel`, `argmaxN`, `detSplitBatch`, `shuffle` | process | "⚠ Replaces …", "Measured off the val wire 2026-08-05 …", "This used to hardcode …", "It used to be hardcoded to 4" | — | present-tense reason kept (why all 4 bytes / why `n` / why the real grid / why not a literal); 7200 & 25428 re-derived | 224/7×7: 5880+196+4+1120=7200; 448/14×14: 23520+784+4+1120=25428 |
| F32Array.lean | process | brick #2/#3, Phase 3/3b ×4, 4 planning refs, "LEGACY", ⚠ ×6, ⛔⛔, ⭐, ▶, "load-bearing" | — | removed | 27 flagged lines → 0 (one ⚠/▶ left in a `--` comment in `dropoutMask`'s body) |
| SpecHelpers.lean module | process + stale | "These were copy-pasted into every Main*Train.lean… should be a pure function of NetSpec — no IO" | file has IO defs (`heInitParams`, `applyHeadPriorBias`, `applyDetPriorBias`, pretrained patches) | describes the helpers as functions of the spec (and the buffer); no "no IO" claim | `grep -n "^def" LeanMlir/SpecHelpers.lean` |
| SpecHelpers.lean prior-bias docstrings | process | "brats_demo Workstream A", "yolo_fpn.md Tier 2", "both Tier-1 levers … T1a/T1b write-ups", "e12 run", bold | — | dropped / "a 12-epoch run" | 6 flagged → 0 (2 false positives: "Lin et al. §3.3" citation, "FFI uses") |
| Spec.lean:659 `NetSpec.summarize` | process + stale + banned phrase | "all 41 were the same ten lines …", "⚠ … **load-bearing**", "pin all 189 variants" | tests/bestiary_params.yml has 201 rows | parameters that vary named; "parsed, not decoration"; "pin every variant" (count dropped) | `grep -cE '^  ".*": [0-9]+' tests/bestiary_params.yml` = 201 |
| Spec.lean:2 module | missing (thin) | "param counting, feature queries, arch display, validation" | — | names `Layer.paramSlots`, `NetSpec.totalParams` | — |
| Ddpm.lean | process | planning link §4, ⭐, ⚠ ×2, bold | — | removed | 5 flagged → 0 (1 false positive "(minibatch OT)") |
| E4M3Quant.lean | process | "§3b render-tie", "of §3b" | E4M3Fold.lean is the render tie | "the fp8 render tie" | 2 → 0 |
| Cam.lean, Blackjack.lean, Pong.lean | process | planning refs in module docs | — | removed | 1 + 1 + 1 → 0 |
| SyncBnCheck.lean | process/stress | "`R×b` IS `1×Rb`", "⚠ Only the DP step", "AND" | — | "`R×b` against `1×Rb`"; marker dropped | 1 → 0 |
| MnistData, LEBytes, FloatFmt, ReferenceNets | process | — | — | no hits, no change | ds.py |
| VerifiedNetsCore.lean:1 (module) | stale | "Specs with no proof importing them yet (e.g. `resnet34Verified`) stay in their own `Main*Verified.lean`" | `resnet34Verified` is defined here and used by SpecVJP; no `VerifiedNetSpec` def exists outside this file | "Every verified spec lives here, including the ImageNet and sweep specs that no proof names." | `grep -rlE "^def .*: *VerifiedNetSpec"` → only VerifiedNetsCore.lean; `grep resnet34Verified LeanMlir/Proofs/SpecVJP.lean` |
| VerifiedNetsCore.lean:14 `RmsSchedule` | process | "RenderCifar8Sgd02 / EfficientNet-16× failure … (handoff §2a-quater, §2a-quinquies)", "four entry points", "⚠" | structure with 5 fields | purpose kept (lr is a runtime arg; one definition for Imagenette+ImageNet entry points of both nets); history and § refs removed; "four" dropped | `grep -rn "RmsSchedule" apps/` (MainEfficientNetVerifiedAdam, MainMobilenetV2VerifiedAdam, MainMobileNetV2Imagenet, EfficientNetRender comment) |
| VerifiedNetsCore.lean:36 `decayEpochs` | wording | "⚠ Not 1 on both nets." | mnv2 default 1.0, enet 2.4 | "(1 for MobileNetV2, 2.4 for EfficientNet-B0)" | `mnv2RmsSchedule`, `enetRmsSchedule` defs |
| VerifiedNetsCore.lean:47,55 | process | "(2026-09-25)" ×2 | — | dates removed | — |
| VerifiedNetsCore.lean:78 `mlpVerified` | stale | "(`mlpVerifiedHasVJP` / `_at`)" | `mlpVerifiedHasVJP` is `HasVJP.canonical` (no content); folded VJP `mlpVerifiedHasVJPAt` takes `h0 h1` (both ReLU pre-activations ≠ 0) | "its folded VJP is `mlpVerifiedHasVJPAt` … at an input where both ReLU pre-activations are nonzero" | SpecVJP.lean:97,105 read |
| VerifiedNetsCore.lean:96 `mlpG` | overclaim | "(VJP: `mlpHasVJP`, which is polymorphic …) so any (d₁,d₂) is covered by that one theorem"; renders "verified_mlir/mlp_…" | `mlpHasVJP` is canonical; the folded one is `Proofs.mlpHasVJPAt` {d₀ d₁ d₂ d₃}; `mlirDir := ".lake/build"` | cites `Proofs.mlpHasVJPAt`; path `.lake/build/mlp_{d₁}x{d₂}_…` (`mlirDir`, build product) | MLP.lean:307,322 (namespace Proofs 14–485); spec's `mlirDir` field |
| VerifiedNetsCore.lean:134 `cnnVerified` | stale | "(`cnnVerifiedHasVJPAt`, folded through conv/maxpool/dense)" | no `cnnVerifiedHasVJPAt`; `cnnVerifiedHasVJP` is canonical; fold is `Proofs.mnistCnnNoBnHasVJPAt` via `cnnVerified_denote_eq` | names `cnnVerified_denote_eq` → `Proofs.mnistCnnNoBnForward`, fold `Proofs.mnistCnnNoBnHasVJPAt` (ReLU/max-pool hyps), `cnnVerifiedHasVJP` canonical | `grep -rnE "def cnnVerifiedHasVJPAt"` → none; SpecVJP.lean:143–156; MnistCNN.lean:76,103 (namespace Proofs) |
| VerifiedNetsCore.lean:154 `cnnG` | stale/wording | "honest den-certified path"; "renders verified_mlir/cnn_{d}_…" | `mlirDir := ".lake/build"`; renderer `cnnTrainStepFaithfulV` takes one `d1` | "every width renders through that renderer"; path `.lake/build/cnn_{d}_…` | CnnRender.lean:35 signature; spec field |
| VerifiedNetsCore.lean:188 `cifarVerified` | wording | "VJP: `cifarCnnHasVJPAt` (Proofs/SpecVJP)" | defined in CifarCNN.lean (Proofs); SpecVJP has `cifarVerified_denote_eq` → `cifarCnnForward` | "`Proofs.cifarCnnHasVJPAt` (at a smooth point), tied … by `cifarVerified_denote_eq`" | CifarCNN.lean:77; SpecVJP.lean:258 |
| VerifiedNetsCore.lean:209 `cifar8Verified` | stale (+ slice-H request) | "(12 ReLU kinks + 4 maxpools), 3-axiom clean", "pedagogical" | `cifarCnn8HasVJPAt` hyps `hf1…hf8` (conv), `hf9`, `hfa` (dense) = 10 kinks, `hp1…hp4` pools; no concrete 8-conv instance claimed | "at a point off the ten ReLU kinks (eight conv, two dense) and satisfying the four max-pool conditions"; axiom claim dropped (not re-run) | CifarCNN.lean:435–470 read; new text makes no instance claim |
| VerifiedNetsCore.lean:243 `cifar8BnVerified` | stale | "(12 ReLU kinks + 4 maxpools + 0<εᵢ ×8), 3-axiom clean" | `cifarCnnBn8HasVJPAt`: `hε₁…hε₈`, `h1…h8`, `h9`,`ha`, `h_mp1…h_mp4` | "under `0 < εᵢ` ×8, the ten ReLU kinks (eight post-BN, two dense) and the four max-pool conditions" | CifarCNN.lean:806–830 |
| VerifiedNetsCore.lean:274 `cifar8BnG` | stale | "renders verified_mlir/cifar8_bn_{d}_…" | `mlirDir := ".lake/build"`; MainCifar8BnGrid docs say .lake/build | path `.lake/build/…` (`mlirDir`, build product) | apps/cifar/MainCifar8BnGrid.lean:1–20 |
| VerifiedNetsCore.lean:328 `cifar8wbVerified` | process | "§4.3 'Lever 3: precision'", "⭐" | book ch 4 `\subsection*{Lever 3: the arithmetic}` | "the net chapter 4's 'Lever 3: the arithmetic' trains"; ⭐ removed | `grep -n "Lever" blueprint/src/content.tex` |
| VerifiedNetsCore.lean:375 `cifar8wbBnVerified` | number | "the 27 bf16 ops are batched-only" | count not re-verified | "the bf16 ops exist only in the batched family" | 38 params re-counted (8×4+6) |
| VerifiedNetsCore.lean:389 `resnet34Verified` | stale ch + process | "ch6", "§2l step B", "rung E", "honest" | content.tex `\chapter{ResNet-34}` is 5th numbered | "Chapter 5"; "110 param tensors (no conv biases: every conv is BN-followed)"; "forward-graph tie"; `Proofs.` prefixes | `grep -n '\\chapter' content.tex`; 110 re-counted by python from ResNet34Layout |
| VerifiedNetsCore.lean:426 `resnet34ImagenetVerified` | stale (claim ceiling) + process | "proof-carrying tier stops at Imagenette: no §1a tie … not 'proven'" (handoff §2k) | `r34_net_tiedB (N) {nCls}` binds class count; one replica (its docstring); sync twin `r34_net_syncTiedB`; SpecVJP at `R34BWeights 10` | "capstone `Proofs.ResNet34TieB.r34_net_tiedB` binds the class count … at one replica, f32 and batch BatchNorm; data-parallel step `Proofs.ResNet34SyncTieB.r34_net_syncTiedB`; SpecVJP ties at 10 classes" | ResNet34StepTieB.lean:429 signature, namespace line 69; SyncStepTieB:621, namespace 54 |
| VerifiedNetsCore.lean:475 `### ResNet-50` header | stale + process | "SKELETON, 2026-08-03 … no ResNet50RenderB.lean, no artifact … nothing renders, trains" | `Proofs/Nets/ResNet/ResNet50{BackB0,FullB,FullBVJP,FullBSeal,StepTieB,SyncB,SyncStepTieB,WholeBackCertifiedTieB}.lean` exist; ResNet50RenderB.lean writes all 39 `verified_mlir/resnet50*` paths; no SpecVJP R50 tie | "Rendered by Proofs/Codegen/ResNet50RenderB.lean … proof chain Proofs/Nets/ResNet/ResNet50*.lean, capstone `Proofs.ResNet50TieB.r50_net_tiedB`; SpecVJP has no ResNet-50 tie" | `ls`; `grep -n verified_mlir/resnet50 Proofs/Codegen/*.lean`; `grep resnet50VerifiedB_denote_eq` → none |
| VerifiedNetsCore.lean:484 `resnet50Verified` | stale + process; report partly wrong | "ch?", "✅ … as of 2026-08-04", "used to record", planning ref; report says `SHlo.maxPool3s2F` does not exist | `SHlo.maxPool3s2F` DOES exist (StableHLO.lean:419) — report wrong on that name | "Chapter 5"; pool fact kept; history paragraph reduced to the present-tense hazard (same output shape, only the emitted window differs) | `grep -n maxPool3s2F LeanMlir/Proofs/Codegen/StableHLO.lean`; content.tex ch 5 has "ResNet-50 on ImageNet" section |
| VerifiedNetsCore.lean:527 `resnet50ImagenetVerified` | stale | "verified driver has no gradient accumulation … not comparable until settled"; 76.66/40.8/78.1 numbers | `VerifiedVariant.accOn`/`accK` exist; `resnet50in_accdp8x64`, `resnet50in160_lambaccdp8x64bce` artifacts exist; reference rsb-faithful = 512×4 | "effective batch 2048 (512 × 4) … driver accumulates in `acc<k>x<B>` variants (`VerifiedVariant.accOn`, `VerifiedVariant.accK`), e.g. …"; unverified accuracy numbers dropped | `ls verified_mlir | grep resnet50in`; jax/MainResnet50Imagenet.lean:197–210 |
| VerifiedNetsCore.lean:602 `resnet50Imagenet160Verified` | stale + process | "DO NOT run with eval until `evalD0` lands"; "§2.1/§2.3/§4"; "376 ms/step … 52.3 h" | `loadData … (evalD0 : Nat := 0)`; driver reads eval width off the artifact; `resnet50in160_fwd_eval.mlir` `%x: tensor<256x150528xf32>` | warning dropped; "Eval runs at 224² through `resnet50in160_fwd_eval.mlir` … passes it to `loadData` as `evalD0`"; wall-clock history cut to one clause | `grep -o "%x: tensor<[^>]*>" verified_mlir/resnet50in160_fwd_eval.mlir`; VerifiedTrain `loadData` signature |
| VerifiedNetsCore.lean:661 `resnet50Imagenet2018Verified` | process | "⛔ WHY", "Caught 2026-08-24 … killed at epoch 13" | — | present-tense reason kept; shim_wiring_gate limitation kept as plain sentence | `ls scripts/shim_wiring_gate.py` |
| VerifiedNetsCore.lean:695 `resnet50ImagenetA1Verified` | stale + process | names `resnet50in_lambaccdp8x64wxclipbcewd001_train_step`; "⭐ Measured (2026-08-27)" | that artifact does not exist; wd001 renders are `resnet50in_emalambacc{,dp}4x128wxclipdropbcewd001{,bf16}`; `wdVariantMark` in `Proofs.StableHLO` | names the existing renders; `Proofs.StableHLO.wdVariantMark`; dates/markers removed | `ls verified_mlir | grep wd001`; ResNet34RenderB.lean:871 namespace 419 |
| VerifiedNetsCore.lean:743 `mobilenetv2Verified` | stale ch + process | "ch7", "rung E", "`mobilenetv2HasVJPAt` is the older stem+2-block fold" | ch 6; 35 relu6 sites = stem 1 + b1 1 + 16×2 + head 1 | "Chapter 6"; "forward-graph tie"; full-depth `Proofs.mobilenetv2ForwardBFullHasVJPAt` only | content.tex `\chapter{MobileNetV2}` 6th; count from layer list |
| VerifiedNetsCore.lean:790 `mobilenetv2ImagenetVerified` | stale (claim ceiling) + process | "(§2p)", "§2g's warning", "Since 4c leg 2 (2026-09-06)", "Claim ceiling (§5): proofs stop at Imagenette" | `mnv2_net_tiedB (N) {nCls}`, one replica; sync `mnv2_net_syncTiedB`; `mnv2FwdChainB` in `Proofs.StableHLO` | capstone binds class count, one replica, f32, batch BN; sync twin named; SpecVJP at 10; §/dates removed | MobileNetV2StepTieB.lean:669 (ns 73); MobileNetV2SyncStepTieB.lean:991 (ns 61); MobileNetV2RenderB.lean:605 (ns 56) |
| VerifiedNetsCore.lean:853 `efficientnetVerified` | stale ch + stale VJP | "ch8", "rung E", "honest pointwise VJP witness is the representative `Proofs.efficientnetHasVJP`" | full-depth `Proofs.efficientnetForwardBFullHasVJP (N) (w : B0Weights) (hεw : w.EpsPos)`, global | "Chapter 7"; "full-depth VJP is `Proofs.efficientnetForwardBFullHasVJP` (global; only hypotheses the `0 < ε` positivities, `Proofs.B0Weights.EpsPos`)" | EfficientNetFullB0.lean:115, 371 (namespace Proofs, outside StableHLO 140–171/269–361) |
| VerifiedNetsCore.lean:927 `efficientnetImagenetVerified` | process + wrong | "(§2p)", "⚠ This is the first ImageNet net here with BatchNorm", "(§5 — …)" | R34/MNv2 ImageNet specs also have BN | "A BatchNorm net, which has two consequences the LayerNorm nets do not"; § refs removed. **Claim-ceiling sentence left verbatim: deferred §5 (B0 10-class)** | `bnChannels` on resnet34ImagenetVerified |
| VerifiedNetsCore.lean:1002 `convnextVerified` | stale (22→23) + process | "ch9", "22 LN positivities … there is no head LN", "⚠ Three things above were stale … until 2026-08-12", "restored 2026-08-30 … was 180/…", "rung E", "REAL", "stuck with" | `convNextForwardTChHasVJP` takes `hsε, h1, hd1, h2, hd2, h3, hd3, h4, hhε` = 1+18+3+1 = 23 | "Chapter 8"; "23 LN positivities (stem + 18 blocks + 3 downsamples + the head LN)"; history paragraph deleted; counts kept | ConvNeXtFullT.lean:294 signature; 182 / 27,827,818 / 28,589,128 re-counted by python from ConvNeXtLayout |
| VerifiedNetsCore.lean:1056 `convnextImagenetVerified` | stale (claim ceiling, cBS, EMA combo) + process | "(handoff §2p)", "Batch is 32 per device because `cBS` is still a private constant", "EMA … not combined with wx/clip/drop in any committed artifact", "used to end … WRONG by four of six as of 2026-08-12", "as of 2026-08-02", "chapter_makeover.md §4a-quater" | `cnx_net_tiedGB (N) {nC}`, one replica, drop-free chain; ConvNeXtRender cBS is a parameter; convnextin renders `%x: tensor<64x150528xf32>`; `convnextin_emadpwxclipdropbf16` exists | capstone scope stated; "64 examples per replica … global 256 at four replicas"; EMA+wx+clip+drop render named; history removed | ConvNeXtStepTieGB.lean:369 (ns 59); ConvNeXtRender.lean:40–50; `grep -o %x…` on 3 artifacts; `ls verified_mlir | grep ^convnextin`; jax/MainConvNeXtImagenet.lean batchSize 256 |
| VerifiedNetsCore.lean:1119 `convnextSImagenetVerified` | stale numbers + process | "342 parameter tensors, 50,222,152 scalars … the count jax/MainConvNeXtSImagenet.lean emits", "⭐", "▶", planning ref, "driver's LEAN_MLIR_DROP_RATE_U" | `#guard … size == 344`, sum `== 50223688`; env var read by apps/imagenette/MainConvNeXtSImagenet.lean, not the driver | "344 parameter tensors, 50,223,688 scalars (the `#guard`s below)"; unverifiable JAX-count clause dropped; env var attributed to the entry point | the file's own `#guard`s; `grep -rn DROP_RATE` |
| VerifiedNetsCore.lean:1201 `convnextBImagenetVerified` | stale numbers + process | "342 parameter tensors, 88,589,416 scalars", "⚠⚠", "⭐" | `#guard … size == 344`, sum `== 88591464` | "344 parameter tensors, 88,591,464 scalars"; `Proofs.StableHLO.CnxDims` | the file's own `#guard`s; ConvNeXtRender.lean:85 (ns 38) |
| VerifiedNetsCore.lean:1273 `vitVerified` | stale ch + process | "ch10", "retiring the old weight-shared scalar-LN caveats", "REAL", "rung E" | ch 9; `vitVerifiedHasVJP (w) (hε : 0 < w.ε)` | "Chapter 9"; `Proofs.vitForwardKV`, `Proofs.StableHLO.vitFwdGraphKMHV`; "forward-graph tie" | content.tex chapters; ViTDepthK.lean:151 (Proofs), :330 (Proofs.StableHLO 276–376); SpecVJP.lean:532 |
| VerifiedNetsCore.lean:1323 `vitImagenetVerified` | stale (claim ceiling) + process | "handoff §2p", "Claim ceiling … never 'proven' (§5)", "since 2026-08-02", "imagenet_parity.md §2.1/§2.2" | `vit_net_tiedGB (N) {nC}`, one replica, drop-free, literal ViT-Tiny dims; shipping `emadp128x4wxclipdropbf16` is bf16+drop | capstone scope stated (one replica, f32, no drop-path); shipping variant marked outside it; planning pointer kept without § | ViTStepTieGB.lean:374 (ns 59); `ls verified_mlir | grep ^vitin_` |
| VerifiedNetsCore.lean:1383 `vitSImagenetVerified` | process + unverified | "first net … added by WIDENING", "⭐⭐", "~154 dimension literals" in the per-example renderer | `vitForwardKVHasVJP` binds heads/d_head/mlpDim/k | proof-coverage sentence kept (as "same definition"); unverified literal count and renderer claim dropped | ViTDepthK.lean:174 signature |
| VerifiedNetsCore.lean:1441 `vitBImagenetVerified` | process | "THE PER-DEVICE BATCH 32 PIN IS LIFTED (2026-08-27)", "32×4 PAIR IS DELETED", run dir | `runViTBImagenet` refuses fp32 without `LEAN_MLIR_MEM_FRACTION`; bf16 twin exempt; artifacts `vitbin_adamdp128x4wxclipdrop{,bf16}` | present-tense requirement kept; history and run IDs removed; `Proofs.StableHLO.VitDims` | apps/imagenette/MainViTBImagenet.lean:115–137; ViTRender.lean:40 (ns 22) |
| VerifiedNetsCore.lean:1505 MNv4 header | process | planning link | — | removed | — |
| VerifiedNetsCore.lean:1507 `mobilenetv4Verified` | process | "sixth Imagenette net", "Converted Conv-S → Conv-M on 2026-08-14 … ch6 §6.5 … 84.58% … SUPERSEDED", "✅ timm's net since 2026-09-24 (planning/…)", "run on that date" | 21 UIB, 233 tensors, 8,447,322 (#guards) | history removed; gate results kept in present tense; counts stated from `#guard`s | counted `.uib` lines (21); `ls scripts/parity/` |
| VerifiedNetsCore.lean:1606 `mnv4ImagenetVerified` | stale (overclaim group 4) | "⭐ This IS Conv-M as of 2026-08-14, so it is now comparable to the chapter's 75.51%"; "no data-parallel render"; `/home/skoonce/…` path | book: 75.48% measured on an earlier transcription (content.tex:7955); DP renders `mnv4in_adamdp64{,bf16}`, `mnv4in_emaaccdp8x128wxdowd005bf16`; `mnv4_net_tiedB {nCls}` one replica; sync `mnv4_net_syncTiedB` | "75.48% … measured on an earlier transcription … a target, not a comparison"; DP renders named; capstone scope stated; 9,715,512 kept | content.tex:7955–7962; `ls verified_mlir | grep ^mnv4in`; MobileNetV4StepTieB.lean:819 (ns 82); MobileNetV4SyncStepTieB.lean:1305 (ns 87); file's `#guard … == 9715512` |
| VerifiedNetsCore.lean (all) | process | ⭐/⛔/⚠/✅/▶ markers, dates, handoff/§/rung/tier/planning refs in docstrings | — | 102 removed docstring lines carried process markers; remaining hit = the deferred B0 ceiling sentence (:909) | `git diff -U0 \| grep '^-[^-]' \| grep -cE '⭐\|⛔\|⚠\|✅\|▶\|dates\|§\|planning/\|handoff\|rung E\|Tier\|used to\|until 20\|as of 20\|4c leg'` = 102; code and `--` comments byte-identical to HEAD (strip-and-compare script) |
| VerifiedSpec.lean:1,16 (module) | overclaim + process | "(Tier-2)"; "faithfulness is the audited `<net>HasVJP` theorem, a hand-unrolled foldl of vjpComp over these same layers … `netVjp` … remaining Tier-2/Tier-3 steps" | SpecVJP: `*_denote_eq` by `rfl` + `*_fwd_faithful` for nine specs; train-step faithfulness is `*StepTie*`; `netVjp` does not exist | "SpecVJP ties nine of the specs — [enumerated] — to their net functions (`<net>Verified*_denote_eq`, by `rfl` …) and to the rendered forward graph (`<net>Verified*_fwd_faithful`). The emitted train step's faithfulness is the per-net `*StepTie*` capstone …; the slug names the committed render, not derived from `layers`" | `grep -nE "^(theorem\|noncomputable def)" SpecVJP.lean` (denote_eq/fwd_faithful for linear, mlp, cnn, cifar, mnv2, r34, enet, convnext, vit); `grep -rn netVjp` → none |
| VerifiedSpec.lean:34 `VerifiedData.imagenet` | process | "(handoff §2k)", "wrong until 2026-08-02" | — | "The shim is per net: each is generated from the TrainConfig that net's reference trainer runs" | — |
| VerifiedSpec.lean:59,69,105,117,124,140,153,157,185 VLayer ctors | process | §2l/§2k/§2m refs, "+17,056 / +21,008 gap closed (§2m)", ⭐/⚠⚠, planning links, "swish until 2026-09-24", "It was 3×3 here until 2026-07-30" | — | invariants kept (bias-free rule, v1.5 stride, SE biases stay, k=0 dispatch, pre/post invisibility, ReLU fused stage); gap numbers replaced by "the derived count matches the reference's" (5,288,548 for B0 re-counted) | python recount of EfficientNetLayout at K=1000 = 5,288,548; tests/TestConvBiasZero.lean header read |
| VerifiedSpec.lean:217 `VLayer.toSpecs` | stale | "`initKind`: 0 = He(fan-in) … it was kind 1 until 2026-09-13" | `mkParam` kind 0: rank-4 `2/(oc·kH·kW)` (He fan-out), rank-2 `2/(in+out)` (Glorot), cnxInit/vitInit overrides, `heFanIn` gate-only | "0 = random weight (`mkParam`: conv He fan-out, dense Glorot, with the ConvNeXt and ViT overrides) … 3 = 1e-6"; date removed | VerifiedTrain.lean mkParam body read (~:438–495) |
| VerifiedSpec.lean:294 `dropKeeps` field | stale | "Empty on every net without a `*sd` render"; "this file sits DOWNSTREAM of VerifiedTrain"; §2k | marker is `drop` (`VerifiedVariant.sdOn := v.contains "drop"`); set on R50/B0/ConvNeXt/ViT specs; VerifiedSpec imports nothing | "Empty = no drop sites. Read only by `*drop*` variants (`VerifiedVariant.sdOn`)"; reason restated (spec modules do not import Proofs/Codegen); `Proofs.StableHLO.enetDropIdxs`/`enetDropTotal` | VerifiedTrain.lean:275; EfficientNetRender.lean:290,298 (ns 27) |
| VerifiedSpec.lean:307 `dropoutKeep` | process | "▶ CLASSIFIER DROPOUT (`recipe_gaps.md` gap C)" | — | "Classifier dropout — …" | — |
| VerifiedSpec.lean (all) | process | as above | — | 24 removed docstring lines carried process markers | same diff grep = 24; code/comments identical to HEAD |
| ParamLayouts.lean:70 `ResNet34Layout` | stale + process | "§2l step B"; "`@resnet34_train_step`"; "(tests/TestResnet34*.lean `allParams`)"; "0 = He(fan-in) (stem fan-in = 147)" | no `resnet34_train_step.mlir`; artifacts `resnet34_<variant>_train_step`; writer ResNet34RenderB.lean; mkParam kind 0 = He fan-out/Glorot | variant-named signature; renderer named (plain-text path); initKind corrected | `ls verified_mlir | grep ^resnet34_`; `grep -ln verified_mlir/resnet34_ Proofs/Codegen/*.lean` |
| ParamLayouts.lean:107 `MobileNetV2Layout` | process + stale initKind | measured-history paragraph (§2m, conv-bias-zero numbers), "since 2026-09-06 (4c leg 2 …)"; initKind He fan-in | 158 tensors; 3,504,872 at K=1000 | history cut; count kept; `Proofs.StableHLO.mnv2FwdChainB`; initKind corrected | python recount (158 / 3,504,872) |
| ParamLayouts.lean:156 `EfficientNetLayout` | stale | "CIFAR 3×32×32, E6 … stride-1 … CIFAR adaptation … 32→16→8→4→2 … (tests/TestEfficientNet*.lean)"; initKind | `xShape` is 3·224·224; stem stride 2 in spec; writer EfficientNetRender.lean | "Imagenette 3×224×224 … stride-2 … 224→112→56→28→14→7"; renderer named; initKind corrected | `xShape` def; `efficientnetVerified` layers; `grep -ln verified_mlir/efficientnet_` |
| ParamLayouts.lean:198 `ConvNeXtLayout` | stale | "LN (global per-example scalar γ/β, rank-0 `#[]`)", "downsample LN scalar {γ,β}", tests/ generator, initKind He fan-in | `blockSpec`/`downSpec` carry `(#[c],1),(#[c],2)`; stem LN `[96]`; writer ConvNeXtRender.lean; cnxInit σ=0.02 | channel LN `[c]` everywhere; stem LN mentioned; renderer named; initKind corrected | ConvNeXtLayout.specs code |
| ParamLayouts.lean:218 `ConvNeXtLayout.specs` | process | "⚠ §2m moved three things … ⭐ The head LN is back (2026-08-30, §7.1)" | head `(#[768],1),(#[768],2),(#[768,10],0),(#[10],2)` | present-tense head description + counts; the 1,536-shortfall hazard kept as a plain sentence | python recount 182 / 27,827,818 / 28,589,128 |
| ParamLayouts.lean:250 `ViTLayout` | stale | "beyond the scalar proof witness `vitFull`"; "(tests/TestViT{Train,Fwd}.lean, from the same `ViTRender.vitParam*` generator)"; initKind He fan-in | param list is `Proofs.StableHLO.vitParamSig` (Proofs/Codegen/ViTRender.lean:491); proof net `Proofs.vitForwardKV` has per-channel vector LN | "as in `Proofs.vitForwardKV`"; `Proofs.StableHLO.vitParamSig`; initKind corrected | `grep -n vitParamSig Proofs/Codegen/ViTRender.lean` (ns 22) |
| ParamLayouts.lean (all) | process | as above | — | 9 removed docstring lines carried process markers | same diff grep = 9; code/comments identical to HEAD |
| VerifiedNets.lean | — | module docstring | accurate | no change | read |
| VerifiedNetsCore.lean:909 `efficientnetImagenetVerified` "Claim ceiling (§5): proofs stop at Imagenette" | deferred §5 | B0 10-class ceiling | `efficientnet_net_tiedG` `t : Vec (N*(1*10))` | not reworded | per instructions |
