# Ledger — slice H (Nets/ResNet, Nets/Small)

`file:line | kind | old claim | what the statement says | new text | how checked`

Line numbers are post-edit positions of the changed docstring.

| site | kind | old claim | statement | new text | checked |
|---|---|---|---|---|---|
| Small/CnnFold.lean:6–56 (module) | overclaim | "each emitted SGD op denotes the certified (`fderiv`/VJP-derived) softmax-CE loss-descent step"; "Honest residual … is the remaining polish" | conv ops: `ConvWSgdTied/ConvBSgdTied` at `cnnChainCotW1/W2` (masked chain defs, CnnChainClose:44–63); only `cnn_W5_tied_totalloss` reaches `pdiv (crossEntropy ∘ …)` | "denotes θ − lr·(certified per-layer Jacobian · the cotangent the rendered backward chain feeds it), and W₅ is folded to the whole-loss gradient"; "What is proved here" list adds `cnn_W5_tied_totalloss`, `cnn_conv_tied_certified`; "Scope" bullet: no theorem states the chain cotangents equal the loss gradient; "M2" dropped | read CnnFold.lean:62–245, CnnChainClose.lean:31–174; `grep -rn "cnnChainCotW" LeanMlir` (defs + uses only in Small/); `grep -rn "theorem cot0_den\|theorem cot1_den" LeanMlir` (MlpFold:39,48); `grep -rn "theorem mlpCotOut[01]_denote"` (MlpTrainStep:98,106 — the old text said `IR`) |
| Small/CnnFold.lean:~147 (dense-tie banner) | stale/process | "(The CONV layers … bigger conv-side work; see §1a of the planning doc.)" | conv fold is the next section of the same file (`cnn_conv_tied_certified`) | "The conv layers `W₁`/`W₂` are stated at their chain cotangents in the next section." | read in place |
| Small/CnnFold.lean:~198 (conv-fold banner) + `cnn_conv_tied_certified` docstring | overclaim | "denote the certified loss-descent step"; "(Residual … is the polish.)" | 4 conjuncts `ConvWSgdTied`/`ConvBSgdTied` at `cnnChainCotW2 …g`, `cnnChainCotW1 W₂ hc1 cotW2`; no smoothness hyp.; no link to `pdiv (CE ∘ fwd)` | "denote θ − lr·(certified ∂convₖ/∂θ · c) with c the rendered backward-chain cotangent (relu masks, select-and-scatter pool-back, conv-back) driven by the emitted softmax-CE cotangent g. That c equals the loss gradient at the conv output is not stated."; "WHOLE cnn train step is den-composed" sentence kept (dense head deferred §5) | statement l.214–239; `grep -c "Conv[WB]SgdTied" CnnFold.lean` conjuncts = 4 |
| Small/CifarFold.lean:6–15 (module) | overclaim | "each emitted SGD op denotes the certified loss-descent step" | conv conjuncts at chain defs; only `cifar_W7_tied_totalloss` reaches `pdiv (CE ∘ fwd)` | "each emitted SGD op denotes θ − lr·(certified per-layer Jacobian · the cotangent the rendered chain feeds it); only the output weight W₇ is tied to the whole-loss gradient (`cifar_W7_tied_totalloss`)"; "M2" dropped | read CifarFold.lean:51–235 |
| Small/CifarFold.lean:29–33 (residual) | stale | "The conv cotangents here are free variables c … Pinning each c … is the remaining polish." | `cifar_conv_tied_certified` pins every conv cotangent (`cnnChainCotW2/W1`, `cifarChainCotW2`) | "## Scope — The conv cotangents are the rendered chain (…); that they equal the loss gradient at each conv output is not stated." | statement l.210–225 (`let cotW4 … cotW1`) |
| Small/CifarFold.lean:41 (empty section) | stale | "## Conv layers — generic `den = certified` (covers all four conv layers)" with no content | `convW_den`/`convB_den` are in Foundation/SgdNodes.lean:60,71 | header deleted | `grep -n "theorem convW_den" -r LeanMlir` |
| Small/CifarFold.lean:~58 banner | process | "## The §1a tie" | — | "## The tie" | — |
| Small/CifarFold.lean:~166 `cifar_conv_tied_certified` | overclaim | "denote the certified loss-descent step" | 8 conjuncts `ConvWSgdTied`/`ConvBSgdTied` at chain cotangents; no smoothness hyp. | "denote θ − lr·(certified ∂convₖ/∂θ · c). That each c equals the loss gradient at its conv output is not stated."; "WHOLE cifar train step" sentence kept (dense head deferred §5) | `grep -c` conjuncts: 8 (l.215–225) |
| Small/Cifar8StepTie.lean:4–6 (module) | wrong/process | "(Chapter 4 …) §1a TIE"; "cifar8 is cifar (ch5)" | CIFAR is ch 4 (CifarCNN.lean:4, CifarFold.lean:6) | "TIE"; "cifar (ch4)" | `grep -n Chapter Small/CifarCNN.lean` |
| Small/Cifar8StepTie.lean:18 | stale | "`denseW_den`/`denseB_den` (`MlpTrainStep.lean`, …)" | defined in Foundation/SgdNodes.lean:31,43 | "(Foundation/SgdNodes.lean, …)" | `grep -rn "theorem denseW_den" LeanMlir` |
| Small/Cifar8StepTie.lean:24 (residual) + `cifar8_convs_tied_certified` | overclaim | "denote the certified loss-descent step" | 16 `Conv[WB]SgdTied` conjuncts at `cnnChainCotW2/W1`, `CifarPoC.cifarChainCotW2`; no smoothness hyp. | "denote θ − lr·(certified ∂convₖ/∂θ · c) with c the rendered backward-chain cotangent driven by g; that c equals the loss gradient at the conv output is not stated"; Scope bullet added; "WHOLE cifar8 train step" sentence kept (dense head deferred §5) | `grep -c "Conv[WB]SgdTied" Cifar8StepTie.lean` = 16 |
| Small/Cifar8BnStepTie.lean:4–6 (module) | process | "§1a TIE"; "cifar8's §1a tie" | — | "TIE"; "cifar8's tie (`Cifar8PoC.cifar8_convs_tied_certified`)" | grep decl in Cifar8StepTie.lean |
| Small/Cifar8BnStepTie.lean:15–20 (residual) | stale | "BN `0 < ε` smoothness" listed as residual | `cifar8Bn_convbn_tied_certified` has no ε hyp.; `bnSgdPairTied_holds` (SgdNodes:122) unconditional | ε item dropped; Scope bullet: cotangents `cotCᵢ`/`dyBnᵢ` not stated equal to the loss gradient | read SgdNodes.lean:81–126 |
| Small/Cifar8BnStepTie.lean:~50 `cifar8Bn_convbn_tied_certified` | overclaim | "denote the certified loss-descent step … both are the genuine cifar8-bn backward chain" | 24 conjuncts (8 ConvW + 8 ConvB + 8 `BnSgdPairTied`) over 32 tensors at chain cotangents | "(…; 24 conjuncts, one `BnSgdPairTied` per γ/β pair) … denote θ − lr·(certified per-layer Jacobian · c) … the rendered cifar8-bn backward chain … That they equal the loss gradient at each layer output is not stated." | `grep -nE "^\s*(∧ )?(ConvWSgdTied\|ConvBSgdTied\|CifarBnPoC.BnSgdPairTied)" Cifar8BnStepTie.lean \| wc -l` = 24 |
| Small/Cifar8BnStepTie.lean:12–13 | deferred §5 | "All 38 params (8 conv W/b + 8 BN γ/β + 3 dense) fold with the generics" | dense-head conjuncts absent from the capstone | unchanged (dense-head row of §5 will add them) | 16+16+6 = 38 checked against binders l.55–64 |
| Small/MnistCNN.lean:16–20 (module) | overclaim | "`TrainedCnn.trainedCnnHasVJPAt` discharges every hypothesis at trained weights and a real MNIST test input" | TrainedCnnWitness.lean:1–27,611: `mnistCnnNoBnHasVJPAt` at `X : Vec (1*(2*3)*(2*3))` (6×6 pooled digit), W1 `Kernel4 2 1 3 3`, W2 `Kernel4 2 2 3 3`, dense 18→8→8→10, k/128 weights, pool-tie regulariser | "…discharges every hypothesis on a reduced instance of this forward — input a 24×24 MNIST crop 4×4-average-pooled to 6×6, conv 1→2→2 (3×3), dense 18→8→8→10, /128-rational trained weights (the max-pool no-tie hypothesis needed a pool-tie regulariser during training) — at one pooled MNIST test digit." | read TrainedCnnWitness.lean:1–60 (module + `T0`, `X`, `W1`, `W2` types) |
| Small/MnistCNN.lean:274–275 | skipped (not a docstring) | "Closes the gap that `mlpHasVJPAt` is never instantiated" | — | unchanged: `--` banner comment, outside §4 docstring scope | — |
| Small/MlpFold.lean:6–27 (module) | overclaim/stale | "proves each output's `den` equals the certified `fderiv`-derived loss-descent step, reusing `mlp_render_{W,b}*_certified`"; "the ReLU smooth-point hypotheses are inherited from the bridges" | only `mlp_W2_tied_totalloss` reaches `pdiv (CE ∘ fwd)`; other five are `Cifar8PoC.denseW_den/denseB_den` at `mlpCotOut{1,0}.denote g`; no theorem in MlpFold carries a ReLU hyp.; no `mlp_render_*_certified` exists | "proves that the output weight's den is W₂ − lr·∂(CE ∘ forward)/∂W₂ … and each of the other five is θ − lr·(certified per-layer Jacobian · the rendered chain cotangent) — `Cifar8PoC.denseW_den`/`denseB_den` at `mlpCotOut1`/`mlpCotOut0`, whose subgraphs `cot1_den`/`cot0_den` pin"; Scope: "that the chain cotangent … equals the loss gradient at the hidden pre-activation is not stated here (`IR.mlp_hidden_total_loss_grad`, `IR.mlp_input_total_loss_grad` leave that factor as a `pdiv`)" | read MlpFold.lean whole; `grep -rhoE "theorem mlp_render_[A-Za-z0-9_]*" LeanMlir` → none; `grep -n "Cifar8PoC.dense" MlpFold.lean` (six proofs) |
| Small/MlpFold.lean:~57 section banner | stale | "the `mlp_render_*_certified` bridges" | proofs use `Cifar8PoC.denseW_den`/`denseB_den` | "`Cifar8PoC.denseW_den` / `Cifar8PoC.denseB_den`" | as above |
| Small/MlpFold.lean:~163 `mlp_train_step_tied_certified` | overclaim (minor) | "all six emitted parameter ops denote the certified loss-descent step — …" | 6 conjuncts: W₂ at `pdiv (CE…)`, five at chain cotangents | "the output-weight op denotes W₂ − lr·∂CE/∂W₂ …, and the other five denote θ − lr·(… the backward-chain cotangent …); that this chain cotangent is the loss gradient at a hidden pre-activation is not stated." | statement l.165–216, `refine ⟨?_×6⟩` |
| Small/MlpCanonical.lean:71–73 `train_step_tied_certified` | overclaim | "every SGD op of the emitted graph denotes the certified loss-descent step of the REAL canonical forward" | = `MlpPoC.mlp_train_step_tied_certified` at (784,512,512,10) | "at the REAL canonical forward, the output-weight SGD op denotes W₂ − lr·∂CE/∂W₂, and each of the other five denotes θ − lr·(certified per-layer Jacobian · the rendered chain cotangent)" | def body l.74–75 |
| Small/MlpTrainStep.lean:4, 21–25 (module) | process/stale | "# M2 — …"; "the only missing piece … This is Crux A of `planning/archive/verified_train_step.md`"; "(already built; …)" | file holds `mlpCotOut0`, its bridges, and the total-loss folds | title without "M2"; "This file supplies `mlpCotOut0` and its weight/bias bridges … and the total-loss folds (`mlp_output_total_loss_grad`, and at smooth points `mlp_hidden_total_loss_grad`, `mlp_input_total_loss_grad`, `mlp_whole_net_weight_grads`)"; "(IR.lean; …)" | `grep -n "^theorem\|^def" MlpTrainStep.lean`; `grep -rn "def mlpCotOut1" LeanMlir` (IR.lean:727) |
| Small/MlpTrainStep.lean:~120 (`--` section comment) | stale | "that conditional fold is the remaining new proof" | proved directly below | "…: `mlp_hidden_total_loss_grad` / `mlp_input_total_loss_grad` below." | read in place |
| Small/MlpTrainStep.lean:~140 `mlp_hidden_total_loss_grad`, ~180 `mlp_input_total_loss_grad` | overclaim | "That inner factor is exactly the cotangent the backward chain delivers at layer 1 (…, cf. `mlpCotOut1_denote`)" / "(the deepest cotangent the backward chain delivers, cf. `mlpCotOut0_denote`)" | RHS inner factor is `pdiv (fun z => crossEntropy …(relu z)…) p k 0`; no lemma equates it to `mlpCotOut*.denote` | "…`∂L/∂p₁`, left as a `pdiv`. (Off the kinks that factor is the cotangent … cf. `mlpCotOut1_denote`; that identification is not stated here.)"; W₀ likewise | statements read in place; `grep -rn "mlpCotOut1_denote" LeanMlir` (no lemma with a `pdiv crossEntropy` side) |
| Small/CifarCNN.lean:~435 `cifarCnn8HasVJPAt` | wrong | "the twelve ReLU smoothness kinks … (`hf1 … hf12`, `hp1 … hp4`) — they discharge for a concrete instance the same way `Tiny.cifarTinyCnnHasVJPAt` …" | binders `hf1…hf8`, `hf9`, `hfa`, `hp1…hp4`; no 8-conv instance anywhere | "Conditional on the ten ReLU kinks (eight conv, `hf1 … hf8`; two dense, `hf9`, `hfa`) and the four MaxPools (`hp1 … hp4`) … No concrete instance of this 4-stage net is proved; `Tiny.cifarTinyCnnHasVJPAt` is a concrete instance of the 2-stage `cifarCnnHasVJPAt` …" | `awk 'NR>=441&&NR<=620' CifarCNN.lean \| grep -nE "\(hf\|\(hp"` → 10 hf + 4 hp; `grep -rn "cifarCnn8HasVJPAt\b" LeanMlir tests` → only CifarCNN + VerifiedNetsCore prose |
| Small/CifarCNN.lean:~761 BN8 banner (`--`) | wrong | "the twelve post-BN ReLU kinks" | `h1…h8` post-BN, `h9`, `ha` dense | "the eight post-BN ReLU kinks, the two dense ReLU kinks, and the four MaxPools" | `awk 'NR>=803&&NR<=925'` binder list: h1…h8 h_mp1…4 h9 ha |
| Small/CifarCNN.lean:~769, ~808, ~1036 `cifarCnnBn8Forward`, `cifarCnnBn8HasVJPAt`, `cifarCnnBn8HasVJPAt_correct` | missing | no docstrings | forward with `bnPerChannelTensor3` after each conv; VJP with `hε₁…hε₈`, `h1…h8`, `h9`, `ha`, `h_mp1…h_mp4`; `_correct` = `.correct` | three docstrings added (MaxPool conditions named by type `MaxPool2Smooth`, not reworded — D1) | read the def bodies and binder lists |
| Small/ChapterGraphTies.lean:~102 `cnnBackGraph_faithful` | missing/process | `--` comment "… (smooth point) — A2c." (doc-gen shows it undocumented) | `den (cnnBackGraph …) = (mnistCnnNoBnHasVJPAt … h1 h2 h_mp h3 h4).backward dy` | `/-- … -/` docstring: denotes the backward of `mnistCnnNoBnHasVJPAt` at a point with the four ReLU pre-activations nonzero (`h1`,`h2` conv; `h3`,`h4` dense) and `h_mp : MaxPool2Smooth …`; "A2c" dropped; the assembly narrative moved to a `--` comment inside the proof | binders l.118–130; `grep -rn "theorem mlpBackGraph_faithful" LeanMlir` (StableHLO.lean:3018) |
| Small/LinearFold.lean:6–12 (module headline) | overclaim/process | "Companion to `planning/archive/verified_faithful_sweep.md`"; "every value the emitted module produces is the certified … loss-descent SGD step" | `poc_train_step_tail_certified`: one example `x`; weight conjunct = `W − lr·pdiv (CE ∘ dense)`; bias conjunct = two-factor sum with `softmax − onehot` | planning pointer dropped; "for a single example `x`: the emitted weight output denotes W − lr·∂(crossEntropy ∘ dense)/∂W … and the bias output denotes b − lr·(certified ∂dense/∂b · (softmax − onehot)). The committed module batch-contracts over `B` examples, which this file does not state." | statement l.91–104 |
| Small/LinearFold.lean:~24–28 | overclaim/process | "each output's `den` is proven = the certified `fderiv`-derived step end-to-end … now share … the connection §1a of the planning doc calls for" | as above | weight = W − lr·∂CE/∂W, bias = certified bias Jacobian · (softmax − onehot); §1a/"now" dropped | as above |
| Small/LinearFold.lean:34–39 (residual) | stale | "`tailDenW`/`tailDenB` *model* … Adding these as `SHlo` nodes … is the last mechanical step." | `tailDenW`/`tailDenB` do not exist; tail is `SHlo.weightSgd`/`biasSgd` (file's own §"The tail fold") | "## Scope — Per-op `den` ⇄ MLIR text for `weightSgd`/`biasSgd`: that the printed `dot_general`/… compute their `den` is the same trusted op-level modelling … (`wGrad`, `wGrad_faithful`)." | `grep -rn "tailDenW" LeanMlir` → none; `grep -rn "theorem wGrad_faithful" LeanMlir` (StableHLO.lean:2814) |
| Small/LinearFold.lean:42 | process | "ℝ → Float32: deferred (a future pass)" | — | "not stated here" | — |
| Small/LinearFold.lean:~87 `poc_train_step_tail_certified` | overclaim/process | "(in-kernel, closed) … denote the certified `fderiv`-derived loss-descent SGD step. The tail's meaning is now a property …" | weight + bias conjuncts as above | "For one example `x`, … denote, respectively, W − lr·∂(crossEntropy ∘ dense)/∂W and b − lr·(certified ∂dense/∂b · (softmax − onehot))." | statement l.91–104 |
| Small/LinearTrainStep.lean (module, l.4, 12–16; docstrings l.48, 60, 114, 146, 158; `--` banner l.~127) | process | "M1 —", "milestone M1", "(M1 …)", "not yet", "now" | — | milestone labels removed; decl titles "Weight update." / "Bias update." / "Weight update, folded."; "for one example `x`" added to the module | `grep -n "M1\|M2" Small/*.lean` → none left |
| Small/CnnChainClose.lean:22–24 (module) | process | "(… the further "= ∂loss/∂θ" fold is the separate … step …) See `planning/archive/render_close_handoff.md` §1 "Optional polish"." | file pins `cnnChainCotW1/W2` only | "This pins the cotangent to the rendered chain; that it equals the loss gradient at the conv output (a `pdiv G = Back.denote` step, the conv analogue of the MLP's `mlp_hidden_total_loss_grad`) is not stated." | read whole file |
| ResNet/ResNetBackChains.lean:40–41 `r34InputGradB` | overclaim | "⭐ `N` is a variable: this chain carries no numerals." | binders `Kernel4 64 3 7 7`, `Mat 512 nCls`, widths 64/128/256/512 at 56/28/14/7 are literals; `N`, `nCls` free | "`N` is a variable: the batch size is never pinned (the 224-px resolution and the 64…512 widths are literals)." | signature l.42–54 |
| ResNet/ResNetBackChains.lean (module + `r50InputGradB`) | process | 5 marker/history items: "retired … on 2026-09-19", ⛔, ⚠×2, "Moved here … on 2026-09-08 (`planning/archive/float_second_pass.md`)", ⭐ + "the net the quoted 76.66% trains" | — | retirement/move history and accuracy dropped; ⛔ → "Note:", ⚠ → plain sentences; ⭐ dropped | `ls verified_mlir \| grep resnet50in` (`resnet50in_fwd`, `resnet50in160_fwd` names kept, exist as artifacts) |
| ResNet/ResNet34Fold.lean:5–52 (module) | stale/process | "makes every parameter update of the per-example, SGD-inline train step `den`-faithful — each emitted SGD op denotes the certified loss-descent step"; "⛔ RETIRED ARTIFACT (2026-09-06, 4c leg 1) … `planning/archive/renderer_convergence.md`"; "Two new core ops, ZERO new theorems for 142 of the 146 params"; "Honest residual … remaining polish" | file holds exactly `convStridedW_den`, `convStridedB_den` (∀ c); consumer `ConvNeXtStepTie.lean:195` | title "The strided-convolution SGD ops denote the certified step"; "any emitted strided-conv weight / bias SGD op … denotes θ − lr·(certified ∂flatConvStride2/∂θ · c) … per-example fused-SGD ties use them (`ConvNeXtStepTie`); batched peer `ResNet34PoCB` in `GradNodesB`"; op-text bullets kept; retirement history moved to a `--` comment; section banner "(the only new content)" dropped | `grep -n "^theorem" ResNet34Fold.lean` (2); `grep -rln "convStridedW_den" LeanMlir tests` (ConvNeXtStepTie, AuditAxioms); `grep -n "namespace Proofs.ResNet34PoCB" GradNodesB.lean` |
| ResNet/ResNet34FullB.lean:4–15 (module) | stale/process | "`ResNet34RenderPC.lean` (retired 2026-09-19) … per-example BatchNorm (… reduce `[2,3]`). That is the world of `resnet34_fwd.mlir` and the Imagenette SGD trainer"; "`formalization.yaml` 4e … `planning/archive/proofs_tier_to_paper_nets.md` section 4"; title "(T1-forward, T2)" | every `verified_mlir/resnet34*` forward/train artifact reduces `[0,2,3]`; `*_fwd_eval` have none | "…at batch BatchNorm: `bnBatchLA` …, reduce `[0,2,3]` … Every committed ResNet-34 forward and train-step artifact (`resnet34_fwd.mlir`, `resnet34_sgd_train_step.mlir`, `resnet34in_mom256_train_step.mlir`, the Adam/momentum steps) reduces `[0,2,3]`; the `*_fwd_eval` artifacts normalise with running statistics." | `for f in verified_mlir/resnet34*.mlir; do grep -c "dimensions = \[0, 2, 3\]"; grep -c "dimensions = \[2, 3\]"; done` → 72/252/468 batch reduces; the one `[2, 3]` in each is the GAP (resnet34_fwd.mlir:1225) |
| ResNet/ResNet34FullB.lean:19–21 | stale | "`ResNet34BackB0.lean` already carries the batched stages (`cbReluB`, `cbReluStridedB`, `projStridedB`, and `projB` from `EfficientNetRenderPC.lean`)" | `cbReluB`/`cbReluStridedB`/`projStridedB` in Foundation/BatchedStageLayers.lean; `projB` in Foundation/BatchedStages.lean | "The batched stages (`cbReluB`, `cbReluStridedB`, `projStridedB` in Foundation/BatchedStageLayers.lean, and `projB` in Foundation/BatchedStages.lean) … `ResNet34BackB0` assembles them into the two block kinds." | `grep -rlE "(def\|theorem\|abbrev) <name>( \|$)" LeanMlir` per name |
| ResNet/ResNet34FullB.lean:~49 | overclaim | "⚠ `N` stays a variable throughout. T1 and T2 carry no numerals" | `resnet34ForwardBFull` input `Vec (N * (3 * (2 * (2 * 56)) * …))`, `Kernel4 64 3 7 7`, `Mat 512 nCls` | "`N` stays a variable throughout: the batch size is never pinned here (the 224-px resolution and the 64…512 widths are literals)" | signature l.~170 |
| ResNet/ResNet34FullB.lean (module + 2 decls) | process | 9 items: "retired 2026-09-19", yaml 4e / planning pointer, ⭐⭐, ⚠×3, "until 2026-08-04 and nothing failed", ⭐ + "the lesson … paid for", "(T4/T5)", "(64); since 2026-09-21", `resnet34ForwardBFull` "retired per-example forward … the T6 tie", `resnet34FwdGraphBFull_faithful` "⭐ **T2 …**" | — | markers/dates/tier labels removed; pool hazard kept as "Same type, different function, so a swap type-checks."; per-replica "(64)" dropped (dp artifacts render 64 and 128 per replica: `resnet34_adamdp128`, `resnet34in_momdp64`); proof note "one `rw` per block …" moved to a `--` comment in the proof | `ls verified_mlir \| grep resnet34.*dp` |
| ResNet/ResNet34BackB0.lean:33–56 ("Structure") + 60–64 | stale | "`cbReluB` — … `_at` VJP + backward-graph faithfulness (`cbReluBackBatchedGraph` + `…_faithful`)" listed as this file's content; "`projBackBatchedGraph` … reused VERBATIM from EfficientNetBackB0"; "`cbReluLayer` / `projLayer` (from `MobileNetV2BackB0`)"; "`_faithful` lives in `EfficientNetBackB0`"; "the new **strided** … primitive" | stages/layers/`cbReluBackBatchedGraph(_faithful)` in Foundation/BatchedStageLayers.lean; `projB` in BatchedStages.lean; `projBackBatchedGraph(_faithful)`, `convStridedBackBatched_faithful` in BatchedBackLinks.lean; this file defines only `r34*` body/block graphs, layers, VJPs and the two capstones | Structure rewritten: stages "are defined in Foundation, not here" with each file named; bullets list only `r34BodyBackBatchedGraph`, `r34BasicBlockBackBatchedGraph_faithful`, `r34BasicBlockLayer`/`r34DownBlockLayer`; `convStridedBackBatched_faithful` "is in Foundation/BatchedBackLinks.lean"; "new" dropped | per-name `grep -rlE` (list in the FullB row); `grep -nE "^(noncomputable )?(def\|theorem)" ResNet34BackB0.lean` (10 decls, all `r34*`) |
| ResNet/ResNet34FullBVJP.lean:~45, ~485 (module, `resnet34ForwardBFullHasVJPAt`) | overclaim | "⭐ `N` is a variable throughout: this tier carries no numerals." ×2 | input `Vec (N * (3 * (2 * (2 * 56)) * …))`, `R34BWeights nCls` with literal widths | "`N` is a variable …: the batch size is never pinned (the 224-px resolution and the 64…512 widths are literals)." | signature of `resnet34ForwardBFullHasVJPAt` |
| ResNet/ResNet34FullBVJP.lean:~35 (module) | wrong | "running activations are named `r34Pre1 … r34Pre16`" | defs `r34Pre0` … `r34Pre16` (17) | "`r34Pre0 … r34Pre16`" | `grep -n "def r34Pre" ResNet34FullBVJP.lean` |
| ResNet/ResNet34FullBVJP.lean (module + 9 decls) | process | 17 items: "(T1, the VJP half)", "last piece of T1 in `formalization.yaml` 4e's port", "one new lemma", "⭐ The one thing that did not exist … written for this", "was already there", ⚠/⛔ markers, ⭐/⭐⭐ on 9 decl docstrings, "T1's VJP half for … 4e's port", "earns its existence" | — | markers and tier/yaml labels removed; `r34SmoothAtB_ok`'s proof note (one `comp_ok_of` per block; `rw` not `rfl` at literal widths, kernel deep recursion) moved to a `--` comment in the proof | procscan over the file → 0 hits |
| ResNet/ResNet34BackCertifiedTieB.lean:~392 `r34InputGradB_correct` | overclaim | "at every batch size, every input, every loss cotangent and every input pixel … the two together are the T6 statement" | hyps `hεs : 0 < εs`, `h_stem : R34StemSmoothAt …`, `h_pool : R34PoolSmoothAt …`, `hb1…hb16 : HasVJPDiffAt bₖ (opaqueA… x)` for arbitrary `b1…b16` | "at every batch size, loss cotangent and input pixel, at any input `x` where the stem relu is off its kink (`h_stem`) and no stem-pool window ties (`h_pool`), for any block maps `b1 … b16` carrying `HasVJPDiffAt` witnesses … (`hb1 … hb16`), with `0 < εs` … `resnet34ForwardBFull_eq_slots` below identifies those eighteen stages with the committed forward." | binder list l.~396–437 |
| ResNet/ResNet34BackCertifiedTieB.lean:57–60 (module) | stale | "r34 carries the heaviest hypothesis budget of the five nets: … That is 4.1d's bundle list, reused verbatim" | tie takes `h_stem`, `h_pool`, 16 opaque witnesses, not `R34SmoothAtB`; R50 has three clauses/block | "## Scope — A smooth-point statement: the tie assumes the stem relu clause (`h_stem`), the stem pool's per-example no-tie (`h_pool`) and `0 < εs`, and takes each of the sixteen blocks as an opaque `HasVJPDiffAt` witness … (a basic block's two relu clauses are the caller's, inside that witness)." | same binder list; `grep -n "R34SmoothAtB" ResNet34BackCertifiedTieB.lean` → none |
| ResNet/ResNet34BackCertifiedTieB.lean (module + 8 decls) | process | 18 items: title ⭐⭐, "retired 2026-09-19", "tier **T6** of `planning/archive/…` §4.2", "(T4/T5 … closed that thread)", "4.1d's apex", "(piece 2)", "4.1c built", "§4.2a left open", "(4.1b)", "## ⛔ Two walls, both measured", ⛔/⚠/⭐ on 8 decls, "§3.10's drift … lived a month", "(4.1d)" | — | removed; the two build walls and the `let`-closing cost moved to a `-- Build notes` comment after the module docstring; the "wrong pool" history and the proof route of `resnet34ForwardBFull_eq_slots` moved to a `--` comment in its proof; genuine padding note kept as a plain sentence | procscan → 2 residual hits, both false positives ("phase" = padding phase; "%h1W" MLIR names) |
| ResNet/ResNet34StepTieB.lean:~497 `r34_lossCot_is_smoothedCE_grad` | overclaim | "Together with the capstone this closes the top of the chain: every parameter node denotes the certified gradient at the cotangent of the loss the trainer actually minimises." | `r34_net_tiedB` ties each node to Σ_n (local op Jacobian)ᵀ·c at constructed cotangents, no smoothness hyp.; `r34IdCotIn_eq_vjp`/`r34DownCotIn_eq_vjp` identify block-input cotangents under `R34*SmoothAt` + `R34*Pos`; no parameter-level `pdiv (loss ∘ net)` statement | "Together with the capstone: every parameter gradient node is the certified per-op gradient at the cotangent the rendered chain delivers from the loss the trainer minimises; at a smooth point with positive epsilons (`R34IdSmoothAt`/`R34DownSmoothAt`, `R34IdPos`/`R34DownPos`, via `r34IdCotIn_eq_vjp`/`r34DownCotIn_eq_vjp`) the block-input cotangents are the certified block backwards." | signatures of `r34IdCotIn_eq_vjp` (hq, hs) and `r34DownCotIn_eq_vjp` |
| ResNet/ResNet34StepTieB.lean:~31–33 (module) | overclaim | "T3 carries no numerals, so nothing here pins the batch." | `r34_net_tiedB` input `Vec (N * (3 * (2 * (2 * 56)) * …))`, `R34BWeights nCls` | "The batch size is never pinned (the 224-px resolution and the 64…512 widths are literals)." | signature; `grep -o "tensor<[0-9]*x3x224x224"` in the three named artifacts → 32, 32, 64 |
| ResNet/ResNet34StepTieB.lean:~40 (census section) | stale | "`resnet34TrainStepFaithfulV` and `ResNet34RenderB` both default to `convBias := false`" | no `resnet34TrainStepFaithfulV` exists; `ResNet34RenderB.lean:136,245,1279` default `convBias := false` | "`ResNet34RenderB`'s writers default to `convBias := false`" | `grep -rn resnet34TrainStepFaithfulV LeanMlir` → only this docstring; census re-counted 3+13·6+3·9+2 = 110, 4+13·8+3·12+2 = 146, 146−110 = 36 conv-bias |
| ResNet/ResNet34StepTieB.lean (module + 10 decls) | process | 17 items: "T3 §1a" title, "(4.1e)", "deleted 2026-09-08" ×2, "4.1d's", ⭐/⭐⭐/⛔/⚠ markers, "since 4d piece 2 (2026-09-07)", "until then `emitGradAllReduce` …", "(2026-09-21)", "## ⛔ … the 146 the per-example tie named", "retired the same day by 4c leg 1", "the §1 fold", "an AST node since 4d piece 2" | — | removed; per-example history paragraph dropped | procscan → only MLIR `%` names left |
| ResNet/ResNet34SyncStepTieB.lean:~611 `r34_net_syncTiedB` | overclaim | "this and it together say the DP step's update is the certified gradient of the global-batch step" | conclusion: each all-reduced mean gradient node = `r34_net_tiedB`'s node at `N := R·N` at the chain cotangent from `G`; nodes are raw gradients (optimizer downstream) | "…whose nodes that capstone ties to the certified per-op gradient at the chain cotangent — so this and it together say each all-reduced gradient equals the single-device node at the global batch." | read `r34NetSyncTiedB` docstring + capstone |
| ResNet/ResNet34SyncStepTieB.lean:~618 | process/overclaim | "⛔ Before 2026-09-21 the DP render normalised per replica and this statement was false: `DataParallel.dpMeanGrad_ne_globalBatchGrad` is the witness" | witness is at `θ : Vec 1`, two replicas, a toy loss (DataParallel.lean:224–235) | "Note: with per-replica BatchNorm the mean of the replica gradients is not the global-batch gradient in general; `DataParallel.dpMeanGrad_ne_globalBatchGrad` is a two-replica, one-parameter counterexample." | read DataParallel.lean:220–240 |
| ResNet/ResNet34SyncStepTieB.lean (module + 3 decls) | process/overclaim | 8 items: "(T3)", "since 2026-09-21", "ties every parameter gradient node to the certified gradient" (×2), ⚠×4, "T3's node"/"T3's chain", ⭐⭐⭐/⭐⭐/⭐ | — | tier labels → `r34_net_tiedB`; "certified per-op gradient (at the cotangent that chain delivers)"; markers removed. P2/P4 kept: they are section names in `DataParallelSync` | `grep -n "P2\|P4" Foundation/DataParallelSync.lean` |
| ResNet/ResNet34SyncB.lean (module + 3 decls) | process | 8 items: "(T2)", "since 2026-09-21", "T2's data-parallel twin", ⭐, "exactly as T2 is", "the T3 half", ⚠×3, "T2's head graph", "T2's `resnet34FwdGraphBFull`", "⭐⭐ **T2 at synchronised BatchNorm" | — | tier labels replaced by the declarations they stand for (`resnet34FwdGraphBFull_faithful`, `ResNet34SyncStepTieB.lean`); markers and date removed | procscan → 0 |
| ResNet/ResNet34FullBSeal.lean (module + 13 decls) | process | 19 items: "`planning/full_width_seals.md` §4.1" / "§7" / "(§3.2 of the plan)" / "the plan's diagonal-on-channel-0", "until now … (`ResNet34Live*`, retired with this file)", ⚠/⚠⚠/⭐⭐ in the module, ⭐/⭐⭐ on 13 decl docstrings, ⚠ in `R` docstring | — | planning pointers and retirement history removed; the centre-tap/broadcast rationale kept as plain sentences ("not a diagonal on channel 0"); "the proxies' channel-difference carrier" → "a within-example channel-difference carrier"; "Level 2/3" kept (the seal files' shared vocabulary: MobileNetV2/V4, ResNet-50 seals use it) | procscan → 0; `grep -rln "[Ll]evel 2" LeanMlir` (4 seal files); √25088: 2·112·112 = 25088 checked |
| ResNet/ResNet50FullB.lean:~34 (conventions table) | wrong | "symmetric at all five sites (stem + three downsample 3×3 + three 1×1 skips)" | the list is 1 + 3 + 3 | "all seven sites" | count of the parenthesis; ResNet34FullB table says seven |
| ResNet/ResNet50FullB.lean:~45 | wrong (citation) | "a different net worth ~0.5 pt of top-1 (`VerifiedSpec.Layer.bottleneckStage`'s note)" | VerifiedSpec.lean:74–78: "compiles, trains, descends and is a different net", no top-1 figure | "~0.5 pt of top-1" dropped; citation kept | read VerifiedSpec.lean:70–82 |
| ResNet/ResNet50FullB.lean:~62 | overclaim | "⚠ `N` stays a variable throughout, as at r34: T1 carries no numerals." | widths literal (`Kernel4 64 3 7 7`, 256…2048), resolution binder `q` | "the batch size is never pinned (the widths are literals and the resolution is the binder `q`)" | `R50BWeights` fields; `resnet50ForwardBFull` signature |
| ResNet/ResNet50FullB.lean (module + 7 decls) | process | 27 items: "(T1-forward, T2)", "largest hole in the Proofs tier (`planning/archive/…` §2 …) §3.5(a) … §3.5(b)", "the one net where T1 matches … (§4) … 4b's or 4c's axes", ⭐/⭐⭐/⚠/⚠⚠/⛔/✅ markers, "76.66%" ×2, "since 2026-09-21", "T2 … the first graph-level tier this net has ever had" | — | removed; the per-block `rw` note moved into the proof as a `--` comment; census 161 and 162-argument signature kept (re-checked) | 3 + 12·9 + 4·12 + 2 = 161; `sed -n 2p verified_mlir/resnet50_fwd.mlir \| grep -o "%[A-Za-z0-9_]*: tensor" \| wc -l` = 162 |
| ResNet/ResNet50BackB0.lean:33–34 | wrong (citation) | "(`VerifiedSpec` records it costing ~0.5 pt of top-1)" | VerifiedSpec.lean:74–78 gives no top-1 figure | "(`VerifiedSpec.Layer.bottleneckStage`'s note)" | read VerifiedSpec.lean:70–82 |
| ResNet/ResNet50FullBVJP.lean (module + `resnet50ForwardBFullHasVJPAt`) | overclaim/wrong/process | "(T1, the VJP half)", "the last piece of T1 for the largest hole … (`planning/archive/…` §3.5(a))", "r34's T1 was blocked on `batchMapHasVJPAt` (4.1c)", "`r50Pre1 … r50Pre16`", "this tier carries no numerals … the net the quoted 76.66% trains", "T1's VJP half, and the first tier ResNet-50 has ever had", ⭐/⚠/⛔ | defs `r50Pre0 … r50Pre16`; binder `hq0 : 0 < q` | tier/plan labels, accuracy and history removed; "`r50Pre0 … r50Pre16`"; "`N` and `q` are both variables (the widths are literals)"; apex docstring gains "beside `0 < q`" | `grep -n "def r50Pre" ResNet50FullBVJP.lean` (0…16); signature l.419 |
| ResNet/ResNet50WholeBackCertifiedTieB.lean:~139 `r50InputGradB_correct` | overclaim | "at every batch size, every resolution, every input, every loss cotangent and every input pixel" | hyps `hq0 : 0 < q`, `hεs`, `h_stem`, `h_pool`, `hb1…hb16 : HasVJPDiffAt …` | "at every batch size, resolution `q > 0` (`hq0`), loss cotangent and input pixel, at any input `x` where the stem relu is off its kink (`h_stem`) and no stem-pool window ties (`h_pool`), for any block maps `b1 … b16` carrying `HasVJPDiffAt` witnesses … with `0 < εs` … identifies those eighteen stages with the committed forward" | `awk 'NR>=139&&NR<=212' … \| grep -oE "\((h…\|hq0\|hεs) :"` → hq0 hεs h_stem h_pool hb1…hb16 |
| ResNet/ResNet50WholeBackCertifiedTieB.lean:36–39 (module) | stale | "ResNet-50 carries the heaviest kink budget in the suite … Those are §3.5b's bundles, reused verbatim; this file adds no hypothesis of its own" | tie takes opaque witnesses, not `R50SmoothAtB` | "## Scope — … takes each of the sixteen bottlenecks as an opaque `HasVJPDiffAt` witness … (a bottleneck's three relu clauses … are the caller's, inside that witness)" | `grep -n R50SmoothAtB ResNet50WholeBackCertifiedTieB.lean` → none |
| ResNet/ResNet50WholeBackCertifiedTieB.lean (module + 3 decls) | process | 13 items: title ⭐⭐, "Tier **T6** of `planning/archive/…` §3.5(e) … T1, T2 and T3 landed 2026-09-06", "(§3.5b)" ×3, "§4.2d's file / measured reason", "76.66%", "(§4d)", "(§3.5c)", "(§3.10)", ⚠/⛔/⭐ markers | — | removed; the `backward_unique` timeout note moved to a `-- Build note` comment; shape-check proof route moved into its proof as a `--` comment | procscan → 0 |
| ResNet/ResNet50StepTieB.lean:30–32 (module) | wrong | "The stem tie is three nodes, not four … no conv-bias gradient op is ever emitted and there is nothing to keep 'to cover the flag', unlike r34's and MobileNetV2's ties." | `r50_net_tiedB`'s first conjunct is `r34StemTiedB …`, which contains `ConvStridedBTiedB` (ResNet34StepTieB.lean:353–356); the file's own capstone banner says 3 exercised slots of 4 | "The block ties carry no conv-bias conjunct, and the stem and head ties are ResNet-34's. … no conv-bias gradient op is emitted, and the bottleneck ties have no bias conjunct. The stem tie is ResNet-34's `ResNet34TieB.r34StemTiedB`, whose conv-bias conjunct is true at `bias = 0` but not exercised, so the stem contributes 3 exercised slots of 4." | read `r34StemTiedB` def; `sed -n 520,530p` capstone conjunct list |
| ResNet/ResNet50StepTieB.lean (module + capstone + BCE corollary) | process | "T3 §1a", "With T1 and T2 that is ResNet-50's T3, … the third net whose train-step tie is about the artifact its quoted accuracy comes from", "4.2a / 4.2c", "76.66%" ×3, "4b's …", "in T1", "## Honest residual", "T2's graph faithfulness", "since 4d piece 2 (2026-09-07), until then …", "(§4d)" ×2, "the §1 fold" | — | removed or replaced by the declaration named (`resnet50FwdGraphBFull_faithful`, `GradNodesB`'s folds, `ResNet34StepTieB`) | procscan: tier/date/§ hits → 0 (markers handled in the marker sweep) |
| ResNet/ResNet50SyncStepTieB.lean:~882 `r50_net_syncTiedB` | overclaim | "so this and it together say the DP step's update is the certified gradient of the mean loss over all `R·N` examples" | conclusion: each all-reduced mean gradient = `r50_net_tiedB`'s node at `N := R·N`, `g := G`; nodes are raw gradients at chain cotangents | "…whose nodes that capstone ties to the certified per-op gradient at the chain cotangent — so this and it together say each all-reduced gradient equals the single-device node at the global batch." | read capstone docstring + `r50NetSyncTiedB` |
| ResNet/ResNet50SyncStepTieB.lean (module + 3 decls) | process/overclaim | "(T3)", "ties every parameter gradient node to the certified gradient" ×2, "as it is in T3", "as T3's is", "T3's wiring", "T3's (…)", "T3's chain", "T3 leaves the loss open" | — | tier label → the declaration (`r50_net_tiedB`, `r50IdTiedB`); "certified per-op gradient (at the cotangent that chain delivers)" | `grep -n "def r50IdTiedB" ResNet50StepTieB.lean` |
| ResNet/ResNet50SyncB.lean (module + 3 decls) | process | "(T2)" / "T2's" ×7, "T3 half", ⭐/⚠ markers in module | — | T2 → `resnet50FwdGraphBFull_faithful` / "the single-device graph"; T3 half → `ResNet50SyncTieB.r50_net_syncTiedB`'s; markers removed | procscan |
| Small/CnnFold.lean conv-fold banner, CifarFold `cifar_conv_tied_certified`, Cifar8StepTie `cifar8_convs_tied_certified` | deferred §5 | "Together with the dense head (… `*_den` at the composed cotangent) the WHOLE … train step is den-composed forward→loss→backward" | capstones carry no hidden dense-layer conjuncts (W₃/W₄, W₅/W₆, W₉/Wa) | unchanged: the §5 dense-head row adds them; the cotangent-vs-∂L part of these docstrings was reworded (rows above) | — |
| Small/MlpCanonical.lean:25–28 | skipped | "MEASURED vacuous — L ≈ 39 ⇒ 0% certified" | a run measurement, not a Lean statement; not in report H | unchanged (candidate for the §4 sweep's "run results" rule) | — |
| Small/MlpFold.lean:8–12 | not changed (cross-slice lead) | "this file makes the *whole* module `pretty(provenGraph)`" | §2(e) of the plan says `MlpRender` has a hand-written `%loss` block | unchanged here; for slice E (Codegen/MlpRender) to confirm | — |

## Process-narrative count (§4)

Docstring lines in the 31 slice files matching markers / dates / § / `planning/` / tier labels (T1–T6, 4.1x, 4b–4e, piece/leg, M1/M2, Crux, A2c) / the 76.66% run: 344 at HEAD, 0 after (`scratchpad/strict.py`, docstring spans only). History that is a maintainer hazard moved to `--` comments: ResNet34Fold (retirement), ResNet34BackCertifiedTieB (two elaboration/kernel walls, `let`-closing cost, wrong-pool history), ResNet50WholeBackCertifiedTieB (`backward_unique` timeout), ResNet34FullBVJP `r34SmoothAtB_ok` (`rw` not `rfl`), ChapterGraphTies `cnnBackGraph_faithful` (assembly route), two `resnet*FwdGraphBFull_faithful` proofs.

## Checks run

* Code unchanged: comment/docstring-stripped HEAD vs working tree identical for all 31 files (`scratchpad/code.py`).
* `/-`/`-/` counts balanced per file; even backtick count per docstring span.
* Every new backticked name carrying a `docstring-checkrefs` marker resolves by last component against a declaration/constructor/field/module index of `LeanMlir/` + `tests/`.
| LeanMlir/Proofs/Nets/Small/MlpFold.lean:9 | overclaim | "makes the *whole* module pretty(provenGraph)" | MlpRender appends hand-written report-only `%loss` (`lossCode`, MlpRender.lean:71) | every line feeding a returned parameter is `pretty` of a proven node; report-only `%loss` named | lead, from slice E cross-check of MlpRender.lean:9,27,71,96 |
