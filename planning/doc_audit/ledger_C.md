# Ledger C — Proofs/Foundation, Proofs/Float, Proofs/SpecVJP.lean

`file:line | kind | old claim (short quote) | what the statement actually says | new text (short) | how nouns/numbers were checked`

Line numbers are pre-edit (the report's), approximately.

| Float/FloatSubnormalBridge.lean:3 | overclaim | "closed as a lemma", "proving activations stay normal", "binary32 RN instantiates it", "floor cannot move any closeness bound" | only instance `exactFaithful` (rnd=id); bnDenom/bnSqrt/istd lemmas bound the BN denominator, not activations; `subFloor_total_negligible` is `n ≤ 2⁶⁴ → n·2⁻¹⁵⁰ ≤ 2⁻⁸⁶`, used nowhere | module lists each decl with its actual statement; "no binary32 instance is built"; "that activations stay in the normal range is not proved"; "No theorem connects this to a closeness budget"; planning/archive cite dropped | `grep -rn FaithfulFloatModel\|subFloor_total_negligible\|bnDenom_normal` (only this file + AuditAxioms); read each signature |
| Float/FloatSubnormalBridge.lean:166 | overclaim | "The subnormal floor cannot move any bound … below every nonzero closeness budget" | `(n:ℝ) ≤ 2^64 → n * subFloorF32 ≤ (2^86)⁻¹` | "If each of n ≤ 2⁶⁴ … total at most 2⁻⁸⁶. An arithmetic fact; not connected here to any closeness budget." | read statement |
| Float/FloatBridge.lean:26 (module), :73 `dot`, :222 `dot_close`, :1268 `sum`, :1277 `sum_close` | overclaim | "valid for every association … survives a backend that reassociates (IREE…)", "association-independent, so the choice is immaterial" | `dot_close`/`sum_close` bound `M.dot`/`M.sum`, both defined as the left fold; no other order stated | "stated for that fold only … classical bound holds for every order, but only the left fold is formalized here; order-parametric form is `FloatModel.bnMean_close_of`"; IREE → XLA | read defs :74/:1270 and statements; `grep -n bnMean_close_of Float/BnFloatBridge.lean` (:202, takes arbitrary `fsum`) |
| Float/FloatBridge.lean:11 | stale | "`Binary32Instance` / `RndP` (binary32, bf16, E4M3 as instances)" | only `binary32` and `fp8E4M3` are `FloatModel` defs | "(`binary32` and `fp8E4M3` as named `FloatModel`s; `rndP` … behind the bf16 sharding lemmas)" | `grep -rn ": FloatModel :=\|: FloatModel where"` → exactModel, toFloatModel, binary32, fp8E4M3 |
| Float/FloatBridge.lean:936 `mlp_w2_step_float_close` | stale | "needs an `exp` accuracy axiom — future rung" | same file proves `softmax_ce_cot_close` (hyp `hfexp`, conclusion `≤ cotErr M.u eexp δ n`) | "`softmax_ce_cot_close` discharges it with `eg := cotErr u eexp δ n`" | read :1613 signature; `grep -n "def cotErr"` |
| Float/FloatBridge.lean:1326 `softmaxF`, :1644 `mnist_cot_budget` | overclaim | "accuracy constant is exactly what the repo's `vjp_oracle` harness validates empirically" | `eexp` is a hypothesis; tests/vjp_oracle/README.md: harness diffs step-2 loss between two pipelines | "its relative accuracy `eexp` is assumed …, not measured by any harness in the repo" | read tests/vjp_oracle/README.md "How it works" |
| Float/FloatBridge.lean:57,1711,1737,1827 | process | "§3c E4M3 MNIST demo", "see §2/§5 of the plan", "(planning §3c)", "separation of §1c", "(planning §3c capstone)" | — | planning § refs removed; "the relative model does not hold" outside normal range | 5 docstring lines; `--` section banners left as is |
| Float/FloatClose.lean:3 (module), :25 `FloatClose.comp`, :69 `floatClose_iterate` | overclaim | "a whole-net float certificate is `.comp` folded…", "the whole-net certificate backbone", "THE FINAL FOLD … r34's within-stage depth … The depth-generic whole-net certificate", "BN keeps them O(1), as the probe confirms" | per-op closeness form + composition + iterate; `FloatClose`/`floatClose_*` appear nowhere outside Proofs/Float except docstrings/comments (MaxPool3s2, BackwardMaps); no whole-net fold | "a whole-net bound would be `.comp` …; no whole-net `FloatClose` is assembled in the repo"; iterate: "(magnitude bound `A` taken as a hypothesis)" | `grep -rln "FloatClose\|floatClose_" --include=*.lean LeanMlir tests` |
| Float/FloatComposeBridge.lean:9 (module), :48 `floatClose_dense`, :163 `floatClose_bn`, :196 `floatClose_r34_stages` | overclaim + stale | "composition backbone (whole-net certificate)", "SE excite/reduce denses and the classifier head are this instance; the ViT MLP denses reuse it", "BN-before-swish steps in EfficientNet's MBConv … are this instance", "The full `r34_float_close` is these `.comp`…" | per-op instances; `floatClose_dense` 0 uses, `floatClose_bn` 0 non-doc uses, `floatClose_r34_stages` is 3/4/6/3 iterates of one block at one width; `r34_float_close` undeclared | retitled "per-op `FloatClose` instances for the conv-net op set"; module enumerates the 11 instances declared in the file; "nothing in the repo instantiates it"; r34_stages: "for one block `blk` at one width `m`. No whole-net ResNet-34 float bound exists in the repo." | `grep -n "^theorem floatClose_" FloatComposeBridge.lean`; use counts by grep excluding theorem/docstring lines; `grep -rn r34_float_close` (only the docstring) |
| Float/FloatComposeBridge.lean:71 `floatClose_maxPool3s2` | process | "⭐", "`planning/archive/rsb_a3_r50_verified.md` §4b" | — | marker + planning cite dropped | — |
| Float/ConvMixedComposeBridge.lean:3 (module title, ⭐⭐ para, ▶ para) | overclaim + process | "the whole-net bf16 bound", "One new instance buys the entire existing fold (… deleted … 2026-09-08)", "the §9.3 separation that makes this non-vacuous at R50's n = 4608" | per-op instance; same file says both bounds vacuous in absolute terms | title "The mixed-precision conv as a `FloatClose`"; "No whole-net bf16 (or f32) float bound is assembled in the repo"; "per-layer relative factor is 1 + O(u); composed depth-first, still vacuous in absolute terms (see `convMixedGain_factor`)" | read `convMixedGain_factor`, `convMixedBudget_affine` |
| Float/ConvMixedComposeBridge.lean:252 `floatClose_flatConvMixed` | overclaim | "⭐⭐⭐ THE INSTANCE … the ONLY thing the whole-net bf16 bound needed" | FloatClose instance, 0 uses | "compose with it unchanged; nothing in the repo instantiates it" | grep use count 0 |
| Float/ConvMixedComposeBridge.lean:297 `convMixedGain_factor` | proof-narrative + process | ~20 lines: numeric ratios, "f32 whole-net bridges the repo carried until 2026-09-08", "which this file will not pretend otherwise" | algebraic identity `convMixedGain = n·w·(1+ε)` | docstring states the factorization and that composed bounds are vacuous; numeric illustration moved to a `--` comment above the docstring (dates dropped) | read statement |
| Float/ConvMixedComposeBridge.lean (other) | process | ⭐ markers, "§9.2" | — | "§9.2" dropped; markers in sweep below | — |
| Float/DepthwiseFloatBridge.lean:3 (module) | overclaim | "(the one new conv lemma for EfficientNet) … the only forward op in the enet line whose float bound isn't already a wrap" | swish/sigmoid have no float bound anywhere in Proofs/Float | "The float forward-error bound for the depthwise convolution of the MBConv blocks (MobileNet, EfficientNet)" | `grep -rn "swish\|sigmoid" LeanMlir/Proofs/Float` (docstrings only); `grep -rln depthwiseConv2d LeanMlir/Proofs/Nets` (MobileNet, EfficientNet, ConvNeXt files) |
| Float/DepthwiseFloatBridge.lean:173 `floatClose_depthwise` | overclaim | "the MBConv depthwise stage folds through `.comp` like any other conv" | FloatClose instance, 0 uses | "it composes by `FloatClose.comp` like the other instances. Nothing in the repo instantiates it." | grep use count 0 |
| Foundation/MuonNewtonSchulz.lean:243 `nsStep_iterate_tendsto_polar` | overclaim + process | "This closes the loop: the thing the hardware computes is the thing the theory says is optimal", "P3", "L3–L6", "§0" | convergence under hypothesis `hconv`; instantiated for cubic and (15/8,−5/4,3/8) only; same file: `qScalar_not_le_one`, `qScalar_one_lt_one`, `qScalar_iterate_band_half` | "`hconv` is proved here for the classic cubic and Higham's quintic …, not for Muon's tuned quintic, which violates it (`qScalar_not_le_one`) and only bands"; proof sketch moved to `--` comment | read all 4 qScalar statements and the two instances |
| Foundation/MuonNewtonSchulz.lean:6 (module) | stale + process | "P1 — the spectral-step lemma", "What remains is that Muon's matmul iteration computes it", "P2/P3 build on these", planning/archive cites, "L1–L6" | file holds spectral step, engine, cubic/q5 convergence, matrix lift, tuned-quintic negatives + band | module enumerates the four groups with their decl names; tuned quintic: "does not satisfy that hypothesis … five-step band bound at σ = 1/2" | `grep -n "^theorem\|^def\|^noncomputable def"` in file |
| Foundation/MuonNewtonSchulz.lean (decl docstrings :47,:109,:132,:182,:214,:280,:290,:305,:327,:335) | process | "P1 —", "P2 (cubic) —", "(P4)", "P3 glue", "planning/archive/muon.md", "left open by hand", "provably computes Muon's update" (cubic) | — | labels → decl names; cubic: "computes `UVᵀ` in the limit"; band: "Stated at the single point σ = 1/2; the interval version … is not proved" | 11 docstring edits |
| Foundation/MuonGeometry.lean:199 `muon_polar_steepest`, :445 `muon_polar_orthogonal` | overclaim | "This is why Muon's `den = UVᵀ` Newton–Schulz update is steepest descent", "the implementation's Newton–Schulz iteration is the retraction that computes this projection" | statements are about `UVᵀ` given an SVD; NS convergence proved only for cubic/q5 | "A Newton–Schulz iteration with convergent coefficients computes `UVᵀ` in the limit (`MuonNewtonSchulz.nsStep_iterate_tendsto_polar`); Muon's tuned quintic approximates it to a band" | names grep'd in MuonNewtonSchulz.lean (namespace Proofs.MuonNewtonSchulz) |
| Foundation/MuonGeometry.lean:300 `muon_polar_achieves_nuclear_of_isUnit`, :120 `muon_polar_achieves_nuclear` | overclaim + stale | "steepest ascent … unconditionally, for any invertible G", "Von Neumann … is the next layer" | ∃ U V s, orthogonal, s ≥ 0, G = U diag s Vᵀ ∧ fInner G (UVᵀ) = Σ s — achievability only; `muon_polar_is_max` (:146) already proves the max | "For invertible G, the constructed SVD's polar factor attains the nuclear norm … achievability only; with `muon_polar_is_max` it makes UVᵀ the … steepest-ascent direction"; :120 "upper half … is `muon_polar_is_max`" | read both statements + :146 |
| Foundation/MuonGeometry.lean:8 (module), :237 `svd_of_isUnit`, :146 `muon_polar_is_max` | stale + process | "L4 (this layer)", "L5 (this layer)", "L6", "The only remaining layer is the singular G case", planning cite, "L1's Cauchy–Schwarz", "the remaining layer" | — | labels dropped; "Scope: the `_of_isUnit` forms need G invertible; the singular case … is not proved"; module adds that NS convergence is MuonNewtonSchulz's, cubic/q5 only | — |
| SpecVJP.lean:18 (module) | stale | "Rung 1: the linear classifier … the first rung … on the simplest net" | file covers linear, MLP, MNIST CNN, CIFAR CNN, MNv2, R34, B0, ConvNeXt-T, ViT-Tiny | retitled "each committed `VerifiedNetSpec` denotes its proven forward"; lists the 9 nets and the three pieces (denote_eq / HasVJP / fwd_faithful), naming which witnesses are real (linear `denseHasVJP`, ViT `vitForwardKVHasVJP`, MLP `mlpVerifiedHasVJPAt`) and which are `HasVJP.canonical` (MLP global, CNN, CIFAR, MNv2, R34, B0, ConvNeXt) | `grep -n "HasVJP.canonical\|:= *$" SpecVJP.lean` + read each witness def; `mlpHasVJP` = canonical (MLP.lean:311) |
| SpecVJP.lean:95,151,268,327,378,421,477 | overclaim | "The spec carries the math" on `HasVJP.canonical` witnesses | `HasVJP.canonical` exists for every `f`, `correct := rfl` (Tensor.lean:282) | "The canonical witness at … `HasVJP.canonical` adds no content; the [real] VJP is `<name>`" | Tensor.lean:282 read; each cited real witness grep'd: `mnistCnnNoBnHasVJPAt`, `cifarCnnHasVJPAt`, `mobilenetv2ForwardBFullHasVJPAt_correct`, `resnet34ForwardBFullHasVJPAt`, `efficientnetForwardBFullHasVJP`, `convNextForwardTChHasVJP_correct` |
| SpecVJP.lean:421 `efficientnetVerifiedHasVJP` | stale | "swish/SE are smooth but relu6 clamps; the per-block differentiability lemmas live in EfficientNetFullB0.lean" | `grep relu6 EfficientNetFullB0.lean` → none; `efficientnetForwardBFullHasVJP (N) (w) (hεw : w.EpsPos) : HasVJP (headFwdB … ∘ mbExpW … ∘ …)` | "B0 has no kinked activation (swish, SE sigmoid, batch BN); its whole-net VJP is `efficientnetForwardBFullHasVJP` (hypothesis `w.EpsPos`), stated on the ∘-chain form" | read EfficientNetFullB0.lean:371 |
| SpecVJP.lean:477 `convnextVerifiedHasVJP` | stale | "the 22 LN positivities only) on the ∘-chain form" | `convNextForwardTChHasVJP_correct` hyps: hsε, h1..h4 (∀ i per block, 3+3+9+3), hd1..hd3, hhε = 23 LN sites; conclusion stated on `pdiv (convNextForwardTCh w)`, not a ∘-chain | "hypotheses: the 23 LN ε positivities — stem, 18 blocks, 3 downsamples, head" | read ConvNeXtFullT.lean:409–417 |
| SpecVJP.lean:58 `linearVerifiedHasVJP`, :103 `mlpVerifiedHasVJPAt`, :536 `vitVerifiedHasVJP` | overclaim (tone) | "This is the whole-model verification…", "the step linear couldn't show", "The strongest rung C in this file" | real witnesses | plain descriptions of the witness and its hypotheses (`h0`, `h1`; `0 < ε`) | read signatures |
| SpecVJP.lean:122,241 section text | stale count | CIFAR "(six ReLU kinks + two maxpools)" | not re-counted | replaced with "(ReLU and max-pool smoothness hypotheses at the input)" (no count) | MNIST "five ReLU/maxpool smoothness hypotheses" at :~228 verified (h1,h2,h_mp,h3,h4) and kept |
| SpecVJP.lean (process) | process | "Rung 1/2/3/4/B/C/E" headers, "ch5 ladder", "6→17-block promotion once fired…", "retired on 2026-09-19" ×2, `denoteConvnextT` "RESTORED 2026-08-30 … §2m/§2n … §2k's own sin", "Retires the rep tie's … caveats", "Completes the B/C/E ladder", "ViTDepthK §3" | — | rung labels → net names / "Forward graph ↔ the committed spec"; ConvNeXt history moved to a `--` comment (dates/§ dropped, hazard kept); retired-twin sentences dropped | ~24 docstring lines |
| Foundation/IR.lean:827 `lossCot_bridge` | overclaim + stale | "the whole train step forward → loss → backward → grads is proof-backed end to end, and only the SGD arithmetic (and printer/IREE/float) stays trusted" | `emitLossCot c logits label j = pdiv (fun z _ => crossEntropy c z label) logits j 0` | "The emitted softmax−onehot vector `emitLossCot` equals ∂(crossEntropy)/∂logits (`softmaxCE_grad`), so the backward's cotangent leaf is proof-backed rather than supplied." | read statement |
| Foundation/IR.lean:268 `convBackDenote_eq_input_grad_formula` | proof-narrative + process | "previously asserted only at two toy 4×4 shapes …", "Proof: …", "The single load-bearing leaf for the §B certified-VJP tie: every conv-heavy net's backward routes through this" | `conv2d (reverseSwap W) 0 dy = conv2dInputGradFormula W dy` for odd kH,kW | statement + "`convFlatBack_eq_vjp_backward`, which ties `convFlatBack` to the certified conv input-VJP, routes through this"; proof sketch moved to `--` comment (and "input channel co" corrected to "output channel") | `grep -rn convBackDenote_eq_input_grad_formula` → ConvBackCertifiedTie.lean:34 (in `convFlatBack_eq_vjp_backward`), DepthwiseBackCertifiedTie docstrings |
| Foundation/IR.lean:288 `conv_back_bridge_1to2`, :730 `mlp_layer1_weight_grad_bridge` | process / category word | "no longer the brute-force `fin_cases` expansion", "(representative)" | — | "An instance of the general …"; "(layer 1)" | — |
| Foundation/IR.lean:7 (module), :75, :182 | process | planning/archive/typed_ir.md ×2, "(D1 shortcut)", "(D2)", "(D3)", "The spike", "Phase-3 mechanism", "will chain" | — | refs/labels dropped; "chain lemmas of this shape" | 5 docstring sites; `--` "Phase" banners left |
| Foundation/CertifiedChain.lean:6 (module) | overclaim | "Every net's whole backward is built this way now: … the MobileNet and EfficientNet chains" | CertLayer used in ResNet34/50FullBVJP (`r34NetLayer`, `r50NetLayer`), MobileNetV2BackB0 (`mnv2BodyLayer`, `mnv2DownBodyLayer`), MobileNetV4 files (trunk not one CertLayer, per MobileNetV4FullBVJP.lean:28), ConvNeXtBackB0 (`cnxBlockChLayer`), ViTBackNet; 0 uses under Nets/EfficientNet | enumerated users; "EfficientNet-B0's backward chain does not use `CertLayer`; it is composed directly (`EfficientNetChainClose`)" | `grep -rln CertLayer --include=*.lean LeanMlir`; `grep -rn CertLayer LeanMlir/Proofs/Nets/EfficientNet \| wc -l` = 0; each def name grep'd |
| Foundation/CertifiedChain.lean:40,:174 + markers | process | "(the §3 trap, one level up)", "(MobileNetV4's T2 took three minutes …)", ⭐/⚠ | — | § ref dropped; "(on MobileNetV4's chain: minutes without these, seconds with them)" | — |
| Float/BnFloatBridge.lean:69 `bnIstd_close_at` | overclaim | "The non-vacuous BN certificate." | conditional bound `|fistd fvarε − bnIstd| ≤ ers/√V + evar/(2V√V)` under hV0,hVfv,hVbn,hrs,hclose; only use is `bnIstd_close` (:116, V = ε) | "A conditional bound under `hVfv`, `hVbn`, `hrs`, `hclose`; apart from `bnIstd_close` (its V = ε case) nothing in the repo applies it." | `grep -rn bnIstd_close_at --include=*.lean LeanMlir` |
| Float/BnFloatBridge.lean:4 (module), :311 `bnForward_close` | stale | "The no-BN CIFAR bridge (`CifarFloatBridge.lean`) reuses…", "(mean/var rounding + the normalize-stage products remain the mechanical tail)", "the variance Higham reduction, the one remaining mechanical piece" | CifarFloatBridge.lean does not exist; file proves `bnMean_close`, `bnMean_close_of`, `bnVar_close`, `bnForward_close_of` | module: composes with `FloatModel.bnMean_close`, `FloatModel.bnVar_close`, `FloatModel.bnForward_close_of`, all in this file; `bnForward_close`: "`hvar` is taken as a hypothesis; `FloatModel.bnVar_close` supplies a variance budget in the rounded-sum form" | `ls` (no CifarFloatBridge); `grep -n "^theorem" BnFloatBridge.lean` |
| Foundation/DataParallel.lean:3 (module "What is NOT claimed") | stale | "Nothing here is about the emitted `all_reduce` … is §4d piece 2 … it waits on 4c's batched chains. Until it lands…"; "BatchNorm statistics are per replica. Nothing all-reduces μ/var" | `SHlo.allReduceMeanF` + `den_allReduceMeanF_eq_dpMean` exist (DataParallelNode.lean); this file proves `dpSyncGrad_eq_globalBatchGrad` (:278); DataParallelSync proves `den_syncStats_left/right` | "The `SHlo` collective and its `den` … are `DataParallelNode.lean`"; "Under per-replica BatchNorm (the non-sync renders) nothing all-reduces μ/var … The sync-BN renders all-reduce the statistics (`DataParallelSync.lean`), and `dpSyncGrad_eq_globalBatchGrad` applies"; added a "What is proved" bullet for `dpSyncGrad_eq_globalBatchGrad` | read :278 statement; `grep -n allReduceMeanF DataParallelNode.lean` |
| Foundation/DataParallel.lean (process) | process | "piece 2; until 2026-09-07 …", "(piece 1 of planning/archive/proofs_tier_to_paper_nets.md §4d)", "(moved 2026-09-08)", "the content of §4d piece 1", "the closing statement of §4d piece 1, and the one that answers the disclaimer every tie carries" | — | removed; lockstep capstone: "The lockstep induction and the gradient mean, composed." | 6 docstring sites |
| Foundation/DataParallelNode.lean:5 (module), :51, :99 | process | "piece 2", "piece 1", "Since 2026-09-07 … old text verbatim … re-rendered byte-identically", "4b's fold", "§4d piece 2's target statement", "Piece 3 is untouched" | — | title "Data parallelism: the collective as an AST node"; pieces → file/decl names (`DataParallel.dpMeanGrad_eq_grad_meanLoss`); history (text emitter, byte-identical re-render) moved to a `--` comment after the module doc; "(`DataParallelSync.lean` relates them under sync-BN)" added | `grep -n "def allReduceMeanText\|def prettyAllReduceMean\|def parseStack"` (StableHLOPretty.lean:1271/4689, StableHLOParse.lean:33) |
| Foundation/DataParallelSync.lean:4 (module), :231, :650 | process | "piece 3", "(piece 1)", "(piece 2)", "`planning/global_bn_verified.md` §2b", "(§3.2 onward of the plan)", "exactly as in piece 2", "the one-round E[x²] exchange could not have, and the reason it was replaced", undefined "P1 / P2 / P2γ" | — | pieces → file names; P1/P2/P2γ now defined in the module ("sync-BN forward (P1), input-VJP (P2) and γ gradient (P2γ)"); "the lemma a one-round E[x²] exchange does not have" | module already describes the Chan two-collective statistics (`den_syncStats_right` via `bnVar_row_shard_chan`), so no E[x²] staleness here |
| Foundation/DataParallelSyncKit.lean:8, :376 | process | "(T2 forward, T3 step)", "(§4) cancels the R homogeneity (§1) carries" | — | "(its forward tie and its step tie)"; "(`inv_mul_R`) cancels the R the homogeneity lemmas (`*_smul`) carry" | `grep -n "theorem inv_mul_R"` |
| Foundation/DataParallelSyncBf16.lean | checked, no change beyond markers | artifact names in module | the three artifacts exist | — | `ls verified_mlir \| grep …bf16` |
| Foundation/SmoothedLossCot.lean:5 (module) | stale | "Every whole-net T3 tie in the repo pins its top-of-chain cotangent to softmax − oneHot … not what the batched ImageNet renders emit" | 12 step-tie files consume `smoothedLossCotGraph` | "The ties of the per-example SGD-inline renders pin …; the batched ImageNet renders emit a label-smoothed chain, and their step ties — ResNet-34, ResNet-50, MobileNetV2, MobileNetV4, EfficientNet-B0, ConvNeXt-T and ViT, plus the sync-BN twins of the first five — state it through `smoothedLossCotGraph`"; planning cite → `DataParallelNode.lean` | `grep -rln smoothedLossCotGraph --include=*.lean LeanMlir` → ConvNeXtStepTieGB, EfficientNetStepTieG, EfficientNetSyncStepTieG, MobileNetV2StepTieB, MobileNetV2SyncStepTieB, MobileNetV4StepTieB, MobileNetV4SyncStepTieB, ResNet34StepTieB, ResNet34SyncStepTieB, ResNet50StepTieB, ResNet50SyncStepTieB, ViTStepTieGB (12) |
| Foundation/BceLossCot.lean:13, :47 | process | "`planning/archive/proofs_tier_to_paper_nets.md` §3.5 lists … This is that item.", "(the §5 carve-out)" | — | "This file proves that chain is the gradient of a loss, which ResNet-50's BCE step tie needs."; carve-out § dropped | — |
| Foundation/Bf16GradNodes.lean:11 | process | "'The bf16 twins consume the same node' was written in three fold headers and is false." | — | "…needs its own certificate: the bf16 twins do not consume the f32 node." | — |
| Float/Binary32Instance.lean:94 `binary32_e4m3_budget_small` | overclaim | "This makes precise why the deployed net … needs only the measured 0.38 drift, not 61" | `denseMixedBudget binary32.u fp8E4M3.u 4 (3/5) 1 1 ≤ 1/2` | "The same worst-case budget at m = 4 is ≤ 1/2. (That the deployed 784-wide net drifts only a measured 0.38 is an empirical observation, not proved here.)" | read statement |
| Float/Binary32Instance.lean:58 `binary32` | overclaim | "the bound is tight for the grid" | `gridModel 23 u32 _`; no tightness statement | "(the standard-model constant for this grid)" | read def |
| Float/Binary32Instance.lean:5 (module), :76, :147 | process | "Historically … two explicit axioms … now DISCHARGED (post_audit_roadmap §2)", "the 2026-06 audit's gaps 2 and 3", "(kept honest)", planning/archive cite, "Gap 2:", "Gap 3:" | — | history/gap labels removed; "What stays trusted: …"; concrete-SGD bullet now names its scope "(2 inputs, 2 classes, all-zero weights `W0`)" | W0/b0/x0 defs read at :142–145 |
| Foundation/MLP.lean:65 `mnistLinearHasVJP_correct` | overclaim | "Whole-model VJP contract … the degenerate simplest case of the per-architecture `*HasVJP_correct` capstones" | `:= (denseHasVJP W b).correct x dy i` | "`denseHasVJP`'s `correct` field, restated for `mnistLinear` so it can be cited by name." | read proof term |
| Foundation/MLP.lean:83, :99, :313 | process | "— theorem (Phase 7)", "promotes the previous vacuous `rfl` …", "Replaces the vacuous `mlpHasVJP.correct := rfl`" (both defs coexist) | — | labels/history dropped; `mlpHasVJPAt`: "Unlike the canonical `mlpHasVJP`, the backward here is built by the chain rule." | `grep -n "def mlpHasVJP\b"` (still present, canonical) |
| Foundation/MLP.lean:20 `dense`, :130 `relu`, :264 `softmax`, :269 `oneHot`, :272 `crossEntropy`, :291 `mlpForward`, :400 `relu6` | missing | (no docstrings) | defs as written | one-line docstrings stating each definition; `dense` names the row-vector convention (`W i j`: input i → output j) | read each def body |
| Foundation/Tensor.lean:277 `HasVJP`, :400 `HasVJPAt` | missing | (banner comment only) | `backward`, `correct : backward x dy i = Σ_j pdiv f x i j * dy j` | docstrings: the contract; `HasVJP.canonical` inhabits it for every f, so a witness says something only when its backward is a formula proved equal to the contraction; `HasVJPAt` = pointwise form | read structures + `HasVJP.canonical` |
| Foundation/Tensor.lean:557, :573, :818, :836 | process | "No longer an axiom", "now a theorem", "(planning/archive/VJP.md follow-up D)", "(Phase 8, …)" | — | removed | — |
| Foundation/BatchMapVJPAt.lean:3 (module) | stale | "`EfficientNetChainClose.lean` lifts … `batchMapHasVJP`", "the one thing standing between ResNet34FullB.lean and T1", "`maxPool3s2FlatHasVJPAtVec` is in the `Float` tier" | `batchMapHasVJP` declared in this file (:161); `maxPool3s2FlatHasVJPAtVec` at BackwardMaps.lean:246; uses in ResNet34FullBVJP.lean:130 and HeadLayers.lean:74 | "The global lift `batchMapHasVJP` (below) …; `batchMapHasVJPAt` is its pointwise peer, which ResNet-34's stem needs … `maxPool3s2FlatHasVJPAtVec` (`BackwardMaps`) is `_at` by nature"; T1 sentence dropped; "lives with r34's VJP (`ResNet34FullBVJP`) and in `HeadLayers`" | `grep -n "def batchMapHasVJP\b"`; `grep -rn maxPool3s2FlatHasVJPAtVec` |
| Foundation/BatchedBackLinks.lean:69 `bnBatchBack_faithful`, :92, :226 | stale + process | "The first batched-backward primitive", "The `bnBatchLA` layout-reindex wrapper … is a thin remaining layer", "The second batch-separable stage brick (after `seB`)", "The fourth (and last) MBConv stage's…" | `bnBatchLABack_faithful` (same file) is the wrapper | "The network-layout wrapper is `bnBatchLABack_faithful`."; ordinals dropped | `grep -n "theorem bnBatchLABack_faithful"` |
| Foundation/BackwardMaps.lean:284 `maxPool3s2FlatBackB`, :63 `perRowFlatPR` | stale | "(`ResNet34StepTieB.mpInB` …)", "The flat analogue of `rowwise` (`Tensor.lean`)" | `mpInB` is in namespace `Proofs.ResNet34TieB` (BatchedBackLinks.lean:485); no decl `rowwise`; Tensor.lean has `rowwiseHasVJPMat` | `ResNet34TieB.mpInB`; "The flat analogue of `rowwiseHasVJPMat`'s row-wise lift" | `grep -rn "def mpInB"` + namespace lines 429/502 |
| Foundation/BackwardMaps.lean:16, :210, :323 | process | "Until 2026-09-08 these lived inside …FloatBridge files … found vacuous and deleted (planning/…)", "Found 2026-08 because the per-example r34 chain (retired 2026-09-19)…", "Until this lemma the only bridge was…" | — | module keeps where the leaf ties live; :210 hazard moved to a `--` comment (dates dropped); :323 "(`maxPool3s2Back_faithful` is the bridge at the per-example constructor, which no shipped render emits.)" | `grep -rn "theorem maxPool3s2Back_faithful"` (StableHLO.lean:3113) |
| Float/ResNet34FloatBridge.lean:6 (module), Float/ConvFloat.lean:9 | stale | "This file holds the two that are not one line elsewhere: residual skip … a two-operand `add_close` …; global-avg-pool"; ConvFloat "(ResNet-34's strided convs through `ResNet34FloatBridge`)"; "every float conv-net bridge composes" | file declares only `gapFlatF`, `gapFlat_close`, `globalAvgPoolFlat_eq_bnMean`; `add_close` is FloatBridge.lean:796 | retitled "global average pooling"; lists where the other R34 ops' float lemmas live; ConvFloat: "(used by `floatClose_flatConv` and `SgdDescentCnn`)" | `grep -n "^theorem\|^noncomputable def" ResNet34FloatBridge.lean`; `grep -rln flatConvF_close` |
| Float/Bf16Fold.lean:36 (module) | stale | "All theorems kernel-close … ([tests/AuditAxioms.lean])" | `grep Bf16Fold\|bf16_render_faithful tests/AuditAxioms*.lean` → 0 (the `Bf16PoC.*WGradB*` lines there are Bf16GradNodes') | "The theorems here are `rfl`. They are not among the `#print axioms` lines of tests/AuditAxioms.lean." (adding them to AuditAxioms is a cross-slice follow-up) | grep as stated |
| Float/Bf16Fold.lean:3 (module) | overclaim + process | "PoC … (planning §5, the symmetric gap)", "§3b" ×2, "(planning §5)", "the tied-and-lowered bf16 forward — the thing fp8 can prove but not run, and bf16 can now do both" | no bf16 `FloatModel`; tie∘accuracy composition not stated | title "The bf16-mixed MNIST-linear render-tie"; "Accuracy is separate … would give an accuracy bound …; that composition is not stated in the repo (there is no bf16 `FloatModel`)" | FloatModel grep (binary32, fp8E4M3 only) |
| Float/Bf16Fold.lean:90, :100, :123, :133, :142 | overclaim + proof-narrative + process | "the render-tie covers the graph we ship", "That op now exists … closes the gap the header left open", "(… it did, first try.)", "the statement the header flagged", "Depth k follows by iterating" | depth-1 and depth-2 `rfl` ties only | "transfers to the `dotInBf16` node, the form that can be lowered"; depth-2 section rewritten; "first try" moved to a `--` comment (hazard kept); "No depth-k theorem is stated" | `grep -n "^theorem" Bf16Fold.lean` (4 theorems) |
| Float/E4M3Fold.lean:3, :70, :80 | process | "PoC … (planning §3b)", planning/archive cite, "Where §3c bounds…", "cf. `LinearFold.lean`" note | — | title "The E4M3 (fp8) quantized MNIST-linear render-tie"; §3c → `linear_e4m3_argmax_preserved` (FloatBridge) | `grep -n "theorem linear_e4m3_argmax_preserved"` (FloatBridge.lean:1842); AuditAxioms prints QuantPoC.* (:1056–1057) so the axioms cite stays |
| Float/DepthwiseMixedFloatBridge.lean:35 `dwSlice` | proof-narrative / process | "⚠⚠ … REUSED … The first draft of this file defined its own `dwWindow` … broken for three commits … ▶ grep before defining" | `dwSlice W ch : Vec (kH*kW)` | docstring: "Channel ch's flattened filter … `dwKernelMat`'s single column."; the build hazard kept as a `--` Note comment (history dropped) | read def |
| Float/DepthwiseMixedFloatBridge.lean:3, :44, :69, :78; Float/ConvMixedFloatBridge.lean:10 | process | "planning/archive/bf16_renderer.md §10.2 predicted", "§9.3 separation", "§9.2", "calls that gap 'the single biggest item in this document'" | — | planning refs removed; numeric table labelled "arithmetic outside Lean, quoted as illustration" | — |
| Foundation/OpaquePrefix.lean:10, Foundation/BatchedStages.lean:39 `stemB` | process | "Until 2026-09-08 … each carried a private copy …", "since 2026-08-08", "(re-spelled 2026-09-05, planning/archive/…)" | — | OpaquePrefix: history moved to a `--` comment (dates dropped); stemB: dates/planning dropped, warning kept as "Note:" | `grep -n "def opaqueA"` (A0–A24, 25 slots) |
| Float/BnFloatBridge.lean:190 `FloatModel.bnMean_close_of` | overclaim + process | "The resulting mean accuracy is DERIVED for the kernel actually shipped rather than supplied by analogy (planning/… §3.31 route 3)" | takes `hsc : |fsum x − Σ x| ≤ γn·Σ|x|` as a hypothesis | "That the shipped kernel's reduction meets `hsc` is the hypothesis, not proved here"; "Classically every summation order satisfies such a bound" | read signature |
| Float/FloatBridge.lean:96 `mlpF`, :1458 `cotErr_nonneg`; Foundation/Batched.lean:29; BatchedBackLinks.lean:5,:373; DataParallelNode.lean:78 | process | "Tier-1", "(e.g. the per-layer η-composition rungs)", "the §1 fold", "T3 tie", "4b's fold" | — | removed / "step tie" / "a batched parameter fold" / "The conv weight-gradient fold composed with the node" | — |
| (all 39 edited files) | process (sweep) | ⭐/⭐⭐/⭐⭐⭐/▶/✅ markers; ⚠/⛔ markers | — | 136 docstring lines: ⭐▶✅ deleted, ⚠/⛔ → "Note: " (bullet-leading "* Note:" reverted to "* "); `--` comments untouched | scratchpad sweep.py restricted to `/--`…`-/` and `/-!`…`-/` ranges; after sweep `grep -c "[⭐⛔⚠▶✅]"` hits only `--` comments (GradNodesB ×4, DataParallelSyncBf16 ×1) |
| Float/FloatBridge.lean, FloatClose.lean, … | checked, kept | "float tier", "Higham §2.2 / §3.1", "Jordan 2024", historical/upstream-issues path, P1–P4 labels (now defined in DataParallelSync's module doc), `*PoC` namespace names | book citations / real names / real path | unchanged | — |
| (slice) | deferred §5 | none of this slice's entries fall under D1 (B0/ViT class count, small-CNN dense heads, MaxPool distinctness, MNv4/ConvNeXt/ViT `*CotIn_eq_vjp`); SpecVJP's `Vec 10` class pins left as stated | — | — | — |

Verification (all edits): Lean code outside comments/docstrings byte-identical to HEAD for all 49 files (strip `/-…-/` and `--` then compare); every docstring block in edited files has balanced backticks and no nested `/-`; every new backticked identifier grep'd to a declaration, module or namespace (remaining misses are variables, Mathlib names, tactic/MLIR words — none contains a docstring-checkrefs project marker). No build run (per instructions).
| Float/ResNet34BlockBridge.lean:38 `bnStep_close` | overclaim | "the BN-before-swish path in EfficientNet's MBConv uses it directly" | only use is `floatClose_bn` (FloatComposeBridge.lean:178) | "`floatClose_bn` wraps it as a `FloatClose` instance" | `grep -rn bnStep_close --include=*.lean LeanMlir` (not in report; found while checking `floatClose_bn`) |
