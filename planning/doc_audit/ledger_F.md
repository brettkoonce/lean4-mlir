# Ledger — slice F (LeanMlir/Proofs/Certificates/** + scripts/certs generators)

Columns: `file:line | kind | old claim (short quote) | what the statement actually says | new text (short) | how nouns/numbers were checked`

Generated files: every generator touched was first re-run unmodified and confirmed byte-identical
against HEAD (`git status` clean), then re-run after the edit; the only diff in each emitted .lean
is the docstring text. Line numbers are pre-edit.

| file:line | kind | old claim | statement | new text | check |
|---|---|---|---|---|---|
| LipschitzCert.lean:6–8 | process | "(`planning/archive/robustness.md`, `…robustness_ladder.md`): … turned from a number into a **theorem**" | module doc | "The certificate behind the `mnist-{linear,mlp,cnn}-pgd` demos: the lower bound of the `cert ≤ TRUE ≤ PGD` sandwich, stated as a theorem." | planning paths dropped; no noun added |
| LipschitzCert.lean:16–17 | stale | "`specNormW` / `specNormConvTapSum` ([`LeanMlir/VerifiedTrain.lean`])" | both are `private def` in LeanMlir/VerifiedAttack.lean:35, :104 | link now points at VerifiedAttack.lean | `grep -n "specNormW\|specNormConvTapSum" LeanMlir/*.lean` |
| LipschitzCert.lean:202–210 | overclaim + stale | "`hp` — Monte-Carlo/Clopper–Pearson estimates are never exactly 0 or 1"; "leaving only … `hg` as a hypothesis" | `hp : ∀ c y, p c y ∈ Ioo 0 1` (true probabilities, every class, every point); SmoothingGaussian.lean proves `hg` as `smoothing_probit_lipschitz` | "every class probability `p c y` in `(0,1)` at every point `y` (`hp`)"; "…discharges `hmono`/`hanti` … and proves `hg` … (`smoothing_probit_lipschitz`); `smoothing_certified_radius_classifier` is the resulting form …, whose hypotheses are `0 < σ`, measurability, `hp` and the radius bound" | read the signature; `grep -n "theorem smoothing_probit_lipschitz\|theorem smoothing_certified_radius_classifier" SmoothingGaussian.lean`; classifier's hypotheses are `hσ hC hp hδ` |
| SmoothingGaussian.lean:8–15 | overclaim + process | "The complete G1–G4 ladder of `planning/archive/…`"; "NO smoothing-side hypotheses left" | `smoothing_certified_radius_classifier` takes `hp : ∀ c x, ∫ 1[C(x+σz)=c] ∈ Ioo 0 1`; so do `smoothing_certified_of_le`, `smoothing_mc_certified`, `smoothing_cp_certified{,_solved}` | "…`smoothing_probit_lipschitz`. The one remaining hypothesis on the smoothed classifier is `hp`: every class's smoothed probability lies in `(0,1)` at every point. `SmoothingNetSemantics.lean` discharges it for argmax nets." | read the five signatures; `grep -n "argmaxNet_smoothProb_mem_Ioo" SmoothingNetSemantics.lean` |
| SmoothingGaussian.lean:17–41 | stale | "This file proves the three facts … `stdNormalCDF_strictMono` … `stdNormalQuantile_continuousOn`"; "G2 … also lives here: `stdNormalCDF_quantile`" | none of these are declared here; all in GaussianQuantile.lean | "`Φ` …, `Φ⁻¹` … live in `GaussianQuantile.lean`. This file proves:" + list of `pi_gaussian_np_shift`, `stdGaussian_np_shift`, `smoothing_probit_lipschitz`, `smoothing_certified_radius_{gaussian,cohen,classifier}`, `smoothing_certified_of_le` | `grep -nE "^(theorem\|lemma\|…)" SmoothingGaussian.lean GaussianQuantile.lean`; each listed name found in SmoothingGaussian.lean; `stdGaussianPi` abbrev at :122 |
| SmoothingGaussian.lean:42–48 | overclaim + process | "`hp` … the realistic regime, since Monte-Carlo/Clopper–Pearson class-probability estimates are never exactly 0 or 1. See `planning/archive/…`" | `hp` is on the true probabilities; SmoothingCPScorecard has a count 10112/10112 | "every use here is guarded by `hp`. `smoothing_certified_radius_probit` asks monotonicity … only on `(0,1)` because no globally monotone function agrees with the true (unbounded) quantile there." | statement read |
| SmoothingGaussian.lean:88–91 | stale + process | "`hg` — the Neyman–Pearson core, the ONE remaining smoothing-side hypothesis, G2–G4 of `planning/…`" | `hg` abstract over `p`; proved for smoothed scores by `smoothing_probit_lipschitz` | "`hg` — for Gaussian-smoothed `[0,1]` scores this is `smoothing_probit_lipschitz`, applied in `smoothing_certified_radius_cohen`"; `hp` now "every class probability `p c y` inside `(0,1)` at every point `y`" | `grep -n smoothing_probit_lipschitz SmoothingGaussian.lean` (used at the `_cohen` body) |
| SmoothingGaussian.lean:453 | overclaim | "`hp` — Φ⁻¹ needs `(0,1)`; Monte-Carlo estimates always satisfy this"; "the certificate the `*-smooth` drivers report, end to end" | `hp : ∀ c x, (∫ z, if C (x+σ•z)=c then 1 else 0 ∂γ) ∈ Ioo 0 1` | "`hp` — every class's smoothed probability lies in `(0,1)` at every point, i.e. no decision region is Gaussian-null or conull …; for an argmax net it follows from one strict-argmax witness per class (`argmaxNet_smoothProb_mem_Ioo`) … The radius has the form the drivers report, stated at the true class probability." | signature read; `argmaxNet_smoothProb_mem_Ioo` at SmoothingNetSemantics.lean:139 |
| SmoothingGaussian.lean (§G3/§G4 `/-!` blocks, `smoothing_probit_lipschitz`) | process + copied | "same pointwise MLR trick as G2"; "The plan's riskiest item … turned out to ship with Mathlib"; "the G3 bound"; "the G1 radius theorem"; "**G4 core** … now a THEOREM"; "`f : E → [0,1]`" | f is on `EuclideanSpace ℝ (Fin (n+1))`; G1/G3 are `smoothing_certified_radius_gaussian` / `stdGaussian_np_shift` | plan labels replaced by the declaration names; Mathlib anecdote dropped; `f : EuclideanSpace ℝ (Fin (n+1)) → [0,1]`, "`p(x)` lies inside `(0,1)` at every `x`" | signature of `smoothing_probit_lipschitz` read. `-- § G2/G3/G4` section comments left (comments, not docstrings) |
| SmoothingMC.lean:7–20, :112–120 | process + overclaim (dropped hyp) | "the honest gap flagged since the smoothing theorems landed. This file closes it"; "now end to end a theorem"; `smoothing_mc_certified` doc omits `hp` | `smoothing_mc_certified` takes `hC`, `hp` | "This file ties the estimate to the theorem"; "…under the classifier theorem's hypotheses: `C` measurable and `hp` …"; decl doc adds "Hypotheses: `C` measurable, and `hp` — every class's smoothed probability lies in `(0,1)` at every point." | signature at SmoothingMC.lean:121 |
| SmoothingCP.lean:7, :27–28, :366–372, :420–426 | process + overclaim (dropped hyp) | "This file closes that last arithmetic gap"; "Guarantee AND arithmetic now match the deployed CERTIFY"; `smoothing_cp_certified{,_solved}` docs omit `hp`; "the hypothesis is pure kernel rational arithmetic" | both theorems take `hC`, `hp` | "This file states the certificate with that bound"; "…certified, under the classifier theorem's hypotheses (`C` measurable, `hp`)"; decl docs name `hp`; "the tail hypothesis is pure kernel rational arithmetic" | signatures at SmoothingCP.lean:373, :427 (not in the report; same class as its hp finding) |
| SmoothingNetSemantics.lean:5–20 | stale + process | "the last informality flagged in the scorecard headers. This file closes it"; "* `stdGaussian` full support — `IsOpenPosMeasure` instances for …" (listed as this file's content) | both instances declared in GaussianQuantile.lean:22, :28 | "This file instantiates `C` as the argmax of a logit map and discharges both:"; "* full support of `stdGaussian` (the `IsOpenPosMeasure` instances of `GaussianQuantile.lean`) gives the witness regions positive mass;" | `grep -n "^instance" GaussianQuantile.lean SmoothingNetSemantics.lean` |
| GaussianQuantile.lean:169 | process | "G2's Neyman–Pearson bound enters through it" | `stdNormalCDF_quantile` | "`smoothing_probit_lipschitz` applies it to feed the Neyman–Pearson bound its threshold" | `grep -n stdNormalCDF_quantile SmoothingGaussian.lean` → :393 inside `smoothing_probit_lipschitz` |
| LipschitzCertInstance.lean:11–21 | stale/missing + process | "Closes the 'certificate machinery, never instantiated' gap"; "Two instances" | three sections: linear, 2→2→2 MLP, trained 49→8→10 `mlpT` (`trained_demo_certified{,_gram,_gram2}`, `W1t_lip_lower`, `W2t_lip_lower`, `mlpT_logit_continuous`) | "Three instances: …" with the trained bullet naming `mlpT_lip`/`_gram`/`_gram2`, the lower bounds, and the three files that build on `mlpT` | `grep -nE "^(theorem\|def…)"` on the file; `grep -ln LipschitzCertInstance` → LipschitzCertScorecard, SmoothingNetSemantics, SmoothingNetWitness (plus DenseEuclid, TrainedMlpWitness, AuditAxioms) |
| LipschitzCertInstance.lean:126 | process (comment) | "-- Generated by scripts (see planning)" | a `--` comment | not changed (comment; §4 is docstrings only) | — |
| SmoothingPhiBounds.lean:88–89 | stale | "32 terms: relative error < e⁻²⁴ at `x = 5.12`, our largest use" | only grid: `phiScanRev (1/1000) 3300` (SmoothingDecScorecard) → largest `ratPdfUB` arg 3299/1000, `ratExpLB` arg 3.299²/2 = 5.4417 | "relative error below e⁻³² at `x = 3.299²/2 ≈ 5.44`, the largest argument the 3300-panel `h = 1/1000` grid of `SmoothingDecScorecard.lean` uses" | `phiGridUB` def (left endpoints m·h, m < 3300); python: Σ_{i≥32} x^i/i!/eˣ = 6.9e-15 = e^-32.6 at 5.4417 |
| SmoothingPhiBounds.lean:146–148 | stale | "~190-digit numerators whose lcm across 640 grid points" | 3300 grid points; numerators 120–234 digits | "up to ~230-digit numerators … across the 3300 grid points" | python over `k = 1..3299 step 97` of exact `ratPdfUB (k/1000)`: min 120, max 234 digits |
| SmoothingPhiBounds.lean:6–8, :24 | process | "This file closes that gap with"; "the §prefix-scan section" | — | "This file supplies"; "the prefix-scan section" | "(279 images)" re-counted: `grep -c "^/-- img" SmoothingDecScorecard.lean` = 279 |
| IntervalBound.lean:11–13 | overclaim (mild) | "the certificate that scales past the h=16 wall to canonical widths" | no canonical-width instance; dense IBP instances are 784→16→10 | "so it is the tier that could scale past the h = 16 wall. Every dense instance here is still at h = 16 (784→16→10)." | `H, K, DIM = 16, 10, 784` in lipschitz_cert_scorecard_full.py:53; `ibp2_certified_at_eps` used only in LipschitzCertScorecardIBP{,Uncon} |
| IntervalBound.lean:25–26, :174–177 | stale + process | "A tighter bracket (CROWN, `planning/archive/crown_ibp.md`) therefore plugs into this tier"; "a CROWN backward pass, say" | CrownBound.lean does not discharge `BoxSoundE`; it certifies through `certified_of_marginPos` | "Any tighter bracket therefore plugs into this tier … The CROWN bound of `CrownBound.lean` plugs in one rung lower, on the margin, through `certified_of_marginPos`." | `grep -n "certified_of_boxSound\|BoxSoundE\|theorem certified_of_marginPos" CrownBound.lean` |
| IntervalBound.lean:60 | stale | "`CertifiedAt` (`LipschitzCertScorecard.lean`)" | defined DenseEuclid.lean:257 | "(`DenseEuclid.lean`)" | `grep -n "def CertifiedAt" Certificates/*.lean` |
| CrownBound.lean:48–53 | stale + process | "(… `planning/archive/crown_ibp.md` §5.5) … so the coefficients stay at the same `/256` scale as the weights" | slope on /2^8; coefficients `/65536`, `A` `/16777216` (emitted instance) | "…so the slope sits on the weights' `/256` grid, the coefficients at `/65536` and `A` at `/16777216`, instead of carrying the layer-1 denominators." | `grep -n "65536\|16777216" LipschitzCertScorecardCrown.lean`; crown_ibp_scorecard.py `DC = DEN*DS`, `DA = DC*DW` |
| DenseEuclid.lean:192–197 | wrong | "strictly tighter than Frobenius whenever the spectrum has any spread" | `(Σσ⁴)^¼ < (Σσ²)^½` iff ≥ 2 nonzero σᵢ | "whenever `W` has rank ≥ 2" | math: equality iff one nonzero σ |
| scripts/certs/lipschitz_cert_scorecard.py:436 → LipschitzCertScorecard.lean:644 (`scorecard`) | overclaim | "**The scorecard, as a theorem**: … the capped net certifies 34/100 … and the unconstrained net 1/100"; "the 8 and 1 witnesses BELOW" | `(cappedCerts.length = 8 ∧ ∀ p ∈ cappedCerts, CertifiedAt mlpS (1/10) …) ∧ (unconCerts.length = 1 ∧ …)`; lists defined above the theorem | "**The proved core of the scorecard**: the 8 capped-net and 1 unconstrained-net witnesses in `cappedCerts`/`unconCerts` each carry a `CertifiedAt … (1/10)` proof … The dataset counts (34/100 capped, 1/100 unconstrained) are exact-rational measurements recorded in the `certMargin*` lines above, not theorems." | numbers are the generator's own `len(cert_c)`, `len(cert_u)`, `measured_c`, `measured_u`; regenerated, diff = docstring only |
| scripts/certs/lipschitz_cert_scorecard.py:177, :193–201 → LipschitzCertScorecard.lean:3, :19–27 | process + overclaim (mild) | "(post_audit_roadmap §1)"; "the roadmap's caps 1.5–2"; bold "**1/100 certified**", "**34/100 certified**" | counts are measurements (header's own next paragraph) | title without the roadmap tag; "tighter caps, 1.5–2, cost too much …"; "(measured, see below)" / "(measured)" after the bold counts | regenerated; diff = docstring only |
| scripts/certs/lipschitz_cert_scorecard_ibp.py:206 → LipschitzCertScorecardIBP{,Uncon}.lean:7 | overclaim | "the box certificate proves **92/100**, **88/100**, **69/100**, **24/100** predictions robust" | aggregate `scorecard_ibp{,_uncon}` states 8/8/8/8 (capped) and 8/8/2 (uncon) witnesses | "the box certificate certifies (measured by exact rational interval propagation) **92/100**, … predictions robust …  *Theorem vs. measurement* below says which of these carry Lean proofs." | statement of `scorecard_ibp_uncon` read (lengths 8, 8, 2); regenerated, diff = docstring only in both files |
| scripts/certs/lipschitz_cert_scorecard_full.py:426–428 → LipschitzCertScorecardFull.lean:17–19 | overclaim (wrong direction) | "full-pixel L2 … a strictly stronger, directly comparable-to-the-literature perturbation model" | 4×4 averaging: ‖Pδ‖₂ ≤ ‖δ‖₂/4 (Cauchy–Schwarz per 16-pixel block), so pooled ε covers pixel radius 4ε | "…the perturbation model the literature reports. It is not the pooled-feature L2 of `LipschitzCertScorecard.lean`: 4×4 averaging maps a raw-pixel perturbation of L2 norm r to a pooled one of norm at most r/4, so a pooled-L2 radius ε covers raw-pixel radii up to 4ε, and the two tiers' ε are not directly comparable." | math: (Σ₁₆δ/16)² ≤ Σ₁₆δ²/16 per block; pooled coordinate = pixel-sum/4080 = block mean of k/255 pixels |
| scripts/certs/lipschitz_cert_scorecard_full.py:414–415 → LipschitzCertScorecardFull.lean:6–7 | process | "(the 2026-07 audit's gap #3)" | — | dropped | regenerated; diff = docstring only |
| scripts/certs/ibp_conv_scorecard.py:404 (+ script docstring :21–23) → IbpConvScorecardNet.lean:10–11 | overclaim | "the QUANTIZED net is what is certified, so the certified network is the deployed one" | `CertifiedAtLinf3 net ε x y` over the ℝ cast of the k/256 data; no float composition for this net (`grep -rln IbpConvScorecard LeanMlir tests` → only the scorecard files and AuditAxiomsHeavy) | "The QUANTIZED weights are what is certified, under exact-ℝ semantics; the float-evaluated forward is not covered (no FloatBridge composition exists for this net, unlike the dense `LipschitzCertFloat.lean`)"; script docstring likewise | regenerated; see diff note at end |
| scripts/certs/ibp_conv_scorecard.py:398 → IbpConvScorecardNet.lean:3 | process | "The first certificate in this repo that covers a convolution …" | — | "A certificate over a convolution, a max-pool, and more than two layers." | regenerated |
| scripts/certs/lipschitz_cert_float.py:149 → LipschitzCertFloat.lean:11 | process | "The 2026-07-02 audit's gap #1, closed:" | — | sentence starts "The scorecard's per-image Lipschitz-margin certificates …" | `lipschitz_cert_float.py` rerun; diff = docstring only |
| scripts/certs/lipschitz_cert_pair_sdp.py:236, lipschitz_cert_pair_sdp_full.py:204, :226–227 → LipschitzCertScorecardSDP{,Uncon,Full,FullUncon}.lean | process | "The tighter-Lipschitz-constant pass over …"; "(see `planning/archive/certs_heavy_psd_memory.md`)" | — | "A tighter Lipschitz constant over …"; planning pointer dropped | regenerated (≈8 min and ≈10+ min on CPU); see diff note at end |
| scripts/certs/smooth_scorecard_gen.py:148–153 → SmoothingCPScorecard.lean:11–16 | process | "Honest scope: … closed corpus-wide by …"; "remain untied (the same witness-generator pass at full width)" | — | "Scope: … The float Φ⁻¹ printout gets proved decimal lower bounds in the decimal-radius scorecard …; this scorecard's own 784-dim driver checkpoints are not tied to them." | regenerated |
| scripts/certs/smooth_dec_scorecard_gen.py:150–153 → SmoothingDecScorecard.lean:26–29 | process | "Honest scope: …"; "(the same witness-generator pass at full width)" | — | "Scope: …; the scorecard's own 784-dim driver checkpoints are not tied to them." | regenerated |
| scripts/certs/smoothing_net_witness_gen.py:86–100 → SmoothingNetWitness.lean:3–20 | process + overclaim (mild) | "The smoothing chain's last informal hypotheses"; "(that tie = the same generator pass at full width)"; "the driver's float forward vs this real-semantics net is the FloatBridge tier's story" | no FloatBridge composition exists for the smoothing chain | "The smoothing chain's hypotheses on the abstract classifier …"; "Scope: mlpT is the pooled net of the Lipschitz certificates, not the 784-dim smoothing-driver checkpoint, and the driver's float forward is not tied to this real-semantics net." | `smoothing_net_witness_gen.py` rerun; diff = docstring only |
| (slice-wide) | process | planning-doc paths, audit dates/gap numbers, plan labels (G1–G4), "closes the gap"/"now a THEOREM" status notes, one ⚠ marker | — | removed or replaced by the declaration/file they referred to; about 37 docstring sites across 22 files: LipschitzCert 2, SmoothingGaussian 8, SmoothingMC 2, SmoothingCP 2, SmoothingNetSemantics 1, GaussianQuantile 1, SmoothingPhiBounds 2, IntervalBound 2, CrownBound 1, LipschitzCertInstance 1, LipschitzCertScorecard 2, …Full 1, …Float 1, …SDP/…SDPUncon 1 each, …SDPFull/…SDPFullUncon 2 each, SmoothingCPScorecard 1, SmoothingDecScorecard 1, SmoothingNetWitness 2, IbpConvScorecardNet 1 | count from the diff; "tier" kept where it names a certificate family (IBP tier, LipSDP tier); `-- §` section comments and Python-side script docstrings (other than ibp_conv_scorecard.py's) left alone |
| Certificates/README.md | deferred | markdown | — | not touched (README*.md out of scope) | — |

Deferred §5: none of the slice's entries fall under D1 (no B0/ViT class pin, dense-head, MaxPool or MNv4/ConvNeXt/ViT `*CotIn_eq_vjp` text in Certificates/).
