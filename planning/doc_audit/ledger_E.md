# Ledger — slice E (LeanMlir/Proofs/Codegen/)

`file:line | kind | old claim | what the statement actually says | new text | how nouns/numbers were checked`

Line numbers are at the edited file as of this pass.

StableHLOParse.lean:3-21 | overclaim+stale | "closes the semantic half of R4: `den (emit g) = fderiv`"; "`parse_skel`"; "structure … is now proven recoverable — it leaves the trusted surface"; "load-bearing" | `roundtrip : parse (toToks (skel a)) = some (skel a)` — Raw/List Tok only; no `den (emit g) = fderiv` theorem exists; `*_faithful` equate `den` with named ℝ fns (e.g. `fwdGraph_faithful : den (fwdGraph W b x) = mnistLinear W b x`) | module doc lists Raw/skel/toToks/parse/`parse_toToks`/`roundtrip`; scope: statement about `toToks`, not the text; emitTok (operand order/arity/types), lexical syntax, per-op semantics, lowering, float32≈ℝ trusted | `grep -n "theorem parse_toToks\|theorem roundtrip"` StableHLOParse; `grep -rn parse_skel` (none); `grep -n "theorem fwdGraph_faithful"` StableHLO.lean:2769; `grep -n "def mnistLinear"` Foundation/MLP.lean:62
StableHLOParse.lean:~210 (`roundtrip`) | overclaim | "op structure / shapes / SSA names leave the trusted surface" | same statement | "postorder token encoding … invertible … statement about `toToks`, not about the text …emitTok … remains trusted" | read `roundtrip` statement + `pretty` body (StableHLOPretty `pretty` = serializeToks B (toToks (skel g)))
StableHLOPretty.lean:20-24 (module) | overclaim | "lexical conformance … checked by execution (iree-compile / PJRT) and by the StableHLOParse round-trip" | roundtrip never reads text | "emitTok text not proved; scripts/gates/parse_verified_mlir.py parses every committed verified_mlir/*.mlir; trainers run under PJRT; roundtrip covers only toToks" | parse gate globs `verified_mlir/*.mlir` (parse_verified_mlir.py:45)
StableHLOPretty.lean:~4646 (`pretty`) | overclaim | "round-trip `parse (toToks (skel a)) = skel a` … the printer can't structurally drift" | `pretty` = `serializeToks B (toToks (skel g))`, returns "%MALFORMED" on bad stack | "tokens printed are exactly toToks (skel g) … invertible; emitTok text trusted; bad stack → %MALFORMED" | read `pretty` def body
StableHLO.lean:3175,3632,3665,3678,3692,3717,3731 (`bnPerChannelF_faithful`, `depthwiseF_faithful`, `swishF_faithful`, `sigmoidF_faithful`, `geluF_faithful`, `softmaxRowF_faithful`, `matmulF_faithful`) | overclaim | "(`rfl`, so kept out of the axiom audit — `roundtrip` covers it structurally.)" | each is `den (.xF …) = <ℝ fn> (den e) := rfl`; roundtrip is about toToks∘skel | "(`rfl`: `den`'s arm is this function by definition.)" | read all 7 statements; `grep -c <name> tests/AuditAxioms.lean` = 0 for all 7 (not in audit, so no audit claim added)
StableHLO.lean:31-44 (module) | overclaim | "Every verified artifact in `verified_mlir/` is `pretty` of a term of one typed AST … the rendered program and the denoted one are one object" | renderers add hand-written text (signature/constants, report-only `%loss`, `%bc1`/`%bc2` passthroughs, ConvNeXt GAP-backward, `%dy`, `sgdOf`) | "renderers build the computation from `pretty` of `SHlo` terms and add hand-written text around it: [list]… `pretty` is trusted: the proofs are about the term `den` reads, not the text" | `grep -ln "REPORT-ONLY\|report-only" Codegen/*.lean` (10 files); `grep -n "%bc1"` CnnRender:375,591; ConvNeXtRender `%dy` (convNextBackAll `none` arm), `%dgi…%dgapf` block, `sgdOf nPsW "psW"` read in place
ConvNeXtRender.lean:4-36 (module) | overclaim+stale+process | "rendered ENTIRELY from the verified AST"; "all 180 params are now SHlo ops"; "⭐ Since 4c leg 3 (2026-09-07)"; retired scalar-LN note dated 2026-07-29 | file writes only convnext_train_step.mlir; 182 params (`allParams`: 4 stem + 18×9 block + 3×4 down + 4 head); SGD psW update is `sgdOf` text; `%dy` divide + GAP-backward are hand-written | title "…with declared carve-outs"; lists what the file writes/holds, the gradient nodes (`.convStride4WeightGrad`/`flatConvStride4WeightGradHasVJP`, `.convStridedWeightGrad`/`flatConvStride2WeightGradHasVJP`, `sWGradGeom`) and the hand-written text; dates/leg labels and the retired scalar-LN note dropped | `grep -rln 'verified_mlir/<f>"'` for every convnext* artifact: only convnext_train_step.mlir from this file, all others from ConvNeXtRenderB; 182 from `allParams` body and `#guard cnxWdCounts 10 == (59, 123)`; artifact func args: convnext_train_step 184, convnext_fwd 183 (awk over func.func line)
ConvNeXtRender.lean:~660 (`convNextFwdFaithfulV`) | stale+overclaim | "rendered ENTIRELY from the verified AST … 180-parameter signature … (181 inputs) … logits [32, 10]"; implied writer of convnext_fwd | 182 params/183 inputs; returns `[cBS, nClasses]`; committed convnext_fwd.mlir written by ConvNeXtRenderB `convNextFwdRenderB` (ConvNeXtRenderB:999); `TestConvNeXtFwdBTie` compares | new docstring: chain + signature/banner/`chLnPrelude`; writer is `convNextFwdRenderB`; byte tie + regen prefix check | read ConvNeXtRenderB:990-1000, tests/TestConvNeXtFwdBTie.lean:44, scripts/regen_verified_mlir.sh:364
ConvNeXtRender.lean:~800 (`convNextTrainStepFaithfulV`) | stale | "(except the two documented weight-grad gaps … neither of which has a VJP-cert `SHlo` op)"; "§2a-quinquies" | both gradients are SHlo nodes now; hand-written: signature, `%sc`/`%bsc`, `chLnPrelude`, `%dy`, GAP-backward, psW `sgdOf` | new carve-out list; 184 in / 182 out | read function body (argSig/retTyL from `allParams`), artifact 184 args
ConvNeXtRender.lean:~895 (`convNextAdamTrainStepFaithful`) | stale+process | "545 in / 543 out … 180 θ"; "As of 2026-07-28 all 180 params are pretty(AST) end to end"; "certified peer of the hand-written render in tests/TestConvNeXtTrain.lean"; "83,434,629 floats, spread 0/180"; "handoff §2h-quater" | writer wrapper for ConvNeXtRenderB (e.g. :1018 convnext_adam_train_step.mlir); 551 in / 549 out; hand-written GAP-backward, `%bc` passthroughs, report-only `%loss`; K derived from nClasses (`negAK`) | new interface 551/549 at T; carve-out list; tie history moved to a `--` comment without the numbers (TestConvNeXtTrain.lean says 179/180 bit-exact, the old docstring said 0/180 — conflicting, so no number kept) | awk over convnext_adam_train_step.mlir func line: 551 args, 549 result types
ConvNeXtRender.lean:~895 emitted string "121 of 180 params take %wdz" | stale (emitter text) | "121 of 180" | `#guard cnxWdCounts 10 == (59, 123)`; 182 params | NOT CHANGED — emitter text baked into artifacts; generator fix + regen of the wx artifacts is plan D2 | `grep -n "no_weight_decay" verified_mlir/convnext_adamwx_train_step.mlir` (line 5208 carries the stale text)
MlpRender.lean:4-17 (module), :21-28 (`mlpTrainStepFaithfulV`) | overclaim | "rendered ENTIRELY … every emitted line is `pretty(provenNode)`"; "`MlpFold` proves each output's `den` = the certified loss-descent step" | renderer appends hand-written report-only `%loss` (lossCode); `mlp_train_step_tied_certified` folds only W₂ to ∂CE/∂W₂ (`mlp_W2_tied_totalloss`), other five are θ − lr·(Jacobian · `mlpCotOut{1,0}` chain cotangent) | "every line that feeds a returned parameter is `pretty` of a denoted node; appended report-only `%loss` hand-written, feeds nothing"; MlpFold scope stated per W₂ vs other five | read MlpRender body; read MlpFold.lean:60-200 statements (W*_den_certified, mlp_W2_tied_totalloss, mlp_train_step_tied_certified)
MlpRender.lean emitted banner "every line is pretty(verified AST node)" | overclaim (emitter text) | banner in mlp_train_step.mlir | contradicts the `%loss` marker in the same file | NOT CHANGED — emitter string; changing it re-renders `verified_mlir/mlp_train_step.mlir` (and same banner in cnn_train_step) — listed for the lead | —
CnnRender.lean:4-15 (module) | overclaim+stale | "Every emitted line is `pretty` of a denoted `SHlo` node"; "(`emitTok`, `StableHLO.lean`)" | cnn render + packed renders append report-only `%loss`, packed renders pass `%bc1`/`%bc2` through; `emitTok` is in StableHLOPretty.lean:1367 | "Every line that feeds a returned parameter is pretty…; hand-written: signatures/constants, `%loss` (cnnTrainStepFaithfulV + packed renders), packed `%bc1`/`%bc2` passthroughs"; emitTok location fixed | `grep -n "REPORT-ONLY\|report-only" CnnRender.lean`; packed signatures read at :609 (`%bc1`,`%bc2` in every packed opt); `grep -n "^def emitTok" StableHLOPretty.lean`
CnnRender.lean:21-31 (`cnnTrainStepFaithfulV`) | overclaim | "ENTIRELY … every emitted line is `pretty(provenNode)`" | lossCode (report-only `%loss`) appended at :85-104 | "every line that feeds a returned parameter is pretty…; appended report-only `%loss` hand-written, feeds nothing"; "`CnnFold` proves each output's den = certified loss-descent step" left as is (deferred §5: small-CNN dense-head conjuncts / §2(b) slice H) | read function body
CnnRender.lean:~113 (`cifarTrainStepFaithfulV`), ~200 (`cifar8TrainStepFaithfulV`) | report-was-wrong | report says these carry a hand-written `%loss` | neither returns `%loss` or appends lossCode; their text besides `pretty` output is signature + banner + return | NOT CHANGED ("ENTIRELY" kept); CifarFold / Cifar8StepTie clauses deferred §5 (small-CNN dense head) | read both function bodies (returns at :191-193, :323-324)
MobileNetV4RenderB.lean:7-16 (module, UIB diagram) | stale | "pre-DW → BN → relu -- takes the block's stride"; "post-DW … at stride 1 if a pre-DW already consumed it" | `uibFwdStridedB` runs pre-DW (BN only, stride 1) + expand at 2h, `.depthwiseStridedAt` on the post-DW; `uibFwdSkipB` pre-DW BN only | "pre-DW → BN -- stride 1, no activation (timm dw_start)"; "post-DW → BN → relu -- carries the block's stride (timm dw_mid)" | read uibFwdSkipB (:212) and uibFwdStridedB (:268) bodies
MobileNetV4RenderB.lean:~31 (module) | stale | "`cnx_render_dw7*_certified`" | `grep -rn cnx_render_dw7 LeanMlir` → only this line | "(`depthwiseFlatHasVJP`, `depthwiseBack_faithful`; …)" | Depthwise.lean:248 def depthwiseFlatHasVJP; StableHLO.lean:3647 theorem depthwiseBack_faithful
MobileNetV4RenderB.lean:~20 (module) | process | "⚠ 8,447,322 … The `3,737,088 / 14-block` figure this line carried until 2026-09-07 … `ed5a797`" | — | kept the two counts, dropped history | python sum over func.func signature of mnv4_fwd.mlir: 233 slots, 8,447,322; mnv4in_fwd.mlir: 233, 9,715,512
MobileNetV4RenderB.lean:~48 (module) | process | "Until 2026-09-24 the three strided rows were PRE-strided … five departures … (planning/mnv4_timm_parity.md)" | — | "Note: the stride position, BN-only pre-DW, relu fused stage, 1×1 head conv, symmetric-pad stem follow timm's `mobilenetv4_conv_medium`; parameter counts alone cannot distinguish these" | head conv `Kernel4 1280 960 1 1` (:454); fused stage relu and stem pad per mnv4FwdChainB docstring/comments
MobileNetV4RenderB.lean:~479 (`mnv4FwdChainB`) | stale+process | "224² → 10 classes"; "as of 2026-08-14 (historical/RESULTS.md's 84.58% …)"; "Block dispatch is forced by the table and checked by the types"; "⚠ This paragraph listed Conv-S's fourteen families until 2026-09-07" | signature takes `nClasses`; module says families are `if`s the compiler cannot check | "224² → `nClasses`"; "Block dispatch follows the table … stride/skip split checked by the types; family (depthwise positions) is an `if` the types do not check"; dates dropped | read signature; module text on `if`s
EfficientNetRenderPC.lean:3-30 (module) | stale+overclaim | "MNV2/r34 get away with a batch-1 `den` because their per-channel BN reduces `[2,3]`"; "retired `ResNet34RenderPC.lean`"; "for every block form B0 has"; "3-axiom clean" | MobileNetV2RenderB / ResNet34RenderB train renders use `bnBatchF` at N := B; this file proves 5 graphs (stem, noExp, strided, resid, head); the stride-1 no-skip expand form is `mbExpGraphB_faithful` (EfficientNetFullB0.lean:158) | MNV2/R34 sentence dropped; the five theorems named; fifth block form pointed at `mbExpGraphB_faithful`; "3-axiom clean" → "All five theorems are in tests/AuditAxioms.lean's `#print axioms` list" | `grep -n "^theorem" EfficientNetRenderPC.lean` (5); each name grepped in tests/AuditAxioms.lean (1 hit each); `grep -rn "theorem mbExpGraphB_faithful"`; MobileNetV2RenderB:28-30, ResNet34RenderB:11-17 read
EfficientNetRenderPCEval.lean:5-8 (module) | stale (mild) | "the five block graphs" | five `*GraphBEval_faithful` (stem, noExp, strided, resid, head); `mbExpGraphBEval_faithful` in EfficientNetFullB0Eval.lean:79 | "five graphs … stem, MBConv1, strided MBConv6, residual MBConv6, head. The stride-1 no-skip expand block is `mbExpGraphBEval_faithful` in `EfficientNetFullB0Eval`" | `grep -n "theorem .*GraphBEval_faithful"` both files
ResNet34RenderB.lean:~241 (`resnet34FwdEvalFaithfulV`) | stale | "`resnet34_adam_train_step.mlir`, which is still a hand-written render in `TestResnet34Train.lean` (since retired) … the remaining §2a work" | the train step is `resnet34AdamTrainStepFaithfulB` in this file (writer at :1612), returning 72 batch stats | "eval partner of `resnet34AdamTrainStepFaithfulB` (this file), whose 72 returned batch statistics the driver EMAs into exactly these slots" | `grep -n "resnet34_adam_train_step.mlir\""` → :1612; 72 = `r34StatSigList` (36 BN sites × μ/var) and the AdamW docstring's "72 batch stats"; resnet34_fwd_eval.mlir has 183 args (awk)
ViTRenderB.lean:~262 (`vitFwdRenderB`) | stale | "Not written to `verified_mlir/` … The writer lands with the swap, not before." | writes vit_fwd (:995), vit_drop_fwd (:658), vitin_drop_fwd (:815), vitsin_drop_fwd (:852), vitsin_fwd (:859), vitbin_fwd (:953), vitin_fwd (:1162) | "It writes every committed ViT forward: [7 names]. `vit-fwd-b-tie` re-renders `@vit_fwd` with this function and compares it with the committed bytes" (report's fix said the tie renders the per-example chain — wrong: tests/TestViTFwdBTie.lean:35 calls `vitFwdRenderB`) | `grep -n 'writeFile "verified_mlir/vit[a-z_]*fwd'`; `ls verified_mlir \| grep '^vit.*fwd'` (7 files, all matched); vit_fwd.mlir has 201 args (200 params + %x)
ViTRender.lean:3-18 (module) | stale | "(the §1 render) — FORWARD portion … the backward-cotangent chain … + the param-SGD tail … follow"; "den(graph) = vitForward"; comparison to LeanMlir/ViTRender.lean | file holds `vitFwdRenderV`, `vitBackAll`, `vitTrainStepRenderV`, `vitAdamTrainStepFaithful`, `vitParamSig`; writes only vit_train_step.mlir (:876); `vitForward` does not exist — `vitFwdGraphKMHV_faithful` concludes `vitForwardKV` for every depth `k` | new title + contents list + "writes one artifact … the step `ViTStepTie` is stated at; every other ViT artifact is written by `ViTRenderB`"; `vitForwardKV`; the legacy-emitter comparison dropped (that file is slice J, §2(f)) | `grep -n "^def \|writeFile" ViTRender.lean`; every `ls verified_mlir \| grep ^vit` file (40) matched a writeFile in ViTRenderB (39) or ViTRender (1); ViTDepthK.lean:349 statement read; ViTStepTie.lean:9 names vit_train_step.mlir
IRPrint.lean:~430 (`convBackModule`) | overclaim | "Denotes the proven conv input-VJP (`conv_back_bridge_1to2`)" | module is shape-generic (B ic oc H Wd kH kW); `conv_back_bridge_1to2` is at `Kernel4 2 1 3 3`, 4×4; general `IR.convBackDenote_eq_input_grad_formula` needs `hkH`/`hkW` (odd kernel) | "For odd kH, kW its op sequence is the conv input-VJP formula (`IR.convBackDenote_eq_input_grad_formula`, which takes the two oddness hypotheses; `conv_back_bridge_1to2` is its instance at `Kernel4 2 1 3 3` on a 4×4 map). The printed text is trusted." | read IR.lean:270-300 statements
IRPrint.lean:1-24 (module) | stale+process | "# Phase 0 of planning/archive/verified_codegen.md"; "(Phase 1: feed the output to IREE.)"; "up to the printer, IREE, and float" | 1,883-line file of printers per layer family; nothing imports it; `#eval`s write /tmp/*.mlir (:1814-1853); `mlp_whole_bridge` has `h_smooth_0`/`h_smooth_1`, `relu_at_bridge` has `h_smooth` | new title/purpose paragraph listing the families (from the file's `-- §` section banners), trusted text, /tmp `#eval`s; bridge table kept with "needs p ≠ 0"; mlp bridge stated "at a point where both hidden pre-activations are nonzero everywhere"; "IREE" → "the compiler" | `grep -n "§ " IRPrint.lean` (section list); `grep -rln "import LeanMlir.Proofs.Codegen.IRPrint"` (none); IR.lean:611,616,648 statements; `grep -n "^inductive"` → `Hlo`, `HloF`
StableHLO.lean:~1839 (comment in `den`), StableHLOPretty.lean:232 (comment in `Raw`) | stale (comment) | "`gradSumSqF` … `addScalarF` … `gradClipFacF` …"; "`gradClipFacF` keeps only its two literal strings; `clipScaleF`/`addScalarF` are BINARY" | none of the three exist; ops are `gradSumSqAccF : SHlo 1 → SHlo n → SHlo 1`, `clipScaleF` (c ε, `SHlo 1 → SHlo n → SHlo n`), `lambScaleF` binary | "`gradSumSqAccF acc g` adds ∑g² to the running total; `clipScaleF s g` forms min(1, c/(√s+ε)) from `s` and scales `g`"; Pretty: "`clipScaleF` keeps its two literal strings; `gradSumSqAccF`/`clipScaleF`/`lambScaleF` are BINARY" | `grep -n "| gradSumSqAccF\|| clipScaleF\|| lambScaleF"` StableHLO.lean:1435-1455, StableHLOPretty.lean:234-237; `den` arms read
StableHLO.lean:1269 (comment) | stale (comment) | "see the `_sgd_eq` theorems" | theorems are `*Sgd_eq_grad` (e.g. `weightSgd_eq_grad` :3209) | "see the `*Sgd_eq_grad` theorems" | `grep -n "Sgd_eq_grad"`
StableHLO.lean:~1788 (`den`) | stale | "over `ℝ`, per-example, in primitive terms" | arms include `batchOp` (`batchMap N (denOp op) (den e)`, :2266), `bnBatchF`/`*GradB` batch-coupled arms, and `allReduceMeanF R … g => (1/R)·∑ r, den (g r)` (:1986) | "per-example constructors denote one example; `batchOp` and `*B`/`*GradB`/`bnBatchF` at the batched index N·n (batch-statistic and gradient-sum arms couple the batch); `allReduceMeanF` the mean over its R replica children" | `grep -n "| _, .allReduceMeanF\|| _, .batchOp"`; constructor list at :960, :1234
StableHLO.lean:~3431 (`adamWParamF_faithful`) | overclaim | "The emitted 26-op block denotes exactly `Proofs.adamWParam` … the theorem that moves the optimizer from a trusted hand-written emitter (`ViTRender.emitAdamV` …) into the proven kit" | `den (.adamWParamF …) = adamWParam … (den e) := rfl`; the 26 lines are a hand-written `emitTok` arm | "The node's denotation is `Proofs.adamWParam` of the child's gradient (by definition of `den`), so ties stated over `den` can use it; the 26-op text `emitTok` prints for it is trusted, like every op's." | counted the `s!"    {…} = stablehlo.` lines in the `.adamWParamF` arm of `emitTok` (StableHLOPretty.lean:2122-2150): 26
ResNet50RenderB.lean:3-50 (module) | stale+process | "R50 phase 2 (planning/… §3.2)"; "## ⚠⚠ There is no incumbent … §3.2 … must not be implied"; no mention of the Proofs tier | Proofs-tier files exist: ResNet50FullB (graphs `r50StemGraphB`/`r50IdGraphB`/`r50ProjGraphB`/`r50DownGraphB`), ResNet50StepTieB, ResNet50SyncStepTieB; FwdGraphTextTies.lean:103-121 `#guard`s the R50 stem/block/head emitters against those graphs at batch 2 (report said the tier files import this render — they do not; only FwdGraphTextTies imports it) | new "Proofs tier, and no hand-written render to tie against" section naming the three files and the `#guard` text ties; kept the no-incumbent caveat as plain scope; §/phase refs and ⚠ markers dropped | `grep -rln "import LeanMlir.Proofs.Codegen.ResNet50RenderB"` (FwdGraphTextTies only); `grep -rn "def r50IdGraphB\|def r50StemGraphB\|def r50DownGraphB"` ResNet50FullB.lean:301,367,400; module docstrings of the three files read
ResNet50RenderB.lean:~505 (`R50FwdRecB`) | stale+process | "keeps 89.86% (`runs/r50_imagenette_adam_80ep.log`)"; "planning/archive/mnv4_verified.md §3d(b)"; ⭐⭐/⚠ | log does not exist (`ls runs \| grep -i r50` has no such file) | number/log/§ ref dropped; "it is the artifact that scores, so its bytes stay as they are" | ls runs/
EfficientNetRender.lean:~600 (`eBackNoExp`) | stale (draft text) | "8 params (Wd bd gd btd zW1 zb1 zW2 zb2 ... wait, 4 dw + 4 SE + 4 proj = 12)" | `names` = dW, [db], dg, dbt, SE×4, pW, [pb], pg, pbt; bias slots only at `convBias := true` (`biasSlot`) | "12 params at `convBias := true`: depthwise W b γ β, SE W₁ b₁ W₂ b₂, project W b γ β; the two conv biases drop out at `convBias := false` (10)" | read `names :=` (:628) and `eSigNoExp` (:653-658); `biasSlot` def StableHLOPretty.lean:4524
EfficientNetRender.lean:~1062 (`efficientnetTrainStepFaithfulV`) | stale | "40.6% → 87.81% over 80 epochs, matching README's 87.58%" | README/RESULTS have no 87.58 (grep); the log's 80 val lines: epoch 1 40.625%, epoch 80 87.653689%, max 87.807377% at epoch 79 | "val accuracy 40.63% after epoch 1, 87.65% after epoch 80, peak 87.81% at epoch 79" | awk over runs/efficientnet_verified_crop_gpu1.log (/val_acc/ lines; max); `grep -rn "87.58" README.md RESULTS.md` (none)
CnnRender.lean:~636 (`cifar8AdamTrainStepFaithfulB`) | stale | "`StableHLO.lean` l.2016 vs l.2200 … l.2990 records why it is free" | the arms are `den`'s `.convBack` (:2082) and `.convBackBatched` (:2273); "conv is linear, so this is a global VJP" is `convBack_faithful`'s docstring (:3083) | cites `den`'s `.convBack`/`.convBackBatched` arms and `convBack_faithful` by name, no line numbers | `grep -n "| _, .convBack \|| _, .convBackBatched\|theorem convBack_faithful"`
RenderKit.lean:~128 (`r34WdDecays`) | stale+process | "⚠⚠ This is `a3_paper_fidelity.md` §2.1, open since the A3 run. The live A3 artifact has ZERO `%wdz` … the 77.43% run decayed BN γ/β" | one-line predicate `ds.length ≥ 2`; `r34WdName` binds excluded params to `%wdz` | docstring: rule + `r34WdName` + identical-to-`cnxWdDecays` note; the BN-decay rationale and the A3-run history moved to a `--` comment below the docstring (without the "open" status) | read def + `r34WdName` (:143); `resnet50ImagenetConfigRSBFaithful` exists in jax/MainResnet50Imagenet.lean (comment only, not backticked-resolved in a docstring)
FwdGraphTextTies.lean:13-38 (module) | overclaim (mild)+process | "for every block kind … print the same bytes … Covered: … every block kind, stem and head"; "T2" tier labels | 25 `#guard`s, each at batch 2 on one concrete shape (e.g. `idFwdB 2 64 56 …`); MNv4 checks all 21 table rows (`mnv4Blocks.length == 21 && mnv4Blocks.all …`) | added "each checked by `#guard` at batch 2 on one concrete shape (for MobileNetV4, every row of the 21-row table)"; "T2 theorem/graph" → "graph-faithfulness theorem"/"typed graph" (also in the `mnv4RowGraphText` docstring) | `grep -c "^#guard"` = 25; read each guard's arguments; :209 guard
StableHLOLex.lean:3-71 (module) | process+stale | "# R4 — the syntactic half (4b)"; "Honest scope … (corrects the planning doc)"; "~90 `Tok` constructors"; "Status: deliberate STOP … (decided 2026-06-27)"; "validated by `iree-compile`" | file holds `parseNat` + `parseNat_toString`; `Tok` has 98 constructors; `emitTok (.operand nm _)` = `pure ("", nm :: st)` (StableHLOPretty:1370) | new docstring: what the lemma is and why it is at `List Char`; "No lexer is built"; trusted text/token correspondence; drift guard + parse gate; the operand-re-synthesis finding kept as a "Note" (technical, not history); status/date/planning refs and the per-op volume estimate dropped (no Tok count written) | awk count of `inductive Tok` constructors = 98; read :1370
ResNet34RenderB.lean:~1310 (`resnet34AdamTrainStepFaithfulB`) | stale (found in sweep) | "407 inputs … and 408 outputs"; "This docstring said 515 inputs, 146 θ until 2026-09-06 …"; "the per-example render and both forwards" | resnet34_adam_train_step.mlir: 407 args, 405 result types (110×3 + 3 + 72); per-example R34 render retired | "405 outputs"; "146 θ at `convBias := true`"; "the same single source both forwards use"; history dropped | python count over the artifact's func.func line
ResNet34RenderB.lean:~410 (`R34Opt`) | stale (found in sweep) | "the 146 un-fused parameter gradients" | shipped `convBias := false` = 110 (`r34SigList` docstring) | "the un-fused parameter gradients (110 at `convBias := false`)" | r34SigList docstring arithmetic 3 + 13×6 + 3×9 + 2 = 110
ResNet34RenderB.lean:~465 (`accumScalarConsts`) | stale (found in sweep) | "These eight lines are the whole accumulate/apply mechanism" + planning §1.1 history | the def emits 11 `stablehlo.` lines | "These eleven lines …; one definition keeps the two optimizers from carrying two copies" | counted the string lines in the def body (11)
StableHLO.lean:~419 (`maxPoolBack` constructor comment) | deferred §5 | "Conditional (no-ties) like the ReLU kink" | MaxPool distinct-entries condition | NOT CHANGED — MaxPool "unique argmax / distinct entries" wording is deferred to the live-cell predicate row (D1) | —
ViTRender.lean:~579 (`vitTrainStepRenderV` return type `ty [10]`) | deferred §5 (correctness lead) | returns `ty [10]` for the classifier bias while args use `ty [nClasses]` | — | NOT CHANGED — rides with the ViT nCls row (D1); `vitFwdRenderV`'s "`[BS,10]` logits" also left | —
ConvNeXtRender.lean / ConvNeXtRenderB.lean "certified loss-descent step" (`ConvNeXtFold`) | deferred §5 | "denotes the certified loss-descent step" | ConvNeXt has no `*CotIn_eq_vjp` | NOT REWORDED (kept the existing clause in the rewritten module docstring) — ConvNeXt `*CotIn_eq_vjp` row (D1) | —
CnnRender.lean `cnnTrainStepFaithfulV` / `cifarTrainStepFaithfulV` / `cifar8TrainStepFaithfulV` "`CnnFold`/`CifarFold`/`Cifar8StepTie` proves each output's `den` = the certified loss-descent step" | deferred §5 | small-CNN capstones omit dense-head conjuncts | — | NOT REWORDED — small-CNN dense-head row (D1) | —
EfficientNetRender.lean:~1062 (`efficientnetTrainStepFaithfulV`) | process (follow-up to the stale row above) | run-log citation inside the docstring | — | docstring keeps "a tuned value, not a slip"; the log path and the re-counted numbers (40.63 / 87.65 / 87.81) moved to a `--` comment under the docstring | awk over the log (see row above)
StableHLO.lean:~3958 (`biasName`) | process | "§2l step B"; "MEASURED, and it corrects §2l's stated reason … In the 80-epoch run all 8,512 biases drifted to |θ|max 0.041 … rel 1e-6, against 0.79" | one-line def | docstring keeps the reason (f32 residue; forward independent of the biases); the run measurements moved verbatim to a `--` comment below | read in place; numbers copied, not re-measured (not re-countable from the source)

## Process narrative (§4), docstrings only — one row per file

Count = docstring lines matching `[⭐⛔⚠▶✅] | YYYY-MM-DD | §N | phase N | leg N | planning/ | handoff | T1–T6 | 4a–4d | runs/ | until 20.. | retired | used to | corrected | gate 1`, HEAD → now (scratchpad count.py; RenderKit's remaining hit is "warmup-corrected", ResNet34RenderB's none after the last edit). Glyphs removed mechanically (strip.py, docstring lines only); every date/§/planning/leg/commit-hash reference edited by hand, keeping any real warning as a plain sentence; code outside comments verified unchanged (code.py: all 25 files identical after stripping comments/docstrings).
CnnArtifacts.lean | process | 2 → 0 | "§4.3 Lever 3" → "the book's Lever 3"; "the 27 bf16 ops" → "the bf16 ops" (27 not re-countable without a census) | — | —
CnnRender.lean | process | 19 → 0 | handoff §2i / planning refs / "the retired emitter" / "incumbent (gate 1)" dropped; `cifar8AdamTrainStepFaithfulB` carve-out list now names the `%bc` passthroughs as its V peer does | — | —
ConvNeXtRender.lean | process | 36 → 0 | module rewritten (see row above); `/-!` channel-LN block restated as design, "§2m/§2n" history, "restored 2026-08-30", "rung E", "§2k/§2l-A" dropped; `cnxAdamVariant` "cBS … private constant" (stale: cBS is a parameter) dropped; the `/-!` "scalar-LN γ/β are rank 0" sentence (stale: `blkParams` gives `[c]`) dropped | — | read `blkParams`, `cnxAdamVariant` signature
ConvNeXtRenderB.lean | process+stale | 37 → 0 | module rewritten (writes every ConvNeXt artifact but convnext_train_step; "31 forms"/"78 lines" counts dropped); `cnxInBS` history and "thirteen #evals"/"nine artifacts" counts dropped; `convNextFwdRenderB` "180-parameter" → "182 parameters at ConvNeXt-T" | — | convnext_fwd.mlir 183 args
EfficientNetRender.lean | process | 19 → 0 | "## The batched index" `/-!` block restated in present tense; §2a/§2m/§0.10 refs dropped; "until 2026-09-24"; "enet's +21,008 gap closed on the first attempt" dropped | — | —
EfficientNetRenderPC(.Eval).lean, FwdGraphTextTies.lean, IRPrint.lean, StableHLOLex.lean, StableHLOParse.lean | process | 1+2, 9, 2, 4, 2 → 0 | covered by the rows above (T2 labels, phase labels, dates) plus glyphs | — | —
LambTriple.lean | process | 13 → 0 | "the audit's … was wrong" paragraph → one sentence naming `lambDirF_faithful`/`lambScaleF_faithful`; "§3.5 needs before ResNet-50's T3" → the LAMB artifacts it serves; "(D2)", "4d piece 2 (2026-09-07)", "(moved 2026-09-08)" dropped; `lamb_triple_faithful_excluded` last sentence restated as what the theorem adds over `lambTrust_zero_weight` | — | `grep` Lamb.lean:127,143,152; DataParallelNode.lean:104
MobileNetV2RenderB.lean | process | 27 → 0 | module: retirement history → one paragraph on what the file writes; Proofs-tier list without T/§/dates; "the tie against it must be numeric" (hand-written artifact retired) dropped; `mnv2FwdChainB`, `mobilenetv2FwdFaithfulB` history dropped | — | mobilenetv2_fwd.mlir 159 args
MobileNetV4RenderB.lean | process | 30 → 0 | see MNv4 rows above; "Until 2026-09-24 …" facts restated as design notes (stem padding, head order, fused-stage activation); "tier-2 recipe (planning/…)" and "since 90e4af7e" dropped | — | mnv4_adam_train_step.mlir 858 in / 856 out (python count) matches the docstring
RenderKit.lean | process | 14 → 1 (the 1 is "warmup-corrected", a false positive) | planning refs; ema warmup "held 12.8% … scored 0.00%" (a run result) → plain statement; "248 times" history dropped | — | —
ResNet34RenderB.lean | process+stale | 73 → 0 | module rewritten (sole writer; no retired-renderer narrative); `R34Opt`/`optOne`/`optAllParams`/`optWd*`/`wdVariantMark`/stem/forward docstrings: planning §/dates/leg refs dropped; all-reduce paragraph restated (sync-BN check at 2×32 vs 1×64, per lakefile `resnet34-syncbn-check` docstring); `den_allReduceMeanF` located in StableHLO.lean:2455 | — | lakefile.lean:1588
ResNet50RenderB.lean | process | 26 → 0 | see R50 rows above | — | —
StableHLO.lean | process | 48 → 0 | `BatchableOp` "An earlier note here said …" → present tense; `BnMode` "resnet34_fwd did until 2026-07-27, rel 1.13" dropped; §2a/§2b-bis/§2f/§4/planning refs dropped; `biasName` see row above | — | —
StableHLOPretty.lean | process+stale | 30 → 0 | `tyBf16` "Only the round trip uses it today; when a bf16-operand dot_general lands (rung 2+)" (stale: used by `dotInBf16`, the bf16 convs; :1376, :1655) → current use; `emitTok` "proven-faithful token stream" → "`toToks (skel g)`, whose encoding round-trips"; `alphaOverK` incident narrative (date, run loss) → the design rule; `prettyAllReduceMean` "(4d piece 2, 2026-09-07), measured byte-identical" dropped | — | `grep -n tyBf16` (10 uses)
SyncBnSites.lean | process | 5 → 0 | "Lifted out … (2026-09-21, planning §3.3)" → the five renderers that use it (`grep -rln Codegen.SyncBnSites` + R50 via R34) | — | —
ViTRender.lean | process+stale | 19 → 0 | module rewritten (row above); "as of 2026-07-31 … ~28 other places"; "positionally identical to the hand-written render" → carve-out list (report-only `%loss`, `%bc` passthroughs); `vitForward` → `vitForwardKV` in `vitFwdRenderV` | — | vit_adam_train_step 605/603, vit_ema_train_step 807/805 (python count) match the docstring
ViTRenderB.lean | process+stale | 18 → 0 | module rewritten: title "the forward" → "forward, backward and AdamW renders"; "4c leg 4", "NINETEEN artifacts", "all 47" dropped; §0.9 banner ref restated | — | —
