# Ledger — slice D (Proofs/Architectures, Proofs/Training)

Branch `api-docs-pass`. Docstrings only; no statement, name, proof or attribute changed.
Line numbers are at the time of the edit.

| file:line | kind | old claim | what the statement says | new text | how checked |
|---|---|---|---|---|---|
| Architectures/Residual.lean:52 `residualHasVJP` | overclaim | "the gradient floor is `dy` itself, so it can never get smaller than the loss gradient" | `backward x dy = f.back x dy + dy` via `biPathHasVJP`; no bound on `dx` | "The skip passes `dy` to the input unchanged, so the gradient does not have to go through `f`'s Jacobian … (the two terms can still cancel; no lower bound on `dx` is claimed)" | read def + `biPathHasVJP` |
| Architectures/Residual.lean:153 (module note) | stale | "(`cnnHasVJPAt`, future) must chain" | `cnnHasVJPAt` exists (CNN.lean:1582), uses `resblockHasVJPAt` → `residualHasVJPAt` | "(`cnnHasVJPAt`, in CNN.lean) chains through" | `grep -n "cnnHasVJPAt\|residualHasVJPAt" CNN.lean` |
| Architectures/CNN.lean:715 `conv2dBiasGradFormula` | stale | "(documented, numerically verified, expected to equal `conv2dBiasGrad` up to fp precision)" | ℝ def `fun o => Σ y Σ x dy o y x`; no theorem mentions it (grep: only the def and prose) | "This is the sum `conv2dBiasGradHasVJP`'s backward is defined as, on `Tensor3` … No theorem in this file states the equation" | `grep -rn conv2dBiasGradFormula LeanMlir tests`; read `conv2dBiasGradHasVJP.backward` |
| Architectures/CNN.lean:686 `conv2dBiasGradHasVJP` | stale/process | "Now that `conv2d` is a real def"; "(matches `conv2dBiasGradFormula`)" | backward is `Σ hi Σ wi dy (flat (o,hi,wi))` | dropped history; "the same sum as `conv2dBiasGradFormula`, written on the flattened `dy`" | read def |
| Architectures/CNN.lean:741 `maxPool2` | process | "No longer an axiom — replaced with the explicit four-way max" | concrete def | "written as an explicit four-way max" | read def |
| Architectures/CNN.lean:1158 `maxPool2HasVJPAt3` | stale | "(future) `cnnHasVJPAt3`" | `cnnHasVJPAt` (CNN.lean:1582) uses it via `maxPoolFlatHasVJPAt` (:1425) | "`cnnHasVJPAt` chains through both (this one via `maxPoolFlatHasVJPAt`)" | `grep -n "maxPool2HasVJPAt3\|maxPoolFlatHasVJPAt\|reluHasVJPAt" CNN.lean` |
| Architectures/CNN.lean:1192 (flatten note) | process | "pdiv3 derivation in Phase 5" | — | "the `pdiv3` derivation there" | `grep -n "def pdiv3" Foundation/Tensor.lean` (:980) |
| Architectures/CNN.lean:1200 walkthrough | stale | "a uniform `HasVJP`-style composition would need a type family … we instead trace" | composed witness exists: `mnistCnnNoBnHasVJPAt` (Nets/Small/MnistCNN.lean:100) over the same conv-relu-conv-relu-pool-dense×3 chain | "The composed whole-network witness for this chain is `mnistCnnNoBnHasVJPAt` …; this trace is the per-layer reading" | read `mnistCnnNoBnForward` (MnistCNN.lean:73) against the trace; `ls historical/mlir_poc/hand_cnn_train_step.mlir` |
| Architectures/CNN.lean:1251 summary | stale/process | "black-box forward"; "argmax-routing subgradient convention"; "Phase 7"/"Phase 9"; formulas "numerically verified to equal the VJP's backward" | both forwards are defs; `maxPool2HasVJP3` backward is canonical pdiv3 (`correct := rfl`); `conv2dHasVJP3.backward := conv2dInputGradFormula W dy` | "concrete definitions"; "canonical `pdiv3`-derived witness (zero at argmax ties); `maxPool2HasVJPAt3` is the argmax-routing form"; formulas: input one is the backward by definition, bias one has no linking theorem | read `conv2dHasVJP3` (:226), `maxPool2HasVJP3` (:781) |
| Architectures/CNN.lean:1314 capstone note | process | "which was previously only referenced in codegen" | — | dropped | — |
| Architectures/CNN.lean:1439 `max_close` | process | "(planning §1b-A)" | — | dropped | — |
| Architectures/CNN.lean:887 `MaxPool2Smooth`, :1314 `h_mp` note | overclaim | "natural domain on which `maxPool2` is differentiable" | — | deferred §5 (MaxPool live-cell row) | — |
| Architectures/ConvGrad.lean:5 (module) | stale | "`conv → relu → maxpool → conv → relu → maxpool → dense → … → dense`" | `cnnTrainStepFaithfulV` (Codegen/CnnRender.lean:32) and `mnistCnnNoBnForward`: one pool | "`conv → relu → conv → relu → maxpool → dense → relu → dense → relu → dense`" | read the `pretty` calls in `cnnTrainStepFaithfulV` |
| Architectures/ConvGrad.lean:7,11,21 (module) | process | "M2's `weight_grad_bridge`", "As in M2" | names are `IR.weight_grad_bridge`/`IR.bias_grad_bridge` (Foundation/IR.lean:705,717) | "`IR.weight_grad_bridge`/`IR.bias_grad_bridge`", "As for the MLP" | grep `theorem weight_grad_bridge` |
| Architectures/ConvGrad.lean:17 (module), :27 `conv_weight_grad_bridge` | overclaim | "the emitted conv kernel gradient equals the certified Jacobian" | `(conv2dWeightGradHasVJP b x).backward v c idx = Σ pdiv … * c j`, the `.correct` field | "the backward of `conv2dWeightGradHasVJP` … equals"; module adds "no rendered text appears in them; the link from a rendered conv gradient op is that op's `den` theorem (see `CnnFold`)" | read both theorems; `grep -n cW1_den Nets/Small/CnnFold.lean` |
| Architectures/ConvGrad.lean:67,81 `cnn_render_convW_certified` / `cnn_render_convb_certified` | overclaim | "`Wⁿ = W − lr·(transpose-trick kernel grad)` denotes … certified" | `v idx − lr * backward = v idx − lr * Σ pdiv…`, by `rw [conv_*_grad_bridge]` | "SGD form of `conv_weight_grad_bridge` … A rewrite by the bridge; no rendered text appears in the statement" | read proofs |
| Architectures/ConvGrad.lean:55 section `--` comment | overclaim | "each rendered conv SGD output equals …"; cites `mlp_render_W*_certified` (no such theorem) | — | not edited (a `--` comment, outside the docstring-only brief); lead for the lead | `grep -rn "mlp_render_" LeanMlir` → only this comment |
| Architectures/LayerNorm.lean:13 (module) | missing | "LayerNorm & GELU … Two quick chapters" (nothing about the rest) | file also defines Swish, layer scale, vector LN, γ/β bridges | added "Also in this file" list naming `swish`, `swishScalarDeriv_eq`, `swishHasVJP`, `layerScale`, `layerScaleHasVJP`, `layerNormVec`, `layerNormVecHasVJP`, `layerNormVecPerTokenHasVJPMat`, `vecLNGradGamma/Beta`, the two bridges and two SGD forms; importer list | each name grepped in the file; importers: `grep -rln "import LeanMlir.Proofs.Architectures.LayerNorm" LeanMlir tests` (10 in LeanMlir) |
| Architectures/LayerNorm.lean:57 `layerNormForward` | stale | "in LN, `gamma` and `beta` are per-feature … so they're full vectors" | `layerNormForward (n) (ε) (γ β : ℝ) x := bnForward n ε γ β x` — scalars | "Here `γ` and `β` are scalars, exactly as in `bnForward`. The per-feature `[D]` affine … is `layerNormVec` below" | read def and `layerNormVec` |
| Architectures/LayerNorm.lean:83 `layerNormHasVJP` | proof-narrative | "If you built `layerNormHasVJP` you'd discover … Rather than restate, we just reuse:" | `show HasVJP (bnForward …); exact bnHasVJP …` | "`layerNormForward` is `bnForward` by definition, so `layerNormHasVJP` is `bnHasVJP`" | read body |
| Architectures/LayerNorm.lean:94 "Why this isn't a new chapter" | overclaim | "InstanceNorm (which is what the ResNet code actually uses) … All four normalization variants share one `HasVJP` instance" | only `bnHasVJP`/`layerNormHasVJP` exist; no RMSNorm/GroupNorm/InstanceNorm def in LeanMlir | "BN and LN share one `HasVJP` … RMSNorm drops the mean centering and is a different function. None of those three is formalized here." | `grep -rln "RMSNorm\|GroupNorm\|InstanceNorm" LeanMlir --include=*.lean` → only a Spec.lean comment |
| Architectures/LayerNorm.lean:125,137 `geluScalar`, `geluScalarDeriv` | stale/process | "No longer an axiom" ×2; "this is `Φ(x) + x·φ(x)` for the exact form, or …" | `geluScalarDeriv x := deriv geluScalar x`, `geluScalar` = tanh approx. | dropped history; "derivative of that approximation; its closed form is `geluScalarDeriv_eq`" | read defs |
| Architectures/LayerNorm.lean:211 `pdiv_gelu` | process | "proved (planning/archive/VJP.md follow-up E)" | — | dropped | — |
| Architectures/LayerNorm.lean:238 taxonomy | stale | GELU row "`Phi(x_i) + x_i * phi(x_i)`"; "we show the template once (ReLU …) and assert that GELU follows" | `geluHasVJP` is proved via `pdiv_gelu`/`pdiv_elementwise`; `reluHasVJP` is `HasVJP.canonical` | row "GELU (tanh approx.) \| `geluScalarDeriv_eq`"; "This file instantiates it as `geluHasVJP` and `swishHasVJP`; the other rows are not given separate `HasVJP` instances here" | read `geluHasVJP`, `swishHasVJP`, MLP.lean:222 |
| Architectures/LayerNorm.lean:547 `vit_render_veclngamma_certified` | overclaim | "Covers all five LN sites of the vector-LN representative" | single-site identity, generic `N D`, `rw [vit_veclnGamma_grad_bridge]` | "a rewrite by `vit_veclnGamma_grad_bridge`, stated at one LN site with generic `N`, `D`" | read statement |
| Architectures/Attention.lean:2057 `vitFull`, :2095 `vitFullHasVJP`, :2194 `vitFullHasVJP_correct` | overclaim | "full ViT forward"; "the grand finale"; "the full ViT's backward" | `vitFull` runs `vitBody kBlocks …` = `transformerTower` (`Nat.rec` over ONE tuple `Wq … bfc2`) with `layerNormForward … (γ β : ℝ)` at every LN | "a weight-tied ViT forward …: all `kBlocks` transformer blocks share one parameter tuple, and every LayerNorm (`γ1 β1`, `γ2 β2`, final `γF βF`) is scalar-affine"; `_correct` says "the backward of `vitFull` (weight-tied blocks, scalar-affine LayerNorms)" | read `vitFull`, `vitBody`, `transformerTower` signatures |
| Architectures/Attention.lean:1505 stacking note, :1558 tower, :1640 `vitBodyHasVJPMat`, :1479 block | overclaim | "generalizes trivially to per-block parameters … mechanical once the single-shared-param case is proved"; "a depth-k ViT backbone has a correct VJP" | no per-block tower exists in the file | "Per-block parameters are not formalized in this file"; body: "A depth-k weight-tied backbone (one parameter tuple for every block, scalar-affine LayerNorms)"; block: "Both LayerNorms are scalar-affine" | `grep -n "def transformerTower\|Nat.rec" Attention.lean` |
| Architectures/Attention.lean:665,721,986,1691 | stale | `sdpa_back_{Q,K,V}_correct`, `sdpa_back_*` | declarations are `sdpaBackQ/K/V`, `sdpaBackQ_correct` (:531), `sdpaBackK_correct` (:615), `sdpaBackV_correct` (:648) | real names | `grep -n "sdpa_back\|theorem sdpaBack" Attention.lean` (0 `sdpa_back` left) |
| Architectures/Attention.lean:406 "The backward, concretely" | process/stale | "Earlier drafts … `axiom sdpaHasVJP` … vacuous"; "§Q-correctness below" (no such section) | three `*_correct` theorems, each a `vjpMatComp` chain (Q: 4 steps, K: 5) | "This section gives: … `sdpaBackQ_correct` … each proved compositionally as a `vjpMatComp` chain (four steps for Q)" | read `sdpaQChain`, `sdpaKChain` |
| Architectures/Attention.lean:1588 ViT boundary | stale | "We mark them as future work; closing this would require a unified rank-polymorphic VJP framework" | `patchEmbedFlatHasVJP` (:1921), `classifierFlatHasVJP`, `HasVJPMat.toHasVJP` (Tensor.lean:730) used in `vitFullHasVJP` | "They are bridged below (§ Bridging ranks): … `HasVJPMat.toHasVJP` … `vitFullHasVJP`" | grep defs |
| Architectures/Attention.lean:1679 "What we've proved" | stale | "Softmax cross-entropy loss gradient (`MLP.lean`)"; "Standalone softmax VJP (this file)"; "`sdpa_back_{Q,K,V}`"; "formerly axioms"; "these ten things" | `softmaxCE_grad` Softmax.lean:175, `softmaxHasVJP` Softmax.lean:140; 3 rules + 5 tricks = 8 | Softmax.lean citations with names; real sdpa names; added bullet for `mhsaHasVJPMat`, `transformerBlockHasVJPMat`, `transformerTowerHasVJPMat`, `vitBodyHasVJPMat`, `vitFullHasVJP`; "these eight things" | grep defs; counted the two lists in the docstring (3 rules, 5 tricks) |
| Architectures/Attention.lean:1709 taxonomy trick 4 | overclaim | "(softmax, BN, LN, IN, GN)" | IN/GN not formalized | "(softmax, BN, LN)" | see LayerNorm row |
| Architectures/Attention.lean:36 (module) | process | "(previously we only had it bundled inside CE loss)" | — | "(`softmaxHasVJP`, separate from the CE-loss gradient)"; added item 6 "Bridging ranks … `vitFullHasVJP`" to the file map | — |
| Architectures/Attention.lean:1139 `mhsaComposedHasVJPMat` | process | "**Why the split (kernel-cost lesson, 2026-07):** …" | — | docstring keeps "so `(mhsaHasVJPMat …).backward` reduces in one projection"; the kernel-cost hazard moved verbatim (minus date) to a `--` comment right below the docstring | — |
| Architectures/Attention.lean (process, docstrings) | process | 17 sites: "Phase 1/3/6/8/10", "Apr 2026", "2026-07", "(was an axiom)", "no longer axiom", "former axioms", "Earlier …", "planning/archive/mhsa.md" | — | removed; kept the mathematical content of each sentence | `pn.py` scan before/after (19 → 0 process hits; remaining hits are "backward pass", "§ Bridging ranks") |
| Architectures/SE.lean:50 (module) | stale | "The gate is left abstract …; we sketch the concrete gate from MobileNetV3 in a final commentary section" | file defines `sigmoidHasVJP` (:209), `broadcastFlatHasVJP` (:238), `seGate` (:265), `seGateHasVJP` (:277), `seBlockFull`, `seBlockFullHasVJP` | lists those declarations | `grep -n "^noncomputable def\|^theorem" SE.lean` |
| Architectures/SE.lean:111 "What's actually inside `gate`" | stale | "you'd need to add `globalAvgPoolHasVJP` … and `broadcastHasVJP` … That's a few hours of mechanical work"; "dense and sigmoid VJPs are already in `MLP.lean`" | `seGateHasVJP` composes `globalAvgPoolFlatHasVJP` (CNN.lean:1392), `denseHasVJP`, `swishHasVJP`, `sigmoidHasVJP` (SE.lean), `broadcastFlatHasVJP` | "`seGate` at the end of this file builds `gate` … `seGateHasVJP` assembles its VJP with `vjpComp` from …" | read `seGateHasVJP` body |
| Architectures/BatchNorm.lean:73 `bnMeanSq` | stale | "⭐ The quantity a SYNCHRONISED BatchNorm reduces across replicas — never the variance … `R` replicas exchange `μ` and `E[x²]`"; planning §2b | emitted sync forward exchanges `μ` then `σ²` via `bnBatchVarAtB` (Codegen/StableHLO.lean:1149–1164, Chan); `den` of `bnSyncF` feeds `bnSyncTensor4` `m2 := σ² + μ²` (StableHLO.lean:1170) | "`bnSyncTensor4` and the sync backward are stated at `μ` and a second moment `m2`; the emitted sync-BN forward exchanges `μ` and `σ²` (two rounds, Chan's parallel variance, `bnVar_shard_chan`), and its `den` supplies `m2 := σ² + μ²`" | read StableHLO.lean:1145–1175 |
| Architectures/BatchNorm.lean:102 `bnMeanSq_shard` | stale | "which is the whole reason sync-BN reduces `E[x²]` … ⛔ The variance has NO such lemma, and cannot" | `bnVar_shard_chan` (:138) is the variance's shard formula | "The variance does not shard this way … The variance's shard formula is `bnVar_shard_chan`" | read :138 statement |
| Architectures/BatchNorm.lean:114 `bnVar_eq_bnMeanSq_sub_sq` | process | "⭐ The identity sync-BN is built on" | equation `bnVar = bnMeanSq − μ²` | "the identity that lets the sync-BN definitions be stated at the second moment (`bnSyncXhat` reads `m2 − μ²`)" | read `bnSyncXhat` |
| Architectures/BatchNorm.lean:284 derivation | stale | "`∂σ²/∂xᵢ = (2/N)(xᵢ − μ)(1 − 1/N) ≈ (2/N)(xᵢ − μ)`" | `bnVarDeriv_basisVec`: `= 2 * (x i - bnMean n x) / n` exactly | "`(2/N)·(xᵢ − μ)` (exactly: the −1/N term multiplies Σₖ (xₖ − μ) = 0; `bnVarDeriv_basisVec`)" | read :592 |
| Architectures/BatchNorm.lean:359 `bnSyncGradInput` | stale | "which is the whole reason one collective per direction suffices"; "as `μ` and `E[x²]` do in the forward" | forward uses two all-reduce rounds (StableHLO.lean:1149); args are `μ m2 mdy mdyx` | "Nothing here needs a sum."; "as `μ` does in the forward"; names the four arguments | read signature + StableHLO comment |
| Architectures/BatchNorm.lean:375 `bnSyncGradInput_at_own_stats` | process | "anchor for P2 … committed tiers … planning/global_bn_verified.md §2c" | — | "computes the same function as the per-batch `bnGradInput`" | — |
| Architectures/BatchNorm.lean:477,527,607,789 | process/stale | "used to be axiomatized directly. Now…", "follow-up C", "(was an axiom)", "`bnForward_lb`'s symmetric form" (`bnForward_lb` does not exist) | — | removed | `grep -rn "bnForward_lb" LeanMlir` → only this docstring |
| Architectures/BatchNorm.lean:401 parameter-gradient note | stale | "don't fit our `pdiv` / `HasVJP` framework cleanly … We state these as the *definitions*" | — | not edited: a plain `/- -/` block comment, not rendered by doc-gen4 (lead: `bnPerChannelGradGamma_correct`/`…Beta_correct`, PerChannelBNGrad.lean:90,112, prove the per-channel forms) | read the comment opener |
| Architectures/BatchNorm.lean (⭐/⛔ markers) | process | 8 markers in docstrings (:75,84,102,106,114,138,359,366,375) | — | removed; the warning content kept as plain sentences | `pn.py` scan 15 → 0 process hits |
| Architectures/PerChannelBN.lean:472 `bnSyncTensor4` | stale | "⚠ It takes the SECOND MOMENT … That is not a convenience: `E[x²]` … survives an `allReduceMeanF`, whereas the variance … does not" | def reads `μ m2`, forms `m2 − μ²`; emitted sync forward exchanges μ and σ² (Chan), `bnSyncF`'s `den` passes `σ² + μ²` (StableHLO.lean:1170–1172) | "stated at `μ` and the second moment `m2` … `SHlo.bnSyncF`'s `den` supplies `m2 := σ² + μ²`, so `m2 − μ²` is that `σ²` in ℝ" | read def + StableHLO comment/den (:1968) |
| Architectures/PerChannelBN.lean:300 `bnPerChannelTensor3` | process | "This is the op B9 wires into the ResNet-34 trainer (its `den` target)" | — | "This is what `SHlo.bnPerChannelF` denotes." | `grep -n "bnPerChannelF " Codegen/StableHLO.lean` (:602, comment says it denotes `bnPerChannelTensor3`) |
| Architectures/PerChannelBN.lean (process) | process | "(Chapter 5 Milestone B8)" title; ⭐⭐ ×6; `planning/global_bn_verified.md` §2b/§2c ×3; "the existing tier is already tied to" | — | removed; `R = 1` anchors now say which per-batch function they equal | `pn.py` 11 → 0 docstring hits; `--` section headers "B9 entry"/"B8b" left (comments) |
| Architectures/MaxPool3s2.lean:3–25 (module) | stale/process | "`resnet34Verified` has been pooling a different function"; "⚠⚠ WHICH 3×3 pool — … not what the JAX reference emits today"; "the JAX `max_pool2d` helper moves to match. ⚠ Until both land and are re-run, verified and JAX disagree" | `jax/Jax/Codegen.lean:668–685` `max_pool2d` pads `((p,p),(p,p))`, `p = (size-1)//2` | "`maxPool2` (CNN.lean) is 2×2 … this file is that op"; "XLA's `reduce_window(…, 'SAME')` would be a different function …"; "The JAX reference's `max_pool2d` uses the same symmetric padding `(p, p)`"; "the choice only matters for the 3×3 pools (the ResNet-34 and ResNet-50 ImageNet stems)" | read jax Codegen; `grep -rn ctConv_pool_smooth` → R34FullBSeal:340, R50FullBSeal:450 |
| Architectures/MaxPool3s2.lean:44 (module) | stale | "`maxPool2`'s `codegen_matches_canonical`" | name is `maxPool2_codegen_matches_canonical` (CNN.lean:1144) | real name | grep |
| Architectures/MaxPool3s2.lean:176 `maxPool3s2Smooth_of_injective` | stale | "No whole-net witness discharges it yet: the R34 Live/Seal witnesses pool 2×2 and use the `MnistCNN` lemma" | `BatchSeal.ctConv_pool_smooth` (Training/BatchSealKit.lean:795) applies it; used by R34 and R50 full-width seals | "Used by `BatchSeal.ctConv_pool_smooth` for the ResNet-34 and ResNet-50 full-width seals" | `grep -rn "maxPool3s2Smooth_of_injective\|ctConv_pool_smooth" LeanMlir` |
| Architectures/MaxPool3s2.lean (process) | process | 16 ⭐/⚠ markers in docstrings; "Fourth collapse of the same kind"; "STRICTLY SHORTER" | — | markers stripped (content unchanged); count dropped; "It is shorter than its 2×2 peer" | `strip.py` count 16; the whnf-timeout war story is a `--` comment, left |
| Architectures/Depthwise.lean:22 (module) | stale | "So we don't re-derive the VJPs from scratch. We state them as the 'channel-restricted' versions of `conv2dInputGrad` / `conv2dWeightGrad`" | `depthwiseHasVJP3` (:130), `depthwiseWeightGradHasVJP3` (:343), `depthwiseBiasGradHasVJP` (:434) each proved by `rw [hsplit, pdiv_of_affine]` | "proved directly with `pdiv_of_affine`, not derived from the regular-conv ones; they have the same shape … with the sum over input channels removed" | `grep -n pdiv_of_affine Depthwise.lean` |
| Architectures/Depthwise.lean:461 `depthwiseConv2dBiasGradFormula`; :548 summary; :478 "constrained regular conv" | stale | "(documented, numerically verified, expected to equal `depthwiseConv2dBiasGrad` up to fp precision)"; "you derive them by specializing the regular conv VJPs" | ℝ def; no linking theorem (grep) | "the sum `depthwiseBiasGradHasVJP`'s backward is defined as … No theorem in this file states the equation"; "(This file proves them directly, not by specializing …)" | `grep -rn depthwiseConv2dBiasGradFormula LeanMlir` |
| Architectures/Depthwise.lean (process) | process | "(the ℝ-carrying audit headline)", "(Phase 7 — …)", ⭐/⚠ ×2, "`planning/archive/mnv4_verified.md` §3d measured MNv2's five sites at 2.9e-1 …" | — | removed; the measurement moved to a `--` comment after the docstring (a real magnitude a maintainer may want) | `pn.py` 6 → 0 |
| Architectures/StridedConv.lean (process) | process | title "Chapter 5 Milestone B, the hard new op"; "the Chapter-5 handoff (`planning/archive/verified_r34.md` §3.6) flags as gating the jump from the ch6-A …"; "(the ℝ-carrying audit headline)" ×2; "symmetric ON PURPOSE since 2026-08-04"; "(they were added for ConvNeXt's …)"; planning §3e/§3b refs; "~800-line conv input-VJP" (unsourced count); ⭐/⚠ ×3 | — | removed; "this file supplies that operator and its VJPs" | `pn.py` 10 → 0 |
| Architectures/ChannelLN.lean:8 (module) | stale/process | "`convnextVerified`'s LN was `bnForward` … a different function on 21 of the net's 22 sites (the 22nd is the head …)" | `CnxTieWeights` (Nets/ConvNeXt/ConvNeXtStepTie.lean:466): stem LN (`sγ sβ`), 18 blocks (`nG nB` each, [3,3,9,3]), 3 downsamples (`G T`), head (`hG hT`) = 23 | "a different function … on 22 of ConvNeXt-T's 23 LayerNorm sites (stem, 18 blocks, 3 downsamples, head); the head runs after GAP …" | counted fields of `CnxTieWeights`, `CnxTieBlk`, `CnxTieDown` |
| Architectures/ChannelLN.lean:25–38 (module) | process | "Settled on device before any of this was written … rel 0.82 … Δ 0.00 ms"; "## ⚠ The seam"; "§2k's own sin in a new place"; "proven and shipping" | — | removed from the docstring; the device measurement moved to a `--` comment below it | — |
| Architectures/ChannelLNBack.lean:16 (module) | process | "Moved here from the float bridge … on 2026-09-08 (`planning/archive/float_second_pass.md`)"; "which is how ConvNeXt's LN backward turned out to be ViT's as well" | `chanLNTensor3Back` composes `rowLNVecFlatBack` | dropped history; "so ConvNeXt's LN backward and ViT's share `rowLNVecFlatBack`" | read defs; `grep` the two cited theorems (ConvNeXtBackCertifiedTie.lean:102, ViTVecLNBackCertifiedTie.lean:39) |
| Architectures/EvenKernelConvBack.lean:21 (module) | stale | "`StableHLO.lean`'s `.convStridedBack` emitter … says so in as many words — *'The symmetric `[[p,p],[p,p]]` this emitted AGREES …'*" | the comment is at Codegen/StableHLOPretty.lean:3744–3750 (the `.convStridedBackBatched` arm), worded differently | "The `.convStridedBack` emitter in StableHLOPretty.lean … the batched arm's comment records that the symmetric `[[p,p],[p,p]]` agrees at every odd kernel and is wrong at even ones" (paraphrase, no false quote) | `grep -n AGREES LeanMlir/Proofs/Codegen/*.lean` |
| Architectures/EvenKernelConvBack.lean:3–32 (module) | process | title "⛔⛔"; "load-bearing"; "⛔ ConvNeXt-T is the only net in the repo with an even kernel"; "⚠⚠ The codegen tier already knew"; "⭐ That is the recurring twin-drift pattern … for the third time (`planning/archive/float_budget_numbers_log.md` §3.10 … §3.16 …)"; "⭐ The consequence" | — | markers and the twin-drift history removed; "Note: ConvNeXt-T has four even kernels: the 4×4/s4 patchify stem and the three 2×2/s2 downsamples" (the "only net" claim dropped, not checked across all nets); kept the mathematical rationale (`padOdd` repairs even kernels) | four even kernels: `CnxTieWeights.sW : Kernel4 96 3 4 4`, `CnxTieDown.W : Kernel4 co ci 2 2` ×3 |
| Architectures/ConvBackCertifiedTie.lean:75 `dense_transpose_eq_vjp_backward` | copied | "conv is linear so the activation `x` is ignored" | statement is about `dense (Mat.transpose W) 0` vs `denseHasVJP` | "dense is linear in its input, so the activation `x` is ignored"; one ⚠ marker stripped (:57) | read statement |
| Architectures/DepthwiseBackCertifiedTie.lean:4 (module) | process | "§B shared prerequisite"; "The §B certified-VJP ties" | — | "The depthwise adjoint gate + leaf ties"; "The certified-VJP ties for the three CNNs" | — |
| Architectures/PerChannelBNGrad.lean:8 (module) | stale/process | "the BN analogue of `IR.bias_grad_bridge` / `conv_bias_grad`"; "the M2 dense bridges"; "See `planning/archive/render_close_handoff.md` §2b" | `conv_bias_grad` does not exist; `conv_bias_grad_bridge` is ConvGrad.lean:46 | `conv_bias_grad_bridge`; "the dense bridges `IR.weight_grad_bridge` / `IR.bias_grad_bridge`"; planning ref dropped | `grep -rn "conv_bias_grad\b" LeanMlir` |
| Architectures/TokenParamGrad.lean:9,14,15,30 (module) | process | "(… is the optional Item D)"; "M2 `weight/bias_grad_bridge`" ×3 | — | "(this file does not pin that cotangent to the attention chain)"; `IR.weight_grad_bridge` / `IR.bias_grad_bridge` | grep IR.lean:705,717 |
| Architectures/TokenParamGrad.lean:386 trailing `--` comment | stale | "the row-lifted scalar-LN γ/β (§ B)"; "EVERY parameter family of the representative ViT train step" | no § B in file (sections A, C, E); ViT LN γ/β are `vit_vecln{Gamma,Beta}_grad_bridge` | not edited (a `--` comment, outside the brief); lead | `grep -n "^-- §" TokenParamGrad.lean` |
| Architectures/ConvIndex.lean:45 `t3Idx` | process | "⚠ `@[reducible]` is load-bearing on Lean ≥ 4.33 (see planning/archive/lean_434_and_cleanup.md)" | — | "Note: on Lean ≥ 4.33 the proofs below fail without `@[reducible]`:" (explanation kept) | — |
| Architectures/Softmax.lean:48,163 | process | "Proved (was an axiom)"; "proved (was an axiom in MLP.lean; relocated here to use `pdiv_softmax`)" | — | dropped; "proved from `pdiv_softmax`" | — |
| Training/Optim/AdamStep.lean:8 (module) | stale/process | "(Phase 3a)"; "the load-bearing rung of `planning/archive/vit_train_to_vit_verified.md`"; "the later faithfulness theorem (`den (adamGraph) = adamWStep …`)"; "See the doc's proof/host boundary note" | no `adamGraph`; faithfulness is `adamW_triple_faithful` (Codegen/StableHLO.lean:3442) | "so the faithfulness theorem `adamW_triple_faithful` (Codegen/StableHLO.lean) is a structural match"; history dropped | `grep -rn "adamGraph\|theorem adamW_triple_faithful" LeanMlir` |
| Training/Optim/Lamb.lean:34 (module) | stale | "`lambDir_wd_inside` below states the second as an inequality" | `lambDir … wd … i = lambDir … 0 … i + wd * θ i` (equation) | "states the second as an identity: the direction moves by exactly `wd·θ` before the trust ratio" | read :172 |
| Training/Optim/Lamb.lean:4–12 (module) | stale/process | title "the one item `rsb_a3_r50_verified.md` §2.3 ESTIMATED"; "the faithfulness theorems in `StableHLO.lean`"; "(which reached **76.66% top-1 @ ep100**)"; "load-bearing" ×2; ⭐/⚠ ×9 | LAMB faithfulness is `lamb_triple_faithful` in Codegen/LambTriple.lean:72 | title "LAMB over ℝ — RSB-A3's optimizer"; "the faithfulness theorem `lamb_triple_faithful` (Codegen/LambTriple.lean)"; run result dropped; "matter"/"necessary" | `grep -rn "theorem lamb_triple_faithful" LeanMlir` |
| Training/Optim/RmsPropStep.lean:93 `rmsSqNext_nonneg` | stale | "the FIRST step is damped (`g/√(1−ρ+…)`)" | `rmsSqNext ρ 1 g = ρ + (1−ρ)g²`; `rmsBufNext` divides by `√(rmsSqNext … + ε)` | "the FIRST step is `g/√(ρ + (1−ρ)g² + ε)` rather than the `s = 0` start's `g/√((1−ρ)g² + ε)`, which for small `g` is amplified by up to `1/√(1−ρ)`" | read `rmsSqNext`, `rmsBufNext` |
| Training/Optim/RmsPropStep.lean (process) | process | "`planning/archive/recipe_gaps.md` v1.2"; "§2k's Nesterov-vs-heavy-ball trap" ×3; "handoff §5"; "the faithfulness theorem in `StableHLO.lean`"; ⭐/⚠ ×4 | faithfulness theorem is `rmsProp_triple_faithful` (StableHLO.lean:3499) | named the theorem; §2k → "heavy-ball momentum (`SgdMomentumStep`)" / "heavy-ball's coupled decay"; plan refs dropped | `grep -n "theorem rmsProp_triple_faithful\|momParam_heavyBall_diff"` |
| Training/Optim/SgdMomentumStep.lean:5–12 (module) | process | "(handoff §2i)"; "Why these did not exist … the same shape of blocker §2a found for Adam" | — | "Why these are separate from the `*Sgd` ops … (the fused `θ − lr·g` was also what the AdamW render had to replace)" | — |
| Training/Optim/GradClip.lean (process + counts) | process | title "the ViT / ConvNeXt recipe's last v1.4 piece"; "`planning/archive/grad_clip.md` (`recipe_gaps.md` v1.4b)", "§7 — `wdx-tie`'s …", "gate 3"/"gate 4"; "load-bearing" ×2; "emitted 200× per ViT render", "~200-case", "the other 200", "all 200 (ViT) / 180 (ConvNeXt) parameters" (unchecked counts); ⭐/⚠ ×14 | — | plan refs and gate numbers replaced by what the gate is ("the clip-identity gate", "a gate in the clipping regime"); counts replaced by "once per parameter" / "every parameter of the ViT and ConvNeXt renders" (not re-counted, so not restated) | — |
| Training/SgdDescent.lean:25 (module) | stale | "Discharging its hypotheses for the concrete MNIST nets … is future work" | `SgdDescentLinear`/`SgdDescentMlp`/`SgdDescentCnn` prove segment-Lipschitz lemmas per layer (13/26 `lipschitz` names in Mlp/Cnn) and apply `sgd_descends` | "The per-net discharges of the smoothness hypothesis (explicit segment-Lipschitz constants, one layer and one example at a time) are in `SgdDescentLinear`, `SgdDescentMlp` and `SgdDescentCnn`" | `grep -c lipschitz` |
| Training/SgdDescentLinear.lean:21 (module), :198 `linear_sgd_descends` | overclaim | "one inexact SGD step on the MNIST-linear classifier provably decreases the cross-entropy loss … Every hypothesis is checkable arithmetic" | `linearLoss b x label` — one example, weights only (`b` fixed); `hgh` (oracle accuracy) is a hypothesis | "on the weights `W` …, at one example `(x, label)` with the bias held fixed, decreases that example's cross-entropy loss"; lists the remaining hypotheses | read signature |
| Training/SgdDescentLinear.lean:267 `FloatModel.linearFloatGrad` | overclaim | "exactly as the rendered trainer computes it" | a `FloatModel` expression; no theorem ties it to a rendered trainer | "the FloatModel transcription of the per-example weight gradient" | read def |
| Training/SgdDescentLinear.lean:308 `linear_float_sgd_descends` | overclaim | "One binary32 SGD step … provably decreases the cross-entropy loss — with NO abstract gradient-accuracy parameter. This is Item D / G1 … the honest residue" | conclusion `linearLoss … (Mat.flatten W - lr • M.linearFloatGrad …) ≤ …` (`-`, `•` over ℝ); one example; `hfexp`, `hδ` hypotheses | "One SGD step with the FloatModel binary32 gradient decreases one example's cross-entropy loss; the gradient's accuracy is proven, not assumed." + "Scope: one example `(x, label)`, weights only (bias fixed), and the update … taken in ℝ — only the gradient is float-modelled." | read statement |
| Training/SgdDescentMlp.lean:806–811 `mlp_output_float_sgd_descends` | stale | "The hidden/input rungs … still take an abstract `η` … needs a per-layer float-backward grad-close (a `mlp_w{1,0}_grad_close`) … left open" | `mlp_w1_grad_close` (:867), `mlp_w0_grad_close` (:1220), `mlp_hidden_float_sgd_descends` (:1023), `mlp_input_float_sgd_descends` (:1382) all proved in the file | "The hidden and input rungs are `mlp_hidden_float_sgd_descends` and `mlp_input_float_sgd_descends`." | `grep -n "^theorem" SgdDescentMlp.lean` |
| Training/SgdDescentMlp.lean:32 (module), :395 `mlp_hidden_sgd_descends`, :694 `mlp_input_sgd_descends` | overclaim | "one inexact SGD step on that layer's weights provably decreases the cross-entropy loss … Every hypothesis is checkable arithmetic"; "every MLP weight layer's descent statement is discharged"; "the remaining open rung" | loss is `crossEntropy … label` of one example, one layer's weights as the variable; `hgh` oracle hypothesis | "at one example `(x, label)` with every other parameter fixed, decreases that example's cross-entropy loss"; "the oracle accuracy, the margins, the small-step and the two dominance conditions remain hypotheses"; "each MLP weight layer has a single-layer, single-example descent statement"; "is not proved here" | read `mlpHiddenLoss`, `mlpInputLoss` |
| Training/SgdDescentMlp.lean:794,1005,1367 float rungs; :849,957,1149,1210 float-gradient defs/closes | overclaim/process | "One binary32 SGD step … — with NO abstract gradient-accuracy parameter … (Item D / G1 for the MLP)"; "the *actual* binary32 … gradient"; "exactly as the rendered trainer computes it" / "the rendered trainer computes"; "the honest, lower-risk first cut"; "left as a refinement" | FloatModel gradients; update in ℝ; one example | "One SGD step with the FloatModel binary32 … gradient decreases one example's cross-entropy loss; the gradient's accuracy is proven, not assumed."; "the FloatModel transcription …"; scope line "one example, the layer's weights only, update in ℝ" on hidden and input rungs; output rung says "with the same scope" | read each signature/def |
| Training/SgdDescentCnn.lean:62 (module), :5400 `cnn_conv1_bias_sgd_descends` | overclaim | "EVERY parameter of the Chapter-3 CNN — both conv kernels, both conv biases, and the dense head — now has a proven descent statement"; "the three dense layers (weights and biases via the MLP rungs)" | each capstone is one layer, one example, under margins + `hgh`/`hsmall`/`h1`/`h2`; the MLP rungs are weights-only (SgdDescentMlp module: "Bias columns … are omitted") so dense biases are NOT covered | "Both conv kernels, both conv biases and the dense-head weights … (the latter via the MLP rungs, which omit bias columns) each have a single-layer, single-example descent statement, conditional on …"; float rungs named with "the update is taken in ℝ and only the gradient is float-modelled" | read MLP module + each capstone signature |
| Training/SgdDescentCnn.lean:2312,4304,4932 abstract-η capstones | overclaim | "provably decreases the cross-entropy loss … Remaining hypotheses are checkable arithmetic"; "every conv kernel … has a proven descent statement" | one example, one parameter block moving | "decreases one example's cross-entropy loss (… moving, every other parameter fixed)"; "Remaining hypotheses: the oracle accuracy `η`, …" | read signatures |
| Training/SgdDescentCnn.lean:2454,4445,5722,6150 float rungs | overclaim/copied | "One binary32 SGD step … NO abstract gradient-accuracy parameter"; "the *actual* binary32 … gradient"; "(the honest first cut)"; "float-faithful descent statement"; both bias rungs headed "One inexact SGD step" (copied from the abstract-η rungs) | FloatModel gradient (`M.cnnConv*FloatGrad`, `M.cnnConv*BiasFloatGrad`), update in ℝ, no abstract `η` | "One SGD step with the FloatModel binary32 … gradient decreases one example's cross-entropy loss; the gradient's accuracy is proven, not assumed" + scope line on each | read signatures (`fexp`, `hfexp` binders; no `η`) |
| Training/SgdDescentCnn.lean:1021,1476,3814,4030,5533,5570, :912 | overclaim | "the rendered trainer computes"/"the rendered trainer's … gradient"/"the rendered trainer's float … rounds" | `FloatModel` defs, not tied to a rendered trainer | "FloatModel transcription of the per-example gradient" / "the FloatModel gradient's …" | read defs |
| Training/SgdDescentCnn.lean (process) | process | "(Item A capstone)", "(Item B)" ×2, "(Increment 1/2/3/4 keystone/capstone)" ×7, "Increment 2's conv-2 cotangent chain", "the project's deepest float-backward grad-close" | — | removed; "the conv-2 cotangent chain of `cnn_conv2_grad_close`" | `pn.py` on docstrings: 13 → 0; `--` section headers left |
| Training/SgdDescentCnn.lean:20 (module) pool margin; :2312 `hmq` | overclaim | "`MaxPool2Smooth` (pairwise-distinct window cells) is the qualitative off-the-kink condition" (post-ReLU margin) | — | deferred §5 (MaxPool live-cell row) | — |
| Training/SgdDescentCifar.lean:4–30 (module), :100 `cifar8_lastConv_sgd_descends` | overclaim/process | "(A2 probe)", "`planning/archive/tier23_…` A2 asked …"; "reaches CIFAR-8 **for free**, with the SAME non-vacuous admissible `lr` as MNIST"; "the admissible `lr` is the same non-vacuous MNIST regime"; "The honest stop (why this is the ceiling) … vacuous in any realistic regime … the same honest stop as the deep nets" | instance of `cnn_conv2_sgd_descends` at `x₁`; hypotheses `hgh hm2 hmq hm3 hm4 hsmall h1 h2` carried; nothing proves the `lr` regime non-vacuous; one example `(image, label)` | "with the same hypotheses (oracle accuracy, the relu and post-ReLU pool margins, small-step and dominance conditions) stated at `x₁`"; theorem: "with that lemma's hypotheses (`hgh`, `hm2`/`hmq`/`hm3`/`hm4`, `hsmall`, `h1`, `h2`) stated at `x₁`"; "**Scope.** Descent through the depth of all eight conv layers is not proved here …" | read signature binders (:125–151) |
| Training/JacobianSeal.lean:50 `exists_pdiv_ne_of_fderiv_ne` | copied | "**The seal in `fderiv` form.**" (same heading as `HasVJPAt.backward_nontrivial_of_fderiv_ne`) | conclusion is `∃ i j, pdiv f x i j ≠ 0` only | "**Nonzero `fderiv` ⇒ a nonzero Jacobian entry.**" | read both statements |
| Training/JacobianSeal.lean:12 (module) | process/overclaim | "the missing level-3 seal (see `planning/archive/whole_network_backward.md`, Item B)"; "the rendered backward at this point is not the zero map" | theorems are about `HasVJPAt.backward`, not a rendered artifact | "the level-3 seal"; "the proven backward at this point" | read statements |
| Training/DropPath.lean:320→(now ~158) `keepProb` | copied | "`keep_i = 1 − dropPath · i / (totalDrop − 1)`" | parameter is `dropRate`: `1 - dropRate * i / (totalDrop - 1)` | "`keep_i = 1 − dropRate · i / (totalDrop − 1)`" | read def |
| Training/DropPath.lean (process) | process | "`planning/archive/stochastic_depth.md`" ×3 with "§1 fact 3"/"§3"; "`recipe_gaps.md` … Tier E", "gap C"; "the fourth time … (§2k heavy-ball, recipe_gaps v1.2 RMSProp, the EMA shadow, here)"; "the fifth time"; "gate 1"; "§2k's `α/K` bug"; "`planning/archive/xla_pjrt_handoff.md` §0.10"; "load-bearing" ×2; "it caught `resnet34_fwd` and `mobilenetv2_fwd` …"; "RenderCifar8Sgd02 / enet-16×"; ⭐/⚠/▶ ×9 | — | plan/§/gate/run references removed; the two design requirements, the ramp warning ("Note: `totalDrop` counts ALL blocks …") and the consumer rule kept as plain sentences | `pn.py` 30 → 0 (remaining hits are "prefix audit" prose) |
| Training/DropPath.lean:~180 `keepProb_last` | — | "ConvNeXt-T has 18 blocks, ViT-Tiny 12, EfficientNet-B0 16" | kept | unchanged | ConvNeXt-T: `CnxTieWeights` b1..b18; B0: 1+2+2+3+3+4+1 = 16 MBConv (standard); ViT-Tiny 12 (standard, not re-grepped) |
| Training/BatchSealKit.lean (process) | process | "`planning/full_width_seals.md` §3. The level-2/3 witnesses … have until now been exhibited on 2-channel per-example proxies"; ⭐/⚠ ×18 | — | "The level-2/3 witnesses … on 2-channel per-example proxies see a BatchNorm that is `bnForward` over one activation"; markers stripped (text unchanged); in-file §0–§12 section refs kept | `strip.py` count 18 |
| Training/TrainedMlpWitness.lean:12 (module) | process | "The audit's 'live witnesses use synthetic weights' gap, closed at the MLP rung" | — | "A trained-weight witness at the MLP rung (the full-width seals use synthetic weights)" | — |
| Training/TrainedLinearDescent.lean:3–16 (module), :282 `trained_linear_sgd_descends_concrete` — generated; edited in scripts/certs/trained_linear_descent.py | overclaim/stale/process | "(post_audit_roadmap §3)"; "(the suite's only concrete descent instance)"; "one binary32 SGD step … provably decreases the real cross-entropy loss"; "the whole statement is axiom-free"; "zero axioms. Retires the `W = 0` degeneracy caveat" | the file adds a second concrete instance; proof instantiates `linear_float_sgd_descends … Real.exp … (eexp := 0)`; update `Mat.flatten Wd - lr • …` over ℝ; one example (image #8) | "`binary32_linear_sgd_descends_concrete` (Float/Binary32Instance.lean) holds only at the degenerate `W = 0` net"; "one SGD step with the FloatModel binary32 gradient … decreases the real cross-entropy loss of one example"; "The update is taken in ℝ and the `exp` is exact (`fexp := Real.exp`, `eexp := 0`)"; capstone likewise | generator run before the edit reproduced the committed file byte-identically (`git diff --stat` empty); after the edit the only diff is the two docstrings (`git diff`); `grep -n "Real.exp\|eexp :=" TrainedLinearDescent.lean` (:303–304) |
| Training/TrainedCnnWitness.lean:5 (module) — generated; edited in scripts/certs/trained_cnn_witness.py:417 | process | "(the 2026-07 audit's gap #3)" | — | dropped | generator reproduced the committed file byte-identically before the edit; after it the diff is the one docstring hunk; the `h_mp` bullet (post-ReLU pool condition) left as is — deferred §5 |
| Training/TrainedCnnSeal.lean — generated (scripts/certs/trained_cnn_seal.py) | — | no finding | — | not edited | generator re-run: byte-identical |

## Tally

- Report entries: 28. Fixed 26 (entry 1 partly: "EVERY parameter" / "non-vacuous" fixed, the post-ReLU pool-margin wording deferred §5; entry 19 partly: the γ/β note is a plain `/- -/` comment, not edited). Deferred §5: entry 4 (`MaxPool2Smooth`) and the pool-margin half of entry 1. Report wrong: none outright (entry 24's quote is a paraphrase of the StableHLOPretty comment; rewritten as a paraphrase).
- Beyond the report: dense-head biases have no descent statement (MLP rungs omit bias columns) although SgdDescentCnn claimed "weights and biases via the MLP rungs"; `bnForward_lb` citation; "these ten things" (3 + 5 = 8); "load-bearing" ×7 removed.
- Files touched: 36 Lean files under Proofs/Architectures + Proofs/Training (two of them generated), plus the generators scripts/certs/trained_linear_descent.py and scripts/certs/trained_cnn_witness.py (both re-run: byte-identical before the edit, docstring-only diff after); trained_cnn_seal.py re-run, byte-identical.
- Process scan (`pn.py`, docstrings only): 235 matching lines before, 35 after (the remainder are "backward pass", in-file § section refs, "prefix audit", "axiom audit").
- Every changed diff line lies inside a comment or docstring (checked by a hunk-vs-comment-span script); backtick parity per docstring unchanged.
