# Ledger — slice G (LeanMlir/Proofs/Nets/MobileNet/)

file:line | kind | old claim | what the statement says | new text | how checked
---|---|---|---|---|---
MobileNetV2Close.lean:6 (module) | stale / process | "every MobileNetV2 train-step parameter output denotes θ − lr·(certified Jacobian · cotangent)"; retired-step family table; "depthwise W stride 1 (blocks b2,b4)"; planning/archive Item C/D | file holds 4 theorems: `mnv2_depthwise_bias_grad_bridge`, `mnv2_render_depthwiseb_certified`, `mnv2_render_stem_convW_certified`, `mnv2_render_stem_convb_certified`, each generic in the cotangent | "Three SGD bridges … for a free cotangent `c`" + list of the three with their VJPs; users MobileNetV2Fold (→ ConvNeXtStepTie) and ResNet34Fold; mnv2_net_tiedB ties strided params through the GradNodesB `*_den` lemmas, not this file | `grep -n theorem` on the file; `grep -rln mnv2_render_…` (users: MobileNetV2Fold, ResNet34Fold, AuditAxioms); `grep -n Mnv2PoC.depthwise ConvNeXtStepTie.lean`; the four `*_den` names grepped in GradNodesB.lean (namespaces Proofs.EnetPoCG/Mnv2PaperPoCG) and their uses in MobileNetV2StepTieB (:475, :604, :605; EnetPoCG one via ResNet34PoCB.convStridedXlaWTiedB_holds)
MobileNetV2Fold.lean:5 (module) | process | "Written for MobileNetV2's per-example fold; ConvNeXt-T's … ties are what use them now"; "## Honest residual … the §1a tie. Per-op `pretty` lexing + ℝ → Float32" | `depthwiseW_den`/`depthwiseB_den` hold ∀ cotangent, over ℝ | "`ConvNeXtStepTie` uses them for ConvNeXt-T's depthwise weight and bias nodes"; "Scope: the cotangent `c` is a binder …; nothing here ties it to a net's backward chain. Statements are over ℝ." | `grep -n Mnv2PoC.depthwise ConvNeXtStepTie.lean` (:157, :158); signatures read (`lr : ℝ`, `cot` universally bound)
MobileNetBackChains.lean:4 (module) | process | ⚠ / ⭐ / ⚠⚠ markers, "TO THE IMAGE" caps | — | markers dropped; "Note: `mnv4InputGradB` is stated to the image" | 3 markers removed
MobileNetBackChains.lean:34 `mnv2InputGradB`, :72 `mnv4InputGradB` | stale (minor) | "block backwards and the two BatchNorm backs are supplied; the conv, GAP and dense leaves are concrete" | the relu masks are also binders (`m_stem`, `m_head`; MNv4 `m_stem`, `m_h1`, `m_h`) | added "the two relu6 masks' predicates" / "the three relu masks' predicates" to the supplied list | signature read (:39–:62, :83–:115)
MobileNetV2FullPaper.lean:3 (module) | process | "bind the same records the per-example net did"; "This file used to hold … retired on 2026-09-20" | file holds `IVW`, `IVWNoExp`, `IVPos`, `IVNoExpPos` only | "…(`MobileNetV2FullB.lean`, `MNV2BWeights`) and its step tie bind them."; retirement paragraph deleted | `grep -n "structure\|def" MobileNetV2FullPaper.lean`; `grep -rln IVW` (FullB, StepTieB, SyncB, …)
MobileNetV4Spec.lean:14 `UibSpec.p`, :27 `mnv4Blocks` | process | "Conv-S ran to "14""; ⭐⭐; "Before it existed the same rows were hand-written FOUR times, and §3/§7.2's whole point…"; "where Conv-S used three"; ⚠ | the 21-row list | kept the purpose ("everything downstream folds over this list … the rows are written once"); history, § numbers, Conv-S comparisons, 2 markers removed | family counts re-counted from the 21 rows (13 ExtraDW / 4 ConvNeXt / 4 FFN)
MobileNetV2.lean:3 (module) | overclaim | "Builds a representative MobileNetV2 forward"; "stem (3×3 conv-bn-relu6)"; "Fixed block counts" | `mobilenetv2Forward`: stem `flatConv` (kernel `kHs kWs` binder) → `bnForward` (scalar ε γ β) → relu6, `residual invresBody`, `invresBody`, GAP, dense; all stride 1 | "Defines `mobilenetv2Forward` — stem conv → bn → relu6, one inverted-residual block with a skip, one without … Every "bn" in this file is `bnForward`: one normalisation over the whole flattened tensor with scalar `γ β : ℝ` … Every conv is stride 1. The seventeen-block … net … is `mobilenetv2ForwardBFull`" | read `mobilenetv2Forward` (:252) and `bnForward` (BatchNorm.lean:199, scalar γ β); `grep def mobilenetv2ForwardBFull` (FullB.lean:179); `cnnHasVJPAt` (CNN.lean:1588, two res blocks)
MobileNetV2.lean:367 `mobilenetv2HasVJPAt_correct` | overclaim | "the full MobileNetV2 backward equals the pdiv-contracted Jacobian" | `.correct` projection under h_stem, h_b1e, h_b1d, h_b2e, h_b2d (each ≠0 ∧ ≠6) | "at a point where all five relu6 sites (…) are away from 0 and 6, the backward of the two-block `mobilenetv2Forward` equals …" | signature read; five hypotheses counted in the binder list
MobileNetV2BackB0.lean:4 (module "Structure") | stale / process | "* `cbrB` / `dwbrB` … (`cbrBackBatchedGraph` + `…_faithful`). * `cbrLayer` / `dwbrLayer` / `dwbrStridedLayer` / `projLayer` — the four stages as `CertLayer`s"; "reused VERBATIM from the EfficientNet file"; "⚠ This family takes `ic` and `oc` SEPARATELY (2026-09-06)…" | file defines only `mnv2BodyLayer`, `mnv2DownBodyLayer`, their HasVJPAt/diff/graph/_faithful wrappers and `mnv2ResidBlockBackBatchedGraph(_faithful)`; stages imported | stages named with their defining files (BatchedStageLayers: cbrB, dwbrB, dwbrBstrided, cbrLayer, dwbrLayer, dwbrStridedLayer, projLayer; BatchedStages: projB; BatchedBackLinks: residualBackGraph); ic/oc note kept as a plain sentence without the date/history | `grep -rln "def <name>\b"` for each of the ten names; `grep -n "^noncomputable def\|^theorem"` on the file; `mnv2ExpOnlyB` at FullB.lean:142; b11/b17 widths from the MobileNetV2FullPaper.lean table
MobileNetV2FullB.lean:4 (module) | stale / process | "`MobileNetV2FullPaper.lean` states this net's whole-net ℝ forward and typed graph at per-example BatchNorm"; "`MobileNetV2BackB0.lean` already carries the batched relu6 stages (`cbrB`, …)"; "pinned only where a `Maps` envelope turns a width into a rational (T4/T5)"; "the render's `N` is the PER-REPLICA batch (64); since 2026-09-21…"; ⛔ renderer history (4c leg 2, 2026-09-06); planning/archive §4.2 | FullPaper.lean holds only weight records; stages defined in Foundation/BatchedStageLayers + BatchedStages; no Maps/T4/T5 in tree | header rewritten: what `mobilenetv2ForwardBFull` / `mobilenetv2FwdGraphBFull` / `_faithful` are; stages named with defining files; T4/T5 sentence and renderer history deleted; DP paragraph kept without date/"(64)" | `grep -rln "def cbrB\b"` etc.; `grep -rn Maps\|T4 LeanMlir/Proofs/Nets/MobileNet` (none); `ls verified_mlir \| grep mobilenetv2` (rmsdp128 has N=128, so "(64)" dropped)
MobileNetV2FullB.lean:~42 (artifacts row) | overclaim | "`mobilenetv2_fwd` and every train step — this net now has ONE chain (4c leg 2)" | graph ties (`FwdGraphTextTies` §MobileNetV2) are at the f32 text; eval artifacts take frozen stats (`mobilenetv2_fwd_eval`: 262 params incl. `%b17pnmu`); `mobilenetv2in_rmsdp64wxdols0{,eps0001}bf16` take `%do` | "`mobilenetv2_fwd`, `mobilenetv2in_fwd` and the f32 train steps. Not this graph: the `bf16` train steps, the frozen-statistics evals (…`MobileNetV2FullPaperEval`) and the classifier-dropout variants `mobilenetv2in_rmsdp64wxdols0*`" | python arg-count of each artifact's func signature; `grep -c '%do:'` per artifact (2 MNv2 hits)
MobileNetV2FullB.lean:~48 `MNV2BWeights` | stale | "the retired per-example record was pinned at 10, and the lesson … paid for is that the head's envelope depends on the fan-in" | structure generic in `nCls` | "Generic in `nCls`, so one record covers the 10-class (`mobilenetv2_*`) and 1000-class (`mobilenetv2in_*`) artifacts." | `%Wd: tensor<1280x10>` in mobilenetv2_fwd, `1280x1000` in mobilenetv2in_fwd
MobileNetV2FullB.lean `mobilenetv2ForwardBFull`, `mobilenetv2FwdGraphBFull_faithful`, `mnv2ResidB` | process | "batched peer of the retired per-example forward … so a T6 tie can peel it"; "⭐ T2 for MobileNetV2 at batch BN"; ⭐ | — | "so the whole-net tie (`MobileNetV2WholeBackCertifiedTieB`) can peel it"; "The MobileNetV2 forward graph at batch BN denotes the whole-net forward." | 7 markers/tier labels removed in the file. Counts kept: 52 BN sites (52 `%…g` operands in mobilenetv2_fwd), 35 relu6 (52 − 17 projects), census 158 (159 args − `%x`), 210 = 158 + 52 conv biases
MobileNetV2FullBVJP.lean:3 (module), `mobilenetv2ForwardBFullHasVJPAt`, `mobilenetv2ForwardBFull_eq_chain`, `mobilenetv2ForwardBFull_differentiableAt`, `MNV2SmoothAtB`, head VJP | process | "(T1, the VJP half)"; "the second piece of `formalization.yaml` 4e's port"; "so 4.1c had to write"; "(the per-example fold … retired 2026-09-19)"; "this tier carries no numerals"; "the batched peer of the retired per-example shape check"; 15 ⭐/⚠/⛔ | — | tier/§/date/retirement text deleted; markers dropped; genuine notes (pointwise because relu6 kinks both sides; head not hypothesis-free) kept as plain sentences | counts kept and re-checked: 16 expand-bearing blocks (b2–b17), 32+1+2 = 35 relu6 sites, `MNV2SmoothAtB` has 19 fields (stem, b1–b17, head), 10 skip blocks
MobileNetV2FullBVJP.lean:~521 `mobilenetv2ForwardBFullHasVJPAt_correct` | overclaim (minor) | "the seventeen-bottleneck batch-BN backward equals the pdiv-contracted Jacobian of `mobilenetv2ForwardBFull` ITSELF" | binders `hq : MNV2PosB w`, `hx : MNV2SmoothAtB N w x` | "for weights with `MNV2PosB` and an input with `MNV2SmoothAtB`, the … backward equals …" | signature read (:527–:530)
MobileNetV2FullPaperEval.lean:3 (module) | stale / process | "The eval twin of `MobileNetV2FullPaper.lean`. That file states the seventeen-block … net at TRAINING BatchNorm"; "The seventeen-block TRAINING graph names its parameters `%b17gp`/`%b17btp`, where the render emits `%gp17`/`%btp17`"; "(Built 2026-09-05 … deleted 2026-09-08 … 3.2(e))"; "T2 at inference" | FullPaper.lean holds only weight records; the training graph (FullB.lean:210) writes `%b{pfx}pg`/`%b{pfx}pbt`, matching `mobilenetv2_adam_train_step` (`%b17pg`, `%b17pbt`); the eval artifact uses `%gp17`/`%btp17`; eval forward is per example (`x : Vec (3*224*224)`) | "The inference twin of `MobileNetV2FullB.lean`, the batch-BatchNorm training forward … Both are stated for one example"; "The training graph (`MobileNetV2FullB`) names its parameters `%b{k}{e,d,p}{W,g,bt}`, as the train-step render does. The eval render uses other names, and this file's graph carries them verbatim"; dates/§ deleted; eps0001 twin added to the "covers" sentence | python: arg lists of mobilenetv2_fwd_eval (263, `We17 … gp17 btp17 b17enmu …`) and mobilenetv2_adam_train_step (`b17pg`, `b17pbt`); `grep pg\" MobileNetV2FullB.lean`; mobilenetv2in_fwd_eval{,_eps0001}: 263 args, `Wd 1280x1000`
MobileNetV2FullPaperEval.lean:~300 `mobilenetv2FwdGraphPaperEval_faithful` | proof-narrative / stale | "the training twin's recipe with `bnPerChannelEvalF_faithful` in place of `bnPerChannelF_faithful`" | `den (graph) = mobilenetv2ForwardPaperEval ε w x` ∀ epsStr ε w x | "The typed graph denotes `mobilenetv2ForwardPaperEval`, for every shared `ε`, weight record and input example."; recipe moved to a `--` comment in the proof | statement read
MobileNetV2FullBSeal.lean:20 (module) and :~67 (§1 `--` comment) | wrong | "`β = 0` at the eleven linear-bottleneck projections" | `sealIVW` (:87), `sealResW` (:104), `sealNoExpW` (:116) all set `pβ := kv _ 0`; 17 bottlenecks each have one project BN (52 − 35 = 17) | "seventeen" in both places | `grep -n "pβ :=" MobileNetV2FullBSeal.lean`
MobileNetV2FullBSeal.lean:~1040 `sealX_backward_nontrivial` | overclaim | "`mobilenetv2ForwardBFull`, the forward every MobileNetV2 artifact runs" | eval artifacts run `mobilenetv2ForwardPaperEval` (frozen stats); the seal is at `N = 2` | "`mobilenetv2ForwardBFull`, the training-BatchNorm forward the MobileNetV2 train steps run, here at `N = 2`" | statement read (`Fin (2 * nCls)`); eval artifact signatures (263 args with `…nmu`)
MobileNetV2FullBSeal.lean (module + 9 decl docstrings) | process | `planning/full_width_seals.md` §4.3/§7/§3.5; "until now that was exhibited only on a per-example … proxy, deleted when this file landed"; 13 ⭐/⚠ markers | — | planning refs and history deleted; the variable-shape hazard kept as "Note: every collapse below is stated at variable …, never proved at them." | counts left as-is and re-checked: 22 = 1 + 2 + 3×6 + 1 carrier BNs; 7 channel-changing blocks; 19 bundles
MobileNetV2StepTieB.lean:6 (module) and :~645 `mnv2_net_tiedB` | overclaim | "the four `*CotIn_eq_vjp` lemmas … say the constructed chain IS the certified whole-net backward"; "make the cross-block thread a composition of certified VJPs"; "down through the head chain and the seventeen certified block backwards" | four lemmas `mnv2NoExpCotIn_eq_vjp` (:158), `mnv2ExpOnlyCotIn_eq_vjp` (:242), `mnv2ResidCotIn_eq_vjp` (:265), `mnv2StridedCotIn_eq_vjp` (:343) each equate one block's constructed cotangent with `(…BHasVJPAt …).backward` under `hq`/`hs`; `mnv2HeadCotBlk`, `mnv2StemCotN/C` have no VJP tie anywhere (only `_smul` lemmas in SyncStepTieB) | module: lemmas named, "each says one block kind's constructed input cotangent is its certified block VJP's backward, under that block's positivity and smoothness hypotheses. The head segment (`mnv2HeadCotBlk`) and the stem segment (`mnv2StemCotN`, `mnv2StemCotC`) of the chain are not tied to a VJP."; capstone: "constructed head chain and the seventeen constructed block backwards … at that chain's cotangent … One device, f32." | `grep -rn CotIn_eq_vjp`; `grep -rn "mnv2HeadCotBlk\|mnv2StemCotC"` outside the file (only `_smul` / shard lemmas)
MobileNetV2StepTieB.lean:24 (module) | wrong | "`b3`, `b5`, `b6`, `b8`–`b13`, `b15`, `b16` (skip)" | b11 is the no-skip widening (`mnv2CotInBody … w.b11` in `mnv2_net_tiedB`) | "`b3`, `b5`, `b6`, `b8`, `b9`, `b10`, `b12`, `b13`, `b15`, `b16` (skip)" | read the `let dy…` chain in `mnv2_net_tiedB` (ResidCotIn at b3,b5,b6,b8,b9,b10,b12,b13,b15,b16 = 10)
MobileNetV2StepTieB.lean:34 (module), :~666 (capstone) | stale | "Every statement below is at the PER-REPLICA gradient node; `DataParallelNode.lean` composes it with the mean and the tail"; "⛔ One replica … an AST node since 4d piece 2" | DP artifacts run sync-BN (`bnSyncInB`); `mnv2_net_syncTiedB` (SyncStepTieB:991) equates the all-reduced mean with `mnv2_net_tiedB`'s node at `R·N` | "This file describes a data-parallel artifact only at `N := R·N`: the per-replica statement is `MobileNetV2SyncTieB.mnv2_net_syncTiedB`, whose right-hand side is `mnv2_net_tiedB`'s node at the global batch." | read `mnv2_net_syncTiedB` statement + docstring; namespace `Proofs.MobileNetV2SyncTieB` (SyncStepTieB:61)
MobileNetV2StepTieB.lean:~733 `mnv2_lossCot_is_smoothedCE_grad` | overclaim (minor) | "every parameter node denotes the certified gradient at the cotangent of the loss the trainer actually minimises" | the capstone ties each node at the constructed chain's cotangent; the head/stem segments are not VJP-tied | "every parameter node denotes the certified batched gradient at the chain cotangent that starts from the gradient of the loss the trainer minimises" | as above
MobileNetV2StepTieB.lean (module + 17 decl docstrings) | process | "T3 §1a", "(4b.4)", "With 4.2b it completes MobileNetV2's T3", "4.2b's", "since 4d piece 2 (2026-09-07) … until then `emitGradAllReduce`", "the §1 fold", "one tier above", ~28 ⭐/⚠/⛔ | — | removed; genuine distinctions (XLA vs symmetric strided certificates, bias conjuncts not emitted, head not `batchMap`) kept as plain sentences; `mobilenetv2in_rmsdp64` replaced by `mobilenetv2in_adam64_train_step` as the N=64 single-device example | `ls verified_mlir` (`mobilenetv2in_adam64_train_step.mlir`, `%x: 64x150528`); count 210 = 4 + 8 + 16·12 + 4 + 2
MobileNetV2WholeBackCertifiedTieB.lean:~356 `mnv2InputGradB_correct`, :~258 `mnv2InputGradB_eq_mobilenetv2B_full_vjp` | overclaim | "⭐⭐ The batched chain IS the `pdiv`-contracted Jacobian of the twenty-one-stage net — at every batch size, every input, every loss cotangent and every input pixel"; "`mnv2InputGradB` IS the certified whole-net batch-BN MobileNetV2 gradient" | hypotheses `h_stem : MNV2StemSmoothAtB …`, `h_head : MNV2HeadSmoothAtB …`, 17 supplied `hb_k : HasVJPDiffAt b_k …`; `b1 … b17` are free function binders; RHS is `pdiv (dense ∘ gap ∘ cbrB ∘ b17 ∘ … ∘ b1 ∘ mnv2StemB)` | "at every batch size and loss cotangent, and at every input where the stem and head relu6 clauses hold (`h_stem`, `h_head`): the chain, with its seventeen block slots filled by the supplied certified block VJPs … is the Jacobian-transpose of `dense ∘ gap ∘ cbrB ∘ b17 ∘ … ∘ b1 ∘ mnv2StemB`. The blocks are variables here; `mobilenetv2ForwardBFull_eq_slots` identifies the stages …"; `_eq_` docstring likewise names `hb1 … hb17`, `h_stem`, `h_head` | signature read (:360–:438); 17 `hb` binders counted
MobileNetV2WholeBackCertifiedTieB.lean:50 (module "What this does NOT reach") | stale | "this is at the per-replica gradient before it (§4d)" | single-device statement at batch `N`; DP artifacts use sync-BN over `R·N` | "**Scope.** One device's batch `N`: the data-parallel artifacts normalise over the global batch, so this describes them only at `N := R·N`." | `MobileNetV2SyncB.lean` module doc (shard `r` of `mobilenetv2ForwardBFull (R * N)`)
MobileNetV2WholeBackCertifiedTieB.lean (module + 7 decl docstrings) | process | "retired 2026-09-19 with `MobileNetV2PaperWholeBackCertifiedTie.lean`", "tier T6 of planning/archive/… §4.2", "(moved here 2026-09-19 …)", "(4.2b)" ×3, "(§3.10)", "(§4d)", "(§4.2c)", "chainComp₂_comp (retired)", ~15 ⭐/⚠/⛔ | — | removed; kernel-timeout hazard (head re-association, opaque blocks) kept as plain sentences | `ls LeanMlir/Proofs/Nets/ResNet` (ResNet34BackCertifiedTieB.lean exists)
MobileNetV2SyncStepTieB.lean:~974 `mnv2_net_syncTiedB` | overclaim | "so this and it together say the DP step's update is the certified gradient of the global-batch step" | conjuncts are about the all-reduced mean gradient nodes (158); no optimizer tail stated | "…every all-reduced gradient the DP render emits is the global-batch step's gradient node. The optimizer update that follows (RMSProp/AdamW) is not stated here." | statement read; DP artifacts `mobilenetv2_adamdp`, `mobilenetv2in_rmsdp*`, `mobilenetv2in_adamdp64*` (ls verified_mlir)
MobileNetV2SyncStepTieB.lean:~988 `mnv2_net_syncTiedB` | stale / process | "⛔ Before the render's sync-BN swap … this statement was false: `DataParallel.dpMeanGrad_ne_globalBatchGrad` is the witness, and stays as the statement of what those runs did" | `dpMeanGrad_ne_globalBatchGrad (θ : Vec 1)` is a two-replica toy counterexample (DataParallel.lean:235) | "With per-replica BatchNorm the corresponding statement is false in general; `DataParallel.dpMeanGrad_ne_globalBatchGrad` is a two-replica counterexample." | read DataParallel.lean:222–235
MobileNetV2SyncStepTieB.lean, MobileNetV2SyncB.lean (module + decl docstrings) | process | "(T3)", "T3's node", "T2's data-parallel twin", "(T2)", "is P1 on the graph", "the T3 half", 13 ⭐/⚠/⛔ | — | tier labels replaced by the declarations they stood for (`mnv2_net_tiedB`, `mobilenetv2FwdGraphBFull_faithful`, `MobileNetV2SyncStepTieB.lean`); "P1" → "the sync-BN shard identity"; markers dropped. File-internal "(§1)"/"(§4)" kept (they name this file's own `-- § 1.`/`-- § 4.` sections) | `grep -n "^-- § [0-9]"` on SyncStepTieB; `den_bnSyncSiteLA` at DataParallelSyncKit.lean:72
MobileNetV4BackB0.lean:6 (module) | stale | "the depthwise-relu stages, the UIB body, the skip block, the stride-2 form…, each a `CertLayer`"; "the family-collapsing body `mnv4UibBody`, the skip block, and the stride-2 form" | no skip-block def in the file; `CertLayer.residual` is applied by callers (`mnv4BodyOfRow` docstring, :518 comment) | "the depthwise stages, the UIB body, the stride-2 body, the fused stage and the head … The identity skip is `CertLayer.residual`, applied by the caller." | `grep -n "^noncomputable def\|residual"` on the file
MobileNetV4BackB0.lean:~220 (`--` comment), :~293 (fused-stage `/-!`) | stale | "`ResNet34BackB0`'s `cbReluLayer` and `projLayer`"; "ResNet's `cbReluStridedLayer`" | all three defined in Foundation/BatchedStageLayers.lean | "`Foundation/BatchedStageLayers`'s `cbReluLayer` and `projLayer`"; "`cbReluStridedLayer` and … `projLayer` (both `Foundation/BatchedStageLayers`)" | `grep -rln "def cbReluLayer\b"` etc.
MobileNetV4BackB0.lean:~463 (`/-!` before `UibParams`) | wrong | "rows 4, 5 and 10 are all `160 → 160, expand 4`, and 4/10 share `k = 3,3`" | `mnv4Blocks` row 10 = ⟨"10",160,160,4,3,0,14,false⟩ (postDWk 0); shape-identical sets are {4,5,7}, {8,10}, {12,18}, {13,14}, {15,19,20} | "Rows with the same `(ic, oc, expand, preDWk, postDWk, h)` — {4, 5, 7}, {8, 10}, {12, 18}, {13, 14} and {15, 19, 20} — have records of the same type" | read all 21 rows of `mnv4Blocks` (MobileNetV4Spec.lean:48–68) and grouped by tuple
MobileNetV4BackB0.lean (module + 17 docstrings) | process | "Until 2026-09-24 …" ×3, "(`planning/mnv4_timm_parity.md`)", "T1 and T2 … T3 … T6", "That is §3's trap", ~20 ⭐/⚠/⛔ | — | dated before/after notes rewritten as the mathematical point (stride on the pre-DW / conv_head before the pool would be a different function); planning ref → `scripts/parity/mnv4_timm_parity.py`; tier labels → named files; markers dropped. `--` comments at :403–:426 left (not docstrings) | `ls scripts/parity/mnv4_timm_parity.py`
MobileNetV4FullB.lean (artifacts row), :~807 `mnv4FwdGraphBFull` | overclaim | "`mnv4_fwd`, `mnv4_fwd_eval`, `mnv4_adam_train_step`, and the five `mnv4in*` ImageNet twins"; "diffs against `mnv4_fwd.mlir` and its five ImageNet twins name for name" | 9 `mnv4in*` files; `mnv4{,in}_fwd_eval` take frozen stats (388 args); `mnv4in_fwd_eval_s256` has `%x: 64x196608` (3·256²); `mnv4in_emaacc{,dp}8x128wxdowd005bf16` take `%do` | row lists `mnv4_fwd`, `mnv4in_fwd`, the f32 224×224 train steps, and names what is not this graph (bf16 steps, frozen-stat evals, s256 eval, `%do` variants); graph docstring: "diffs against `mnv4_fwd.mlir` and `mnv4in_fwd.mlir` … (the artifacts it covers are listed in the module's conventions table)" | `ls verified_mlir \| grep mnv4` (12 files, 9 `mnv4in*`); python arg count per artifact; `grep -c '%do:'`
MobileNetV4FullB.lean (module) | overclaim | "Unlike R50 there is no `q` binder — MNv4 ships one resolution." | statements at 224×224; `mnv4in_fwd_eval_s256` runs at 256 | "Unlike R50 there is no resolution binder `q`: the statements are at 224×224." | `%x: tensor<64x196608>` in mnv4in_fwd_eval_s256
MobileNetV4FullB.lean (module, `/-!` before `mnv4Pre0`) | stale / wrong | "~60 relu clauses across the net"; "`Mnv4SmoothAt` binds one `.ok` per group (seven)" | seal `#guard 1 + 1 + Σ(1 + [postDWk≠0]) + 2 = 38`; `Mnv4SmoothAt` has 8 fields (stem, fused, g28, g14a, g14b, g7a, g7b, head) | "38 relu clauses, counted by `MobileNetV4FullBSeal`'s `#guard`"; "one `.ok` per group plus the stem's clause (eight fields)" | MobileNetV4FullBSeal.lean:58; MobileNetV4FullBVJP.lean:95–111
MobileNetV4FullB.lean (module), `Mnv4BWeights` | wrong | "Rows 4/5/10, 12/18 and 15/19/20 are shape-identical"; "(4/5/10, 12/18, 15/19/20)" | see MobileNetV4BackB0 row: {4,5,7}, {8,10}, {12,18}, {13,14}, {15,19,20} | "Rows 4/5/7, 8/10, 12/18, 13/14 and 15/19/20 are shape-identical (same `ic, oc, expand, preDWk, postDWk, h`)" | `mnv4Blocks` rows read
MobileNetV4FullB.lean `mnv4SkipGraphB` | process / banned phrase | "Trivial as a definition and load-bearing as a barrier"; "which is what this file did first" | — | "Trivial as a definition; it exists as a barrier to unfolding."; history clause removed | —
MobileNetV4FullB.lean (module + ~25 decl/section docstrings) | process | planning/archive refs ×2, "closed the same two tiers on 2026-09-06", "since 2026-09-24 (`planning/mnv4_timm_parity.md`)", "the rerun on timm's net is queued" (status), "this cost a day to establish", "Every one of those four spellings was measured", "Until 2026-09-24 this was the XLA-`SAME` phase", "(T1-forward, T2)", "T1's forward half", "T2 for MobileNetV4", "The first graph-level tier this net has ever had", "the single hardest thing this file learned", ~45 ⭐/⚠/⛔/✅/▶ | — | removed; kernel-timeout hazards (group split, prefix chain, generic-width graph builders, skip combinator, unfold-free corollaries) kept as plain sentences; the duplicated prefix-chain explanation now lives once, in the section before `mnv4Pre0`; the "literals vs binder" lesson kept there without the blow-up counts | `grep -rn "def cbReluStridedB\b"` (BatchedStageLayers.lean:376)
MobileNetV4FullBVJP.lean:9 (module) | stale | "what pins the artifact to the reference's function is the pair of ties re-run 2026-09-07 (forward `max |Δ| = 3.770e-06` …)" | net changed to timm parity; current gates named in MobileNetV4FullB header | "The statements are about timm's `mobilenetv4_conv_medium`; the artifacts are pinned to it by `scripts/parity/mnv4_timm_parity.py`, `scripts/parity/mnv4_forward_tie.py` and `scripts/parity/grad_tie.py --net mnv4`." | `ls scripts/parity` (all three exist; grad_tie.py has an `"mnv4"` entry)
MobileNetV4FullBVJP.lean:26 (module), `Mnv4SmoothAt` | wrong | "roughly sixty clauses" ×2 | 38 relu sites (seal `#guard`); stem field + 37 in the seven `.ok` fields | "38 clauses (the relu count `MobileNetV4FullBSeal`'s `#guard` checks)"; "38 relu clauses in total (the stem's plus 37 across the seven `.ok` fields)" | MobileNetV4FullBSeal.lean:58 guard: 1 + 1 + Σ + 2 = 38
MobileNetV4FullBVJP.lean:47 (module) | stale | "the artifacts' `N` is the PER-REPLICA batch (`DataParallel.lean`, §4d)" | DP artifacts use sync-BN; `MobileNetV4SyncB` states replica r = shard r of the forward at `R·N` | "On the data-parallel (sync-BN) artifacts, instantiate at the global batch `R·N` (see `MobileNetV4SyncB`)." | MobileNetV4SyncB module doc
MobileNetV4FullBVJP.lean:~171 `mobilenetv4ForwardBFullHasVJPAt_correct` | overclaim | "⭐ And it IS the `pdiv`-contracted Jacobian … The reading that says the object above is the gradient rather than merely a function of the right type." | proof is `(… HasVJPAt N w x hx).correct dy i`; binder `hx : Mnv4SmoothAt N w x` | "The `.correct` field of `mobilenetv4ForwardBFullHasVJPAt`, restated: at a point satisfying `Mnv4SmoothAt`, its backward is the `pdiv`-contracted Jacobian of `mobilenetv4ForwardBFull`, at every batch size and class count." | statement and proof term read
MobileNetV4FullBVJP.lean (module + 6 docstrings) | process | "(T1, the VJP half)", "(T2)", "the other half of T1, and with it MobileNetV4 has its first net-level tier", "T1's VJP half, and the first net-level tier this net has ever had", "records the measurement", "No new `Foundation` lemma was needed", ~15 ⭐/⚠/⛔ | — | removed | R50 counts kept after re-count: `R50PosB` 17 fields + `R50SmoothAtB` 18 = 35 (awk over ResNet50FullBVJP.lean)
MobileNetV4FullBSeal.lean:~945 `sealX_backward_nontrivial` | overclaim | "`mobilenetv4ForwardBFull`, the forward every MobileNetV4 artifact runs" | eval artifacts take frozen stats (`mnv4{,in}_fwd_eval`, 388 args), s256 eval at 256², `mnv4in_emaacc*` take `%do`; seal at `N = 2` | "the training-BatchNorm forward of the MobileNetV4 train steps (the `mnv4in_emaacc*` ones add classifier dropout), here at `N = 2`" | artifact signatures (python arg count, `grep -c '%do:'`); statement `Fin (2 * nCls)`
MobileNetV4FullBSeal.lean:~62 (§1 `--` comment) | stale (minor) | "`β = 160` at every BatchNorm a relu follows; `β = 0` at the projections" | `sealP` sets `bq2 := kv s.ic 160` at the BN-only pre-DW (no relu follows); `bz2`, `f0pbt` = 0 | "`β = 160` at every BatchNorm except the projections' (`β = 0`, no relu follows them)" | `grep -n "kv … 160\|kv … 0"` on the file (:79, :97, :127)
MobileNetV4FullBSeal.lean (module + 15 docstrings) | process | `planning/full_width_seals.md` §4.4 / §3.5, `planning/mnv4_timm_parity.md`, ~17 ⭐/⚠ | — | refs and markers removed; variable-shape hazard kept as "Note: …" | counts kept: 38 relu (#guard), 17 carrier BNs = 1 + 2 + 3·4 + 2
MobileNetV4StepTieB.lean:25 (module ¶ "every block's `*CotIn_eq_vjp` is its block layer's `.faithful`"), :~803 `mnv4_net_tiedB` first ¶ ("certified head backward, the 21 certified UIB block backwards"), MobileNetV4WholeBackCertifiedTieB.lean:8 | overclaim — deferred §5 | no MNv4 `*CotIn_eq_vjp` exists (`grep -rn CotIn_eq_vjp` hits only MNv2/R34/R50/B0 files) | out of scope: the MNv4 `*CotIn_eq_vjp` row of §5 (D1) will make or rewrite this prose. Left untouched, including its markers and "4.2a/4.2c/§3.5c" refs, so the §5 landing rewrites one intact paragraph | — | `grep -rhoE "theorem [A-Za-z0-9_]*CotIn_eq_vjp"`
MobileNetV4StepTieB.lean:76 (module), :~817 `mnv4_net_tiedB` | stale | "⛔ One replica. Under `mnv4in_adamdp64*` every node named here feeds `allReduceMeanF` (…§4d); this is the per-replica gradient." | DP render is sync-BN; `mnv4_net_syncTiedB` (SyncStepTieB:1305) equates the all-reduced mean with `mnv4_net_tiedB`'s node at `R·N` | "This file describes a data-parallel artifact only at `N := R·N`: the per-replica statement is `MobileNetV4SyncTieB.mnv4_net_syncTiedB`, whose right-hand side is `mnv4_net_tiedB`'s node at the global batch."; capstone adds "One device, f32" and "The AdamW update that follows is not stated here." | read `mnv4_net_syncTiedB` docstring/statement; namespace `Proofs.MobileNetV4SyncTieB` (SyncStepTieB:87)
MobileNetV4StepTieB.lean:~815 `mnv4_net_tiedB` | overclaim (minor) | "MNv4 ships a single resolution, so unlike ResNet-50 there is no `q`" | statement at 224×224; `mnv4in_fwd_eval_s256` runs 256 | "The statement is at 224×224; unlike ResNet-50 there is no resolution binder `q`." | `%x: 64x196608` in mnv4in_fwd_eval_s256
MobileNetV4StepTieB.lean:50 (module heading) | banned phrase / process | "## ⚠⚠ Everything here is GENERIC IN THE ROW, and that is load-bearing"; "records four separate kernel blow-ups" (FullB said three, WholeBack six) | — | "## Everything here is generic in the row"; count dropped: "ends in kernel timeouts (`MobileNetV4FullB.lean`)" | —
MobileNetV4StepTieB.lean (module + ~30 docstrings) | process | "T3 §1a", `planning/mnv4_timm_parity.md`, "Until 2026-09-24 the stem was XLA-`SAME`…", "the §1 fold" ×2 (no §1 in this file), "the granularity T1 and T2 need", "one tier down", ~40 ⭐/⚠/⛔ | — | removed/replaced ("the shared `∀ cot` leaf folds", "the whole-net forward, graph and VJP"); "three other nets'" → "ResNet-34's and EfficientNet-B0's" (the table's certificates are `ResNet34PoCB.*` and `EnetPoCG.*` only) | census re-added: 3 + 6 + 13·12 + 4·9 + 4·6 + 8 = 233; table rows read
MobileNetV4SyncStepTieB.lean:~1287 `mnv4_net_syncTiedB` | overclaim | "so this and it together say the DP step's update is the certified gradient of the global-batch step"; "⛔ Before the render's sync-BN swap … `DataParallel.dpMeanGrad_ne_globalBatchGrad` is the witness, and stays as the statement of what those runs did" | conjuncts are the 233 all-reduced gradient nodes; AdamW/EMA/accumulation not stated; `dpMeanGrad_ne_globalBatchGrad (θ : Vec 1)` is a two-replica toy | "…every all-reduced gradient the DP render emits is the global-batch step's gradient node. The AdamW update that follows (and, in `mnv4in_emaaccdp8x128wxdowd005bf16`, the EMA and gradient accumulation) is not stated here."; "With per-replica BatchNorm the corresponding statement is false in general; `DataParallel.dpMeanGrad_ne_globalBatchGrad` is a two-replica counterexample." The capstone's reliance on the chain being certified is the deferred §5 MNv4 `*CotIn_eq_vjp` item; wording there left as "ties to the certified gradient at its chain cotangent" | statement read; `ls verified_mlir` (emaaccdp artifact); DataParallel.lean:235
MobileNetV4SyncStepTieB.lean, MobileNetV4SyncB.lean (module + decl docstrings) | process | "(T3)", "T3 binds …", "as T3 is" ×2, "T3's" ×6, "(T2)", "T2's data-parallel twin", "Same shape as T2, and for T2's reasons", "the T3 half", bare "P1"/"P2", ~25 ⭐/⚠/⛔ | — | tier labels replaced with the declaration/file they denote (`mnv4_net_tiedB`, `MobileNetV4StepTieB`, `MobileNetV4FullB`, `mnv4FwdGraphBFull_faithful`, `MobileNetV4SyncStepTieB.lean`); "P1"/"P2" glossed as `DataParallelSync`'s sync-BN forward / input-VJP shard identities (the names that file defines); markers dropped; file-internal "(§1)" kept (this file's `-- § 1.`) | `grep -n P1\|P2 DataParallelSync.lean` (:23, :140); `grep -n "^-- § [0-9]"`; `BnPairTiedB` is GradNodesB.lean:154 (reworded "the `BnPairTiedB` nodes `MobileNetV4StepTieB` uses")
MobileNetV4WholeBackCertifiedTieB.lean:~530 `mnv4InputGradB_correct`, :~403 `mnv4InputGradB_eq_mnv4B_full_vjp` | overclaim | "⭐⭐ The chain IS the `pdiv`-contracted Jacobian of the twenty-six-stage net — at every batch size, every class count, every input, every loss cotangent and every input pixel"; "`mnv4InputGradB` IS the certified whole-net MobileNetV4-Conv-M gradient" | hypotheses `h_stem : Mnv4StemSmoothAtB …`, `h_h1`, `h_h2` (head relu clauses), 22 supplied witnesses `hfused`, `hb1 … hb21 : HasVJPDiffAt …`; `fused`, `b1 … b21` are function binders | "…at every input where the stem and both head relu clauses hold (`h_stem`, `h_h1`, `h_h2`): the chain, with its fused-stage and twenty-one block slots filled by the supplied certified VJPs … is the Jacobian-transpose of the composition of those stages. The stages are variables here; `mobilenetv4ForwardBFull_eq_slots` identifies them…"; `_eq_` docstring likewise | signature read (:534–:565); 22 witness binders counted
MobileNetV4WholeBackCertifiedTieB.lean:67 (module), :~408 | stale | "so this is at the per-replica gradient"; "the artifacts' `N` is the PER-REPLICA batch (`DataParallel.lean`, §4d)" | single-device statement; DP artifacts are sync-BN at `R·N` | "**Scope.** One device's batch `N`: the data-parallel artifacts (`mnv4in_adamdp64*`) normalise over the global batch, so this describes them only at `N := R·N`."; "On the data-parallel (sync-BN) artifacts, instantiate at the global batch `R·N` (see `MobileNetV4SyncB`)." | MobileNetV4SyncB module doc
MobileNetV4WholeBackCertifiedTieB.lean:~717 `mobilenetv4ForwardBFull_eq_slots` | wrong | "rows 4/5/10, 12/18 and 15/19/20 — shape-identical" | {4,5,7}, {8,10}, {12,18}, {13,14}, {15,19,20} | "rows 4/5/7, 8/10, 12/18, 13/14 and 15/19/20" | `mnv4Blocks` rows
MobileNetV4WholeBackCertifiedTieB.lean:8 (module ¶ "Tier T6 … certified from its ℝ forward (T1) through … its 233-parameter train-step tie (T3) …") | process (+ deferred §5 overclaim) | tier/session/planning/"closed by user decision on 2026-09-05" narrative, containing the "certified … train-step tie" summary | — | paragraph deleted as process narrative and replaced by what the file does ("Ties the batched input-gradient chain `mnv4InputGradB` to a certified VJP for `mobilenetv4ForwardBFull`"); no new claim about the train-step chain was written, so the §5 MNv4 `*CotIn_eq_vjp` landing has nothing here to rewrite | —
MobileNetV4WholeBackCertifiedTieB.lean (module + 8 docstrings) | process | "(T1)", "(T2)", "T6 wants", "T2's", "(T3)" ×2, "six blow-ups across sessions 1–2", "§4.2d's measured reason", "(§3.10) … would have caught ResNet-34's wrong pool", "`unfold`, three `rw`s, `rfl`", "which is why it takes two seconds", `planning/mnv4_timm_parity.md`, ~20 ⭐/⚠/⛔ | — | removed; the eq_slots proof note moved to a `--` comment in its proof | —
