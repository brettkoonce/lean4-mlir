# Ledger — slice I (ConvNeXt / EfficientNet / ViT)

One row per changed site. `file:line` is the line of the docstring before the edit.

| file:line | kind | old claim | what the statement says | new text | how checked |
|---|---|---|---|---|---|
| ConvNeXt/ConvNeXtWholeBackCertifiedTieB.lean:523 (`convnextImagenetInputGradB_eq_vjp`) | overclaim | "class count of every `convnextin_*` / `convnextsin_*` / `convnextbin_*` artifact … 64 or 128 per device … the whole statement at the artifact" | `w : CnxTWeightsCh 1000`, forward `convNextForwardTCh` (ConvNeXt-T `[3,3,9,3]`, 96-wide, no drop-path), `B` binder, 23 ε-positivity hyps | "class count of the `convnextin_*` (ConvNeXt-T) artifacts … (64 per device) … drop-free `convNextForwardTCh` … `*drop*` and ConvNeXt-S/B (`convnextsin_*`, `convnextbin_*`) compute other functions and are not covered" | `grep -om1 '%x: tensor<…' / '%onehot' / '%psW'` over verified_mlir/convnext*: every convnextin_* train step is 64×150528, 1000 classes, psW 96; convnextsin/bin are 32-batch, bin psW 128; no 128 batch in convnextin_*; peers grepped `theorem r34InputGradB_eq_r34B_full_vjp` etc. |
| ConvNeXt/ConvNeXtWholeBackCertifiedTieB.lean:4–16 (module) | overclaim + process | "Every shipped ConvNeXt artifact … `convnextsin_*` / `convnextbin_*` … T6 … last net without a batched tie (planning/archive/…) … covers all seven nets" | as above; the file proves the batched tie for `convNextForwardTCh` at every `B`, `nC` | Module names ConvNeXt-T, no drop-path, exact arithmetic; drops the S/B families, tier label, planning link and "last net / all seven" status note | read `convnextInputGradB_eq_batchMap_convNextForwardTCh_vjp` signature; `cnx*BAt` names grepped (report's `cnx*B_at` spelling does not exist) |
| ConvNeXt/ConvNeXtWholeBackCertifiedTieB.lean:34–48 (module items 3–4) | process / proof-narrative | ⚠/⛔ rfl recursion note; "~18 min on Lean 4.32.2 … 4.34.0" | — | moved to a `-- Proof-shape notes` comment after the module doc | text moved verbatim, markers dropped |
| ConvNeXt/ConvNeXtWholeBackCertifiedTieB.lean:361,437 | process / proof-narrative | "⭐⭐ THE BATCHED TIE … every step a `rw`, so the kernel replays rewrites"; "⭐⭐ THE APEX … Only the 23 LayerNorm positivities" | tie at every `B`,`nC`,`x` under 23 positivities | stars dropped; tie docstring states the result, rw narrative moved to a `--` comment in the proof; apex docstring names the drop-free ConvNeXt-T forward | read the two signatures (hsε, h1…h4, hd1…hd3, hhε = 1+4+3+1 binders, 23 LN positions per ConvNeXtFullT) |
| ConvNeXt/ConvNeXtWholeBackCertifiedTieB.lean:198,208,236 | process | "⛔ `padOdd` is load-bearing", "⚠ Generic on purpose" | — | plain "`padOdd` is required …", "Note: stated at variable dims because …" | — |
| EfficientNet/EfficientNetStepTieG.lean:11–16 (module, axis 1) | overclaim | "every ImageNet artifact emit[s] … One statement therefore covers AdamW, RMSProp, EMA, the clipped and drop-path variants and their data-parallel and bf16 twins" | `efficientnet_net_tiedG` threads `efficientnetForwardBFull` (no drop-path, no dropout), conjuncts are `*GradB` node dens at one replica | "…the f32 `efficientnetin_*` artifacts emit … The optimizer update … is outside this statement … without drop-path and without classifier dropout, so the `*drop*` / `*do*` artifacts are not covered; the bf16 artifacts emit `*GradBBf16` nodes". Class-count wording left as is (deferred §5, nCls B0) | `ls verified_mlir \| grep ^efficientnet` (drop/do/bf16 names); sibling `EfficientNetSyncStepTieG.lean` module states the same exclusions; `grep def ConvWTiedB` in GradNodesB (gradient, not update) |
| EfficientNet/EfficientNetStepTieG.lean:405 (`efficientnet_net_tiedG`) | overclaim | "all denote the certified batched Σ_n loss-descent step" | each conjunct: `den (…GradB …) = Σ_n Σ pdiv · cot` at the chain cotangent built from witness `.backward`s | "every gradient node … denotes the certified batched gradient `Σ_n Σ pdiv · cot` … at one replica's `*GradB` nodes; the optimizer update and the bf16 `*GradBBf16` nodes are outside it" | read the `let` chain (dy16…dy0 are `HasVJP.backward`s, g is `smoothedLossCotGraph`); `ConvWTiedB` body in GradNodesB.lean:209 |
| EfficientNet/EfficientNetStepTieG.lean:318–326 (Head section + `enetHeadTiedG`) | copied | "then the batched per-row softmax-CE gradient `g = rowSoftmax(logits) − onehot`" / "certified step … `g = rowSoftmax(logits) − onehot`" | `enetHeadTiedG … (g : Vec (N * nC))` — `g` is a binder; conjuncts are gradient nodes | "The loss cotangent `g` is a parameter here"; "gradient nodes … denote the certified gradient … a loss cotangent `g` taken as a parameter" | read signature |
| EfficientNet/EfficientNetStepTieG.lean:150,292 (section docs) | stale | `depthwiseStridedWeightSgdB`, `convStridedXlaWeightSgdB` | conjuncts use `DepthwiseStridedWTiedB` / `ConvStridedXlaWTiedB` over `SHlo.depthwiseStridedWeightGradB` / `SHlo.convStridedXlaWeightGradB` | `…WeightGradB` | `grep -n "den (SHlo" GradNodesB.lean:258,293` |
| EfficientNet/EfficientNetStepTieG.lean (module) | process | 5 items: "T3 §1a", "4b left open (planning/archive/…)", "4b.1's", "4d piece 2 (2026-09-07)", "(2026-09-21)", ⭐×2 ⚠×1 ⛔×2 | — | removed; "⛔ ONE REPLICA" kept as a plain "**One replica.**" scope paragraph; the ⚠ in the head's `let` comment made plain | — |
| ViT/ViTWholeBackCertifiedTieB.lean:290 (`vitTinyInputGradB_eq_vitTiny_vjp`) | overclaim (batch) | "128 or 512 per device in the shipped `vitin_*` artifacts … tier T6 at the paper net and the shipped index" | `B` binder, `Wcls : Mat (3*64) 10`, forward `vitForwardKV` (no drop-path), `hε : 0 < ε` | "(the `vitin_*` artifacts run 32, 128 or 256 per device; 512 is the global batch of the `128x4` runs) … drop-free `vitForwardKV` … `*drop*` artifacts compute another function". "10 classes" left as is (deferred §5, nCls ViT) | `grep -om1 '%x: tensor<…'` over verified_mlir/vitin_*: 32 (2), 128 (12), 256 (1); names `…128x4…` / `…256x2…` |
| ViT/ViTWholeBackCertifiedTieB.lean:4–17 (module) | process + copied | "vitin_* family at 128 or 512 per device"; "closed T6"; "ViT was the one net without a batched whole-net tie; … covers all seven nets"; "4.1c's field-by-field lift" | file proves batched tie for every `B` | per-device numbers dropped from module (stated once on the capstone); tier/§ labels and status sentence deleted; drop-free / exact-arithmetic scope stated | — |
| ViT/ViTWholeBackCertifiedTieB.lean:163,227 | process | "⭐⭐ THE BATCHED TIE", "⭐⭐ THE APEX" | — | stars dropped; apex states drop-free forward and `0 < ε` | `grep def vitKVBHasVJPAt` |
| ConvNeXt/ConvNeXtStepTieGB.lean:13–17 (module, axis 1) | overclaim | "what … every `convnextin_*` artifact emit … One statement covers AdamW, the `wx`/`clip` variants, EMA and the data-parallel twins" | conjuncts are `*GradB` node dens at one replica, drop-free chain; bf16 renders use `*GradBBf16` | "every f32 `convnextin_*` artifact emit … The optimizer update … is outside this statement. The bf16 artifacts emit `*GradBBf16` nodes … not covered" | `ls verified_mlir \| grep convnextin.*bf16` (5 bf16 convnextin artifacts); ConvNeXtFoldGB.lean:45–48 states the bf16 split |
| ConvNeXt/ConvNeXtStepTieGB.lean:350 (`cnx_net_tiedGB`) | overclaim | "at the nodes `convnext_adam_train_step.mlir` and every `convnextin_*` train step emit" | same | "at the `*GradB` nodes … every f32 `convnextin_*` train step emit; the bf16 artifacts emit `*GradBBf16` and are not covered"; one-replica and drop-free scope kept as plain sentences. "certified cotangent chain" wording left (deferred §5, ConvNeXt `*CotIn_eq_vjp`) | read signature (N, nC binders, `t` target) |
| ConvNeXt/ConvNeXtStepTieGB.lean (module + capstone) | process | "T3 §1a", "4b and 4c left open (planning/archive/… §4b, §4c-quater)", "(§4c-quater)", "4d piece 2 (2026-09-07)" ×2, ⭐×4 ⚠×2 ⛔×5, "a declared carve-out" | — | removed; one-replica / drop-free / GAP hand-written text kept as a plain **Scope** paragraph | — |
| ViT/ViTStepTieGB.lean:12–16 (module, axis 1) | overclaim + stale | "every `vitin_*` artifact emit since 4c leg 4 … One statement covers AdamW, … EMA, the 4× accumulation and the data-parallel twins" | `*GradB` node dens, one replica, drop-free; the ViT renders have no accumulation loop | "every f32 `vitin_*` artifact emit … optimizer update … outside this statement. The bf16 artifacts emit `*GradBBf16` … not covered" | `ls verified_mlir \| grep ^vitin.*bf16` (3); `grep -i accum ViTRenderB.lean` → :888/:901 "one shot … removes the need … to grow an accumulation loop"; `vitin_adamdp128x4` = 4 replicas × 128 (ViTRender.lean:602–604) |
| ViT/ViTStepTieGB.lean:49–53, 372 | stale | "the 4× accumulation is `momVNextF` at its other reading on top" | no ViT artifact accumulates; `128x4` is four replicas | deleted; "(four replicas of 128)" added to the one-replica sentence | as above |
| ViT/ViTStepTieGB.lean:359 (`vit_net_tiedGB`) | overclaim | "at the nodes `vit_adam_train_step.mlir` and every `vitin_*` artifact emit" | as axis 1 | "at the `*GradB` nodes … every f32 `vitin_*` artifact emit; the bf16 artifacts emit `*GradBBf16` and are not covered … one replica, drop-free chain, ViT-Tiny's literal dims". "certified … gradient" wording left (deferred §5, ViT `*CotIn_eq_vjp`) | read signature |
| ViT/ViTStepTieGB.lean (module + decls) | process | "T3 §1a", "(§4b.6)", "the 4b capstone that closes the set at five of five", "since 4c leg 4", "(§4c-ter)", "4d piece 2 (2026-09-07)", "(4d)", ⭐×7 ⚠×1 ⛔×4, "honest" framing | — | removed / plain prose | — |
| ViT/ViTStepTie.lean:13–15 (module) | overclaim | "the chain every `vitin_*` accuracy comes from" | `vit_net_tiedGB` is stated at the drop-free chain, `*GradB` nodes | "the chain of the drop-free f32 `vitin_*` artifacts"; added "per example, at one image and a hard label; the artifact runs a batch of 32 …" | `grep -om1 '%x: tensor' verified_mlir/vit_train_step.mlir` → 32; `vit_net_tied_certified` binds `img : Vec (3*224*224)`, `label : Fin 10` |
| ViT/ViTStepTie.lean:326 (`vit_net_tied_certified`) | overclaim (scope) + unverified | "the FIRST net with zero param gaps … 3-axiom clean" | per-example, hard label | scope sentence added (one image, hard label, batch-32 mean outside); status phrase and unverified axiom claim removed. "certified loss-descent step" left (deferred §5, ViT `*CotIn_eq_vjp`) | signature read; no build run, so the axiom claim was not re-verified and is dropped rather than repeated |
| ViT/ViTStepTie.lean (module + sections) | process | "T3 §1a", "Task 3", "§1-fold" ×2 | — | removed | — |
| ConvNeXt/ConvNeXtStepTie.lean:9–17, 564 (module + `cnx_net_tied_certified`) | overclaim (scope) | "`verified_mlir/convnext_train_step.mlir`, measured … the whole 18-block train step is den-composed" (no mention that the statement is per example) | binds `x : Vec (3*224*224)`, `label : Fin 10`; artifact `%x: tensor<32x150528>` | added "per example, at one image and a hard label; the artifact runs a batch of 32 … (the batched form is `ConvNeXtStepTieGB.lean` / `CnxTiePoCGB.cnx_net_tiedGB`)". "certified ∂Loss/∂θ" / "certified loss-descent step" left (deferred §5, ConvNeXt `*CotIn_eq_vjp`) | signature read; `grep -om1 '%x: tensor' verified_mlir/convnext_train_step.mlir` → 32 |
| ConvNeXt/ConvNeXtStepTie.lean:9–13 | stale | "M2/M3/ViT" folds, "the per-example original was deleted 2026-09-08" | imports are MobileNetV2Fold, ResNet34Fold, ViTFold | "`ConvNeXtFold` and the MobileNetV2 / ResNet-34 / ViT folds it imports"; `MobileNetV2TieB.mnv2_net_tiedB` kept as the batched MobileNetV2 peer | import lines 1–7; `grep namespace MobileNetV2StepTieB.lean` → `Proofs.MobileNetV2TieB` |
| ConvNeXt/ConvNeXtStepTie.lean (module + decls) | process | "PoC", "Chapter-7 §1a" ×2, "§1 fold", "restored 2026-08-30", the whole "Superseded scope note" (2026-09-05, §2m, §2n, 2026-08-30), "What is new against the scalar-LN version" history clauses, "honest residual", "declared §5 carve-out" ×2, "(§2m — the pre-§2m render …)", ⚠×1 | — | removed; section retitled "The channel-LN specifics" keeping the per-site facts; ⚠ → "Note:" | — |
| ConvNeXt/ConvNeXtWholeBackCertifiedTie.lean:47–52 (module, apex item) | wrong | "unlike every other whole-net backward tie in this repo it carries no smoothness side-condition" | ViT's `vitInputGradK_eq_vitForwardKV_vjp` takes only `hε : 0 < ε`; B0's `efficientnetInputGradBFull_eq_efficientnetForwardB_full_vjp` only `hεw : w.EpsPos`; R34/R50/MNv2/MNv4 batched ties carry `SmoothAt`/`SmoothAtB` hyps | "like ViT's (…) and EfficientNet-B0's (…), and unlike the ReLU nets' (ResNet-34/50, MobileNetV2, MobileNetV4), it carries no smoothness side-condition" | `grep -rnoE "theorem …InputGradB?_eq_…vjp"` (10 hits) and read the hypothesis lists of each; the four ReLU-net ties grep `SmoothAt`/`SmoothAtB` |
| ConvNeXt/ConvNeXtWholeBackCertifiedTie.lean:54, 71 | stale | "`convNextForwardTCh_eq_chain`, the `rfl` saying…"; "each `cnxTk`" | `convNextForwardTCh_eq_chain` is proved by `rw [convNextForwardTCh]; rw [Function.comp_apply, …]` (ConvNeXtFullT:403–406); no `cnxTk` declaration | "(proved by `rw`)"; "`cnxSavedA k`" | `grep -rnE "(def\|theorem) cnxTk"` → none |
| ConvNeXt/ConvNeXtWholeBackCertifiedTie.lean:4–100 (module) | process + proof-narrative | "⛔ what the tie FOUND", "the finding is the deliverable", planning/archive §3.18/§3.10/§3.14/§3.16/§3.7(d) ×5, "twin-drift pattern … for the third time", "the only net in the repo" (unchecked), ⭐×6 ⚠×3 ⛔×5, build-cost measurements | — | module keeps what is proved (items 1–4) and the even-kernel fact as plain prose; the spelling rule and cost measurements moved to a `-- Proof-shape notes` comment; "the only net in the repo" replaced by the checked "ConvNeXt's 4×4/s4 stem and three 2×2/s2 downsamples are even-kernel sites" | `grep def padOdd/convFlatBack/flatConvStride{2,4}Back` |
| ConvNeXt/ConvNeXtWholeBackCertifiedTie.lean:78,117,134,168–189,227,513 (decl + section docs) | process | ⛔ "load-bearing and not cosmetic … the four sites the whole-net tie found", ⚠ "§3.3-lesson-2 trap", "⭐⭐ THE STAGE-FOLD TIE", section-doc cost log with §3.7(d), "⭐⭐ … IS the certified …" | apex binds `x : Vec (3*224*224)`, `nC` implicit, 23 positivities | markers/§ refs removed; section doc shortened to the wrapper rule with a pointer to the proof-shape notes; apex docstring states one image, every `nC`, 23 positivities, drop-free forward | signature read |
| ViT/ViTWholeBackCertifiedTie.lean:5–7 (module) | stale | "The ViT peer of `r34InputGrad_eq_resnet34_vjp`, `mnv2PaperInputGrad_eq_mobilenetv2Paper_vjp`, …" | neither name exists | "peer of … `convnextInputGrad_eq_convNextForwardTCh_vjp` and … `efficientnetInputGradBFull_eq_efficientnetForwardB_full_vjp`; the batched ties of the other nets are `r34InputGradB_eq_r34B_full_vjp` and `mnv2InputGradB_eq_mobilenetv2B_full_vjp`" | `grep -rnE "(def\|theorem) r34InputGrad_eq_resnet34_vjp\|mnv2PaperInputGrad_eq_mobilenetv2Paper_vjp"` → none; the replacements grepped |
| ViT/ViTWholeBackCertifiedTie.lean:29–31 (module) | wrong | "like ConvNeXt-T and unlike ResNet-34 / MobileNetV2 / EfficientNet-B0 this is a `HasVJP`" | B0's tie is against global `efficientnetForwardBFullHasVJP` under `w.EpsPos` only | "like ConvNeXt-T and EfficientNet-B0 and unlike ResNet-34 / MobileNetV2"; drop-free / exact-arithmetic scope stated | EfficientNetFullWholeBackCertifiedTie.lean:183–185 signature |
| ViT/ViTWholeBackCertifiedTie.lean (module + decls) | process + unverified | "tier T6 of planning/archive/…", "3-axiom-clean", "the retired per-example ResNet-34 tie lacked and ConvNeXt wrote before anyone needed it", ⭐×5, "T6 column" | — | removed; shape check docstring now states "by `rfl`" (checked: the proof is `:= rfl`) | ViTWholeBackCertifiedTie.lean:124 |
| EfficientNet/EfficientNetFullWholeBackCertifiedTie.lean:6–19, 34–36 (module) | stale + process | "`EfficientNetWholeBackCertifiedTie.lean` closed this for the three-block representative … That is what the representative's file could not do — it stopped at a `▸`-transported `_committed` witness"; "the T6 row of planning/archive/… §3.3(c)" | EfficientNetWholeBackCertifiedTie.lean holds only `stemBBack_eq_vjp_backward` and `headFwdBBack_eq_vjp_backward`; no `_committed` witness exists | "The stem and head endpoint ties … are `EfficientNetWholeBackCertifiedTie.lean`'s … the sixteen blocks stay opaque"; history sentences and tier label deleted; hypothesis `w.EpsPos` and drop-free forward stated. Title "paper EfficientNet-B0" / 10-class head left (deferred §5, nCls B0) | `grep -n "^theorem" EfficientNetWholeBackCertifiedTie.lean` (2 theorems); `grep -rn _committed Nets/EfficientNet` → only this docstring |
| EfficientNet/EfficientNetFullWholeBackCertifiedTie.lean:103,178,221 | process | ⭐⭐ ×3, "IS" caps | — | markers dropped | — |
| EfficientNet/EfficientNetWholeBackCertifiedTie.lean:13–16 (module) | process | "⭐ … the planning note that said it would is withdrawn … §5's standing trap" | — | "**`batchMapHasVJP`'s transport does not block the reduction.** … an `Eq.mpr` can block `.backward` from reducing. It does not here" | — |
| ConvNeXt/ConvNeXtFullT.lean:23–24 (module) | stale | "the whole-net VJP is GLOBAL (unconditional except the 22 LN positivities) — ConvNeXt-T joins `efficientnetForwardBFullHasVJP` and `vitForwardKV`" | `convNextForwardTChHasVJP` binds hsε, h1..h4 (3+3+9+3), hd1..hd3, hhε = 23 | "under the 23 LN positivities (1 stem + 18 block + 3 downsample + head) … as are `efficientnetForwardBFullHasVJP` and `vitForwardKVHasVJP`" | counted binders of `convNextForwardTChHasVJP` (ConvNeXtFullT.lean:294–299); `vitForwardKV` is a forward, its witness is `vitForwardKVHasVJP` |
| ConvNeXtFullT.lean:58–62 (section doc) | stale | "the reference's 22 LN sites are 1 stem + 18 block + 3 downsample … So this forward has a stem LN and no head LN" | `convNextForwardTCh` applies `rowLNVecFlat 1 768 w.hε w.hγ w.hβ` after GAP | "The LN sites here are 1 stem + 18 block + 3 downsample (all channel-LN) + the head LN after GAP" | ConvNeXtFullT.lean:270–283 |
| ConvNeXtFullT.lean:563 (`convNextFwdGraphTCh_faithful`) | stale | "Same `rw` chain as the scalar apex, with `chanLNGraph_faithful` where the `bnF`s were" | proof: `rw [convNextFwdGraphTCh, denseF_faithful, headLNGraph_faithful, gapF_faithful, cnxStageChGraphK_den, cnxDownChGraphW_faithful ×3, …, chanLNGraph_faithful, flatConvStride4F_faithful, den_operand]; rfl` | "One `rw` per stage: `cnxStageChGraphK_den` / `cnxDownChGraphW_faithful` … `chanLNGraph_faithful` at the stem LN and `headLNGraph_faithful` at the head" | proof read |
| ConvNeXtFullT.lean:257–258 (`CnxTWeightsCh.hε` field) | stale | "whose `_diff`" | lemma is `rowLNVecFlat_differentiable` | `rowLNVecFlat_differentiable` | `grep theorem rowLNVecFlat_differentiable` (ChannelLN.lean:83) |
| ConvNeXtFullT.lean:387 (`convNextForwardTCh_eq_chain`) | copied | "see `convNextForwardTCh_eq_chain` for why the proof shape matters" (points at itself) | nested form = twelve-factor chain | "`convNextForwardTCh w x` equals the twelve-factor `∘` chain `convNextForwardTChHasVJP` is stated on"; kernel note moved to a `--` comment in the proof | statement read |
| ConvNeXtFullT.lean:266 (`convNextForwardTCh`) | overclaim (mild) | "every theorem about this forward covers both" [10 and 1000 classes] | forward is drop-free | "It has no drop-path; the `*drop*` artifacts compute another function"; "as the scalar peers" (deleted) removed | definition read |
| ConvNeXtFullT.lean (module, section doc, `CnxTWeightsCh`, `convNextForwardTChHasVJP`, graph docs) | process | "ch9 representative", planning/archive ×4, §2m/§2n/§2f/§2a/§7.1/§7.2 ×12, dates ×6, ⚠×8 ⛔×2 ⭐×1, "THE HEAD LN CAME BACK … the history is the point" | — | module and docstrings state the net; the scalar-LN retirement list and the head-LN parameter-count history moved to `--` maintainer comments; ⚠ in the head-LN site → "Note:" | 28,587,592 / 28,589,128 / 1,536 moved verbatim, not re-derived |
| ConvNeXt/ConvNeXtBackB0.lean:14–19 (module) | stale | "ConvNeXt's whole verified stack is **per-example / batch-1** — LayerNorm here is the per-example separable `layerNormForward` … so NONE of EfficientNet's `batchMap`/`bnBatchLA` batched machinery is needed" | every decl is over `chanLNTensor3`; batched ties `cnx_net_tiedGB` / `convnextInputGradB_eq_batchMap_convNextForwardTCh_vjp` use `batchMap`/`batchMapAux` | "This file's graphs are per-example: … channel LayerNorm (`chanLNTensor3`) is per-example separable … the batched ties … are plain `batchMap`s of these" | `grep layerNormForward ConvNeXtBackB0.lean` → none; batched decls grepped |
| ConvNeXtBackB0.lean:10–11 | stale | "`r34*BackBatchedGraph_faithful`" (family glob) | members: `r34BasicBlockBackBatchedGraph_faithful`, `r34DownBlockBackBatchedGraph_faithful` | both named | `grep -rnoE "theorem r34[A-Za-z]*BackBatchedGraph_faithful"` → 2 |
| ConvNeXtBackB0.lean:126 (`cnxBlockBodyChBackGraph_faithful`) | stale | "Same proof as the scalar peer with `chanLNBackGraph_eq_vjp` where `bnBack_faithful_fn` was" | proof: `rw [depthwiseBack_faithful, chanLNBackGraph_eq_vjp]; simp only [cnxBodyWithHasVJP, convBack_faithful, geluBack_faithful, layerScaleF_faithful]; rfl` | describes that proof | proof read |
| ConvNeXtBackB0.lean:197 (`cnxBlockChLayer`) | copied | "`cnxResidBlockChBackGraph_faithful` is described in `ConvNeXtBackB0` as …" (inside ConvNeXtBackB0) | `faithful := … cnxResidBlockChBackGraph_faithful …` | "with `cnxResidBlockChBackGraph_faithful` as its `faithful` field" | def body read |
| ConvNeXtBackB0.lean (module + decls) | process | "§2o Part A, 2026-07-31", "§2m" ×2, "§B" ×4, "§2n" ×2, "the reason landing §B first was worth doing", "the keystone the §2n drop left uncovered", ⭐×1 | — | removed; §B citations replaced by `chanLNTensor3Back_eq_chanLN_vjp` | `grep theorem chanLNTensor3Back_eq_chanLN_vjp` (ConvNeXtBackCertifiedTie.lean:102, namespace `Proofs`) |
| ConvNeXt/ConvNeXtChainClose.lean:19, 28, 59 (module ×2, `cnxCotN`) | stale | "where the scalar-LN `γ/β` grads read it"; "(layer-scale `γ`, scalar-LN `γ/β`)"; "the cotangent the scalar-LN `γ/β` grads contract with — the substantive LN pin" | consumers are `CnxPoC.ChanLNGammaSgdTied`/`ChanLNBetaSgdTied` at `Vec c` γ/β (via `ConvNeXtStepTie.cnxBlockChTied`) | "channel-LN (`chanLNTensor3`, `Vec c` γ/β)" in all three | `grep -n cnxCotN ConvNeXtStepTie.lean` (feeds chanLn γ/β ties) |
| ConvNeXtChainClose.lean:4–11 (module) | stale + process | "ConvNeXt Item D", "Item C param bridges (… M2/M3 …)", planning/archive link, "Pure-Lean, batch-1 — … none of EfficientNet's batched-VJP machinery (`batchMapHasVJP`) is needed", "Item B render" ×2 | the batched tie `ConvNeXtStepTieGB` lifts these with `batchMapAux` | item labels and planning link dropped; "The definitions are per example; … the batched tie lifts them with `batchMapAux`" | ConvNeXtStepTieGB.lean module (axis 3) |
| ConvNeXt/ConvNeXt.lean:33–41 (module) and 62–67 (`convNextBlockBody`) | stale | "true ConvNeXt LayerNorm is per-spatial-position … a faithful channel-LN-over-NCHW lift is a follow-up" | the lift exists: `chanLNTensor3` (Architectures/ChannelLN), `cnxBodyWith` at it, `convNextForwardTCh` (ConvNeXtFullT.lean) | "ConvNeXt's LayerNorm is per spatial position over the channel axis; that is `chanLNTensor3` … the full ConvNeXt-T at it is `convNextForwardTCh`"; block-body doc points at `cnxBodyWith` | `grep def cnxBodyWith/chanLNTensor3/convNextForwardTCh` |
| ConvNeXt.lean:7–8, 181, 189 | overclaim (category word) | "A representative ConvNeXt block and a small end-to-end ConvNeXt VJP"; "(representative, fixed block count = 2)"; "Same LN representation caveat" | `convNextForward`: 1×1 stem conv, scalar-γ/β `layerNormForward` stem/head LN, two blocks | "a two-block end-to-end ConvNeXt VJP with scalar-`γ, β` LayerNorm … The comparator checks this net; the full ConvNeXt-T … is `convNextForwardTCh`"; "(two blocks, scalar-`γ, β` LayerNorm)"; the "exact" claim narrowed to "the same map as in ConvNeXt-T" with the 1×1 stem named | definition read (ConvNeXt.lean:190–209); comparator row `convnextHasVJP` per doc_honesty_pass.md "What went wrong" |
| ConvNeXt.lean:25–29 (module list) | stale | "a pointwise VJP built by chaining the piece VJPs through `vjpCompAt`"; lists only `convnextHasVJPAt` | `convNextBlockBodyHasVJP` is global, five `vjpComp`s; `convnextHasVJP`/`convnextHasVJP_correct` exist | "global VJP … through `vjpComp`"; lists `convnextHasVJP` / `convnextHasVJP_correct` and the pointwise pair | proof body read (ConvNeXt.lean:102–137); `grep def convnextHasVJP` |
| ConvNeXt.lean:215 | overclaim (mild) | "alongside `vitFullHasVJP` as an unconditional whole-network VJP" | `vitFullHasVJP` is the VJP of "the weight-tied `vitFull`" (Attention.lean:2090) | "`vitFullHasVJP` (the weight-tied ViT)" | Attention.lean:2090 docstring |
| ConvNeXt/ConvNeXtFoldGB.lean:7 (module) | stale | "`ConvNeXtFoldG.lean` folds the fourteen gradient nodes of the PER-EXAMPLE traversal" | ConvNeXtFoldG has 3 theorems (`layerScaleChGammaGrad_den`, `chanLnGammaGrad_den`, `chanLnBetaGrad_den`) | "holds the three per-example lemmas specific to this net (layer-scale γ, channel-LN γ/β); the other node kinds are shared (`GradNodesB`)" | `grep -c "^theorem" ConvNeXtFoldG.lean` → 3 |
| ConvNeXtFoldGB.lean:19–21 | stale | "4c leg 3 moves the drop-free writers onto this chain; this file lands first" | ConvNeXtRenderB.lean:716–722 says the drop-free writers moved; ConvNeXtFoldG header "Every Adam artifact of this net renders from the batched chain" | "…`ConvNeXtRenderB.convNextBackAllB`'s constructors, from which every ConvNeXt Adam artifact renders" | files read |
| ConvNeXtFoldGB.lean:58–62 | stale | "Re-pointing it at these nodes with `SmoothedLossCot` is 4b's ConvNeXt capstone, which this file is the prerequisite for" | `CnxTiePoCGB.cnx_net_tiedGB` exists | "The tie at these nodes … is `CnxTiePoCGB.cnx_net_tiedGB`" | `grep theorem cnx_net_tiedGB` |
| ConvNeXtFoldGB.lean:50–52 | overclaim (mild) | "One lemma per op kind certifies every optimizer tail at once — AdamW, … drop-path and the data-parallel twins" | lemmas are `∀ cot` node dens; bf16 kinds excluded (stated two paragraphs up) | "One lemma per f32 node kind … the optimizer update that consumes the node is outside these lemmas" | lemma statements read (`chanLnGammaGradB_den` etc.) |
| ConvNeXtFoldGB.lean (module) | process | "T3 §1", "4b's … was, for ConvNeXt, a statement about the Imagenette pair only", "owed BEFORE any renderer swap", "per leg 1's ordering rule", "(first stated in this file, 2026-09-07)", "Honest residual", "(4d)", ⭐×4 ⚠×1 ⛔×1 | — | removed; "Honest residual" → "Scope" | — |
| ViT/ViTFoldGB.lean:5 (module) | stale | "`ViTFoldG.lean` folds the ten gradient nodes of the PER-EXAMPLE traversal" | ViTFoldG has 2 theorems (`posEmbedGrad_den`, `clsGrad_den`) | "holds the two per-example lemmas specific to this net …; the other per-example peers are the fused `ViTPoC` lemmas" | `grep -c "^theorem" ViTFoldG.lean` → 2; the op table's third column lists the `ViTPoC` peers |
| ViTFoldGB.lean:44–47 | stale + overclaim | "AdamW, the `wx`/`clip` variants, the EMA shadow and the 4× accumulation all consume the same `*GradB` node" | no ViT accumulation loop (see ViTStepTieGB row); lemmas are `∀ cot` node dens | "One lemma per f32 node kind … the f32 AdamW, `wx`/`clip` and EMA artifacts all emit these `*GradB` kinds, and the optimizer update … is outside these lemmas" | as ViTStepTieGB row |
| ViTFoldGB.lean (module) | process | "T3 §1", "4c leg 4" ×3, "Measured 2026-09-07: all nineteen …" (count not re-derived, dropped), "the whole point of the leg", a quoted ViTRenderB comment, "planning/imagenet_parity.md VT-5", "the scalar-affine spelling this cone was caught on three times", "(4b.7)", "§1a", "(4d)", "Honest residual", ⭐×5 ⚠×1 | — | removed; "Honest residual" → "Scope"; byte-identity kept as a present-tense fact pinned by tests/TestBatchedEmitTie.lean | — |
| ViT/ViTDepthK.lean:24–25 (module) | stale | "Depth-12 ViT-Tiny shapes are now a config change away (the production capstone needs only the P=16/D=192/heads=3 instantiation of these)" | `vitTinyHasVJP_correct` (ViTDepthK.lean:256) is that instantiation | "The ViT-Tiny instantiation (depth 12, P=16, D=192, heads=3) is `vitTinyHasVJP_correct`"; "scaling pass" dropped from the title | `grep theorem vitTinyHasVJP_correct` |
| ViT/ViTVecLN.lean:234 (`vitCotB2outV`) | stale | "Cot at block 2's output" | used as `dy12` at `b12out` in `vit_net_tied_certified` (depth 12); its argument is the input of the final LN | "Cot at the last block's output (the final-LN input)" | ViTStepTie.lean `let dy12 := vitCotB2outV … b12out g` |
| EfficientNet/EfficientNet.lean:41 (`convBnSwishHasVJP`) | stale | "Like `convBnRelu`" | no `convBnRelu`; the relu peer is `convBnReluHasVJPAt` (CNN.lean:381) | "Like `convBnReluHasVJPAt`" | `grep -rnE "def convBnRelu\b"` → none |
| EfficientNet/EfficientNet.lean:24–26 (module) | stale + category word | "`efficientnetHasVJPAt` / `_correct` — a representative end-to-end EfficientNet … built by `vjpCompAt`" | `efficientnetHasVJP` is global, `vjpComp`-built; `efficientnetHasVJPAt := (efficientnetHasVJP …).toHasVJPAt x` | "`efficientnetHasVJP` / `efficientnetHasVJP_correct` — a two-block end-to-end EfficientNet … global `HasVJP` built by `vjpComp` …; `efficientnetHasVJPAt` / `efficientnetHasVJPAt_correct` restrict it to a point by `.toHasVJPAt`. The full sixteen-block B0 is `efficientnetForwardBFull`" | EfficientNet.lean:242, 297–316 read; two MBConv blocks (subscripts ₁ ₂) |
| EfficientNet/EfficientNet.lean:240 | overclaim (mild) | "alongside `vitFullHasVJP` and `convnextHasVJP`" | `vitFullHasVJP` is the weight-tied ViT | "`vitFullHasVJP` (the weight-tied ViT)" | Attention.lean:2090 |
| ConvNeXt/ConvNeXtBackChains.lean:39–40 (`cnxBlockBodyBack`) | stale | "`lnB` the LayerNorm back (= BN-back)" | ties fill `lnB` with `chanLNTensor3Back` (`cnxBodyWithChanLNBack_eq_vjp`) | "(the ties fill it with `chanLNTensor3Back`)" | ConvNeXtBackCertifiedTie.lean:124 |
| ConvNeXtBackChains.lean:33, 68 | copied | "input-gradient VJP at a smooth point" (ConvNeXt has no kink; the ties are global `HasVJP`) | — | "at a smooth point" dropped | `convNextForwardTChHasVJP` is `HasVJP` |
| ConvNeXtBackChains.lean (module + `convnextInputGradB`) | process | "⚠ Padding is SYMMETRIC", "Moved here … on 2026-09-08 (planning/archive/…)", "THE BATCHED …" caps, "the honesty argument … for the batched T3 tie" | — | plain "Note:"; the even-kernel `padOdd` link added; date/planning link dropped | — |
| EfficientNet/EfficientNetBackNet.lean:6–11, 26–38, 40, 66 | process | "planning/archive/mnv4_verified.md §8e swept the repo …", verdict table with "never a hole", "§8e over-counted … §4c(a) … measurement bugs", "genuinely new" ×2, "the last stage-level hole in the repo", ⭐×2 | the file proves `mbNoExpBackBatchedGraph_faithful` and `headBackBatchedGraph_faithful`; the other two stage forwards are covered by `mbDownBodyBackBatchedGraph_faithful` / `mbBodyBackBatchedGraph_faithful` (EfficientNetBackB0.lean:314, 270) | module states the two theorems; table kept as "where each stage forward's backward graph lives"; sweep history and "last hole" status removed | `grep theorem` for all four names |
| EfficientNet/EfficientNetFullB0Eval.lean:9–18, 23, 313 | process + stale | "T2 at inference BatchNorm", "(Built 2026-09-05 … deleted 2026-09-08 … planning/archive/… 3.3(e))", "The one genuinely new piece … which the three-block representative has no instance of and `EfficientNetFullB0.lean` added at training BN as `mbExpFwdB`", ⚠, ⭐ | `mbExpFwdB` is defined in EfficientNetChainClose.lean:79 | history removed; "the eval twin of `mbExpFwdB` (`EfficientNetChainClose.lean`)"; ⚠ → "Note:" | `grep -rnE "def mbExpFwdB\b"`; 312 inputs re-counted: `grep -m1 func.func verified_mlir/efficientnet_fwd_eval.mlir \| grep -o "%[A-Za-z0-9_]*:" \| wc -l` → 312 |
| EfficientNet/EfficientNetSyncStepTieG.lean (module + 7 decl/section docs) | process | "T3" ×14 as a name for `EfficientNetStepTieG`'s statement, "neither tier states", "(§ 3 moved there)", ⭐×8 (incl. ⭐⭐⭐), ⚠×4 | — | "T3" → "the single-device tie"; "neither file states"; history parenthetical dropped; ⚠ → "Note:"; intra-file § refs (§ 0, §§ 2, 5) kept; lines rewrapped to 100 cols | `grep -c` of markers before/after; scope content (213 collectives, 49 conv-bias conjuncts, drop/dropdo and bf16 exclusions) unchanged |
| EfficientNet/EfficientNetSyncB.lean (module + 2 decls) | process | "(T2)", "T2's" ×2, "the T3 half", "T2 does not state", ⭐×2 (incl. ⭐⭐), ⚠×4 | — | tier labels → "its data-parallel twin" / "the step half" / "this file does not state" / "The single-device `efficientnetFwdGraphBFull`"; markers removed | — |
| EfficientNet/EfficientNetStepTie.lean, EfficientNetFold.lean, EfficientNetChainClose.lean, EfficientNetBackChains.lean (module docs) | process | "§1a" ×2, "§1-fold", "PoC:", "the §1 fold", "Item D" ×2, "Item A", "per the plan", ⚠×1 | — | labels removed; ⚠ → "Note:" | — |
| ViT/ViTBackNet.lean:4–53 (module) | process + wrong comparative | "the last net onto `CertLayer`, and the first one folded END TO END", "(ResNet-34/50 now fold image → logits too … ViT was first)", "exactly as the recipe predicted", "One seam remains and is NOT this refactor's … the next thing to generalize", ⭐×2 ⚠×2 | the file proves `vitTrunkV_graph`, `vitTrunkV_fwd`, `vitNetLayer_graph`, `vitNetBackGraph_faithful` | module states what is derived and the scope ("a certified composition … not a proof that vit_train_step.mlir is this graph"); history/firsts removed; the remaining `den e` seam kept as a plain "Note:" | theorem names grepped |
| ViTBackNet.lean:105,119,149–154,168,208–210,244,301–306 (decls) | process + unverified | "⭐⭐ THE PAYOFF", "the smooth tier's payoff", "(r50's is 3 clauses per block × 16 blocks)", "⚠ Not a weaker statement … a stronger one", "**The first net in the repo whose fold is the entire network.**", "proven once for all seven nets", ⭐×9 | — | markers/firsts removed; the unverified r50 clause count dropped; "proven once in `CertifiedChain`" | — |
| ViT/ViTBackB0.lean:109,659,785,893,921 (decls) | process | "⭐ … the old statement is this one at …", "⚠ **This used to bundle the classifier back inside it** … the same mistake as …" | — | stated as what the declaration takes now ("Stated over an arbitrary incoming-cotangent subgraph `ecot` …"); history removed | — |
| ViT/ViTBackChains.lean:12, 90; ViTChainClose.lean:29, 32; ViTBackB0.lean:30, 208, 248; ViTMultiHeadChain.lean:6, 24, 26; ViTMhsaBackCertifiedTie.lean:11 | stale | `sdpa_back_{Q,K,V}`, `vitCotD{Q,K,V}_eq_sdpa_back_{Q,K,V}`, `sdpa_back(dconcat)` | declarations are `sdpaBackQ/K/V` (Attention.lean:449…) and `vitCotDQ_eq_sdpaBackQ` / `vitCotDK_eq_sdpaBackK` / `vitCotDV_eq_sdpaBackV` (ViTChainClose.lean:122–154) | `sdpaBack{Q,K,V}`, `vitCotD{Q,K,V}_eq_sdpaBack{Q,K,V}` | `grep -rnE "(def\|theorem) (sdpa_back_Q\|sdpaBackQ)…"`; `mhsa_backward_collapseMH` still exists and was left |
| ViT/ViTBackChains.lean (module + 2 decls) | process | ⚠×2, "batched T3 tie" ×2, "Moved here … on 2026-09-08 (planning/archive/…)", "honesty argument", "THE BATCHED …" caps | — | removed / plain | — |
| ViT/ViTChainClose.lean:4–12, 34 (module) | process + unverified | "Item D" ×2, "Item C", "Item B", planning/archive link, "Pure-Lean, batch-1", "3-axiom clean" | batched tie lifts these with `batchMapAux` (ViTStepTieGB axis 3) | labels removed; per-example scope stated; unverified axiom claim dropped | — |
| ViT/ViTMhsaBackCertifiedTie.lean, ViTVecLNBackCertifiedTie.lean (module + capstone docs) | process + unverified | "§B:" titles ×2, "§B leaf", "§B tie" ×3, "package 3.1's move", "3-axiom-clean" ×2, ⭐×2 ⚠×1, caps headings | — | labels → "backward leaf/tie"; "the same move as `ConvNeXtBackCertifiedTie.lean` makes for ConvNeXt-T"; unverified axiom claims dropped (no build run) | — |
| ViT/ViTFold.lean, ViTFoldG.lean, ViTFoldGB.lean (decls), ViTMultiHeadChain.lean | process | "§1 fold", "§1a tie", "the M2 `Cifar8PoC…`", "§1-fold"; ⚠×6 ⚠⚠×1 ⭐×2 in decl docs; "`den_patchEmbedBiasGradB`'s neighbour warns …" | — | labels removed; markers → "Note:" or dropped | — |
| ConvNeXt/ConvNeXtBackCertifiedTie.lean (module + 3 decls) | process + stale + unverified | "§B:" title, "closes §B", "THE §2n §B TIE", "§B … BODY/BLOCK tie", "the channel-LN net's §B coverage matches the scalar net's … (new, because …)", "3-axiom-clean" ×3 | scalar-LN net retired (ConvNeXtFullT); the three ties are `chanLNTensor3Back_eq_chanLN_vjp`, `cnxBodyWithChanLNBack_eq_vjp`, `cnxBlockChBack_eq_vjp` | labels removed; comparison to the retired scalar net replaced by naming the three ties; unverified axiom claims dropped | `grep theorem` for the three names (ConvNeXtBackCertifiedTie.lean:102,124,162) |
| ConvNeXt/ConvNeXtFold.lean (module); EfficientNet/EfficientNetBackB0.lean:27 | process + unverified | "§1 fold" ×2, "M2 / M3 certs"; "3-axiom clean" | — | "the fold"; "the existing conv and dense certs"; axiom claim dropped | — |
| ViT/ViTMultiHead.lean:9 | wording | "the load-bearing tie" | — | "the general-`heads` tie, on which the rest of the file builds" | phrase banned by the user |
| ConvNeXt/ConvNeXtStepTie.lean:78–79 (block section doc) | stale | "Only that last-but-one step differs from the scalar-LN thread" | scalar-LN thread retired | sentence dropped | ConvNeXtFullT maintainer note lists the retired names |

## Deferred §5 (not reworded; the Lean change will make or restate them)

| file:line | claim left as is | §5 row |
|---|---|---|
| EfficientNet/EfficientNetStepTieG.lean module + `efficientnet_net_tiedG` | "the f32 `efficientnetin_*` artifacts" (capstone is at `B0Weights`' 10-class head, `t : Vec (N * (1 * 10))`) | nCls B0 |
| EfficientNet/EfficientNetFullWholeBackCertifiedTie.lean title | "paper EfficientNet-B0 gradient" (head `Mat 1280 10`) | nCls B0 |
| EfficientNet/EfficientNetSyncB.lean, EfficientNetSyncStepTieG.lean | `SHlo (N * 10)` / 10-class statements described without the class-count caveat | nCls B0 |
| ViT/ViTWholeBackCertifiedTieB.lean `vitTinyInputGradB_eq_vitTiny_vjp` | "10 classes" beside the `vitin_*` batch sizes (1000-class artifacts) | nCls ViT |
| ConvNeXt/ConvNeXtStepTie.lean module "181 … at the full `θ − lr·(certified ∂Loss/∂θ)` step", `cnx_net_tied_certified` "certified loss-descent step" | chain cotangents `cnxBlockCotInChAt` etc. have no `*CotIn_eq_vjp` | ConvNeXt `*CotIn_eq_vjp` |
| ConvNeXt/ConvNeXtStepTieGB.lean `cnx_net_tiedGB` "every block's certified cotangent chain … certified batched `Σ_n` gradient" | same | ConvNeXt `*CotIn_eq_vjp` |
| ViT/ViTStepTie.lean `vit_net_tied_certified` "EVERY param op `den`otes the certified loss-descent step"; ViTStepTieGB `vit_net_tiedGB` "certified batched `Σ_n` gradient" | no `vit…CotIn_eq_vjp` | ViT `*CotIn_eq_vjp` |
