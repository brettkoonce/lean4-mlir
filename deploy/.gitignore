# Generated by build_orin.sh: the batch-1 graph, the compiled module, and the
# staged weights. All reproducible from a checkpoint; params.bin alone is 86 MB.
build/

# testdata/ DOES ship — frame.png is the smoke test's reference input
!testdata/
